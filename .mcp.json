{
  "mcpServers": {
    "nexus": {
      "command": "${CLAUDE_PROJECT_DIR:-.}/.venv/bin/nexus",
      "args": [
        "serve",
        "--project-root",
        "${CLAUDE_PROJECT_DIR:-.}"
      ]
    },
    "zotero": {
      "command": "zotero-mcp",
      "env": {
        "ZOTERO_LOCAL": "true"
      }
    }
  }
}
