# Method Implementer — Memory Index

Lessons live in `lessons.md`; phase records live in plans and issues, not here.

- [lessons.md](lessons.md) — six lessons no rule or skill carries: two typing traps (L-001), the seam instances of a crosswalk (L-006), a source-hash sentinel is not rename-transparent (L-008), a codomain re-point's wider blast radius (L-009), the curvilinear operator's non-separability (L-013, `[R]` owed a re-measure against `R = R_spatial ⊗ A_angular`), a quotient's two realizations (L-016).
- [ld_curvilinear_shape.md](ld_curvilinear_shape.md) — the derived shape of the curvilinear LD angular-closure member; read before the `AngularClosure` carve (deferred by the user).
- The math-heart classes, one record each: [r2_billiard_class.md](r2_billiard_class.md) (`Billiard`) · [fn_method_moment_space.md](fn_method_moment_space.md) (`MomentSpace`) · [spectrum_singular_eigenfunction.md](spectrum_singular_eigenfunction.md) (`Spectrum`) · [basis_space_galerkin_spectral.md](basis_space_galerkin_spectral.md) (`BasisSpace`). Their `TransportSolver` Protocol sentences are stale: the Protocol was deleted at `626b950a`.
