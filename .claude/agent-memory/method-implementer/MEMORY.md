# Method Implementer — Memory Index

Slim index. Behavioral lessons live in `lessons.md` (read FIRST each
session). This index holds only (2) live resume-state and (3) durable
design pointers. Campaign play-by-play is in the topic `*_closeout.md`
files — do NOT inline it here.

## 1. Lessons (read first)

- [lessons.md](lessons.md) — 13 behavioral lessons. The spine: a
  prototype is not done at "value is right" but at "value right AND
  pyright clean by construction AND no orphan left AND a mutation
  reddens the gate." Pyright-principled-over-suppression (L1/L2),
  retirement blast radius = graph+grep+constructors (L3/L4), the
  convention-crosswalk-before-carve discipline (L6), Mode-10 structural
  teeth (L7), affinity-verify-first (L11).

## 2. Active / in-flight state

- **`feature/field-typed-operator-algebra` (#257 S-series, NOT committed
  — main agent commits after review):** the field-typed operator-algebra
  refactor (numerics §5.6/§5.7 category layer). Shipped through S10b:
  `Functional`/`IntegralKernelOperator` categories, `MultiplicationOperator`
  (`C=M[σ_t]`), pure σ-free `StreamingOperator`, `EmissionSpectrum`
  value-object + production-weighted `chi_mix`, ERR-063 (χ source/sink swap)
  promoted. Latest closeouts: `issue_257_s10b_*`, `issue_257_s10a_*`,
  `issue_257_s9_*`. Each S-stage closeout carries its own STATUS block.
- **`feature/sn-space-angle-tier2` (#240/#38/#37 D5b-S3, NOT committed):**
  the LD spatial-moment iterate fold. IN-FLIGHT BLOCKER = typed-`Field`
  space-shape widening to hold the φ̂ moment axis across `(L+C)−S` (the
  Pattern-4 shape gate the crosswalk under-scoped). DONE: `SpatialMomentSpace`
  factor minted, Σ_s⊗I scattering-lift bridge (byte-id at per_axis==1),
  d=1 CumprodScan moment-source fix (ERR-061 sweep-side analog), pure-z
  matvec guard (ERR-062). Resume = `issue_240_d5b_s3_a_inc_c_closeout.md`
  §BLOCKER + the campaign plan `.claude/plans/issue_240_phase2_*`.
- **`refactor/sn-foundation-cleanup` (#247/#251, may be merged — verify):**
  bulk + boundary moment-source consumption (Mode-10 closures). Closeouts
  `issue_247_legA_*`, `issue_251_legB_*`.

> Before resuming any of these: `git log main..<branch>` to confirm what
> actually landed — these notes freeze at write time.

## 3. Durable reference (reusable design)

- [origin_context.md](origin_context.md) — why this agent exists; the
  A1+A2 case study that motivated the Branch-1/Branch-2 bifurcation loop.
- [capability_matrix_framework.md](capability_matrix_framework.md) — the
  `capability_rows()` meta-generator + auto-discovery (algebra-of-record
  "Capability matrices"); how a richly-populated derivations package
  surfaces its reference inventory to Sphinx.
- [transport_solver_protocol_landing.md](transport_solver_protocol_landing.md)
  — the `TransportSolver` Protocol + `DiscretizationSpec`; structural
  unification of the math-heart classes with their discrete adapters.
- The math-heart class family (one reusable instance each of the
  "math-rich teaching class" pattern): [r2_billiard_class.md](r2_billiard_class.md)
  (Billiard) · [fn_method_moment_space.md](fn_method_moment_space.md)
  (MomentSpace) · [spectrum_singular_eigenfunction.md](spectrum_singular_eigenfunction.md)
  (Spectrum) · [basis_space_galerkin_spectral.md](basis_space_galerkin_spectral.md)
  (BasisSpace).
