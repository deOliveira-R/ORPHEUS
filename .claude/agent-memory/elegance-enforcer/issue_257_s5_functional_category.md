---
name: issue-257-s5-functional-category
description: #257 S5 Functional category (§5.6 suffix law) elegance review — PASS-WITH-NITS, the co-vector mirror of Vector
metadata:
  type: project
---

#257 S5 — the §5.6 `Functional` category. PASS-WITH-NITS (`feature/field-typed-operator-algebra`, HEAD `b404ae1`, UNCOMMITTED, BIT-IDENTICAL additive; 21✓/1 skip; 0-ULP byte-id verified independently).

**What landed.** NEW `numerics/functional.py` (L1): `Functional(Protocol[V_contra, R_co])` `@runtime_checkable`, single method `evaluate(self, x, /) -> R_co` — the co-vector companion of `Vector`, a SIBLING of `LinearOperator` (NOT subclass; disjoint surface — no `apply`/`capabilities`). NEW `transport/production_rate_functional.py` (L2): `ProductionRateFunctional(nu_sigma_f: CrossSectionField)` frozen dataclass, `evaluate(phi) = (nu_sigma_f.values * phi_values).sum(axis=0, keepdims=True)` — per-cell fission emission density `p(r)=Σ_g νΣf_g φ_g`, group-collapsed, NO measure, NO χ. Doc stub `operator_algebra.rst` `:label: production-rate-functional` + honest `.. todo::` (archivist deferred to post-S6).

**Why this is PASS (the rulings worth keeping).**
- Two-typevar (`V_contra` contravariant + `R_co` covariant UNBOUNDED) over `float | V` = ELEGANT not over-engineered. Verified variance: `Functional.__parameters__ == (-V_contra, +R_co)`, `R_co.__bound__ is None`. The per-cell density is NEITHER a float NOR the input carrier (lost the group axis) → `float | V` would be a documented falsehood. The result type genuinely varies (float for keff/reaction-rate, ndarray for density) so the 2nd typevar earns its place even with ONE concrete instance — it is "match the algebra" (P1), not premature P6.
- Disjoint-surface = P4 textbook. `test_functional_category.py` proves the partition BOTH directions + 2 discriminator foils (not-just-`Vector`, not-just-`LinearOperator`) → the rule-of-two-directions that catches an alias-as-category. Sibling-not-subclass is right (subclass would inherit operator surface, dissolve the distinction).
- ⭐ CARDINAL-2 THREE-SITE NON-COLLAPSE = RIGHT scope-boundary, verified by READING each: SN `solver.py:1086` (group-last `(N,ng)`, folds volume_measure AND (n,2n), a RATE) / CP `solver.py:674` (group-last, folds volume_measure, a RATE) / numerics `iteration.py:269` `_default_production_estimator` (`float(np.sum(F.apply(psi)))` — FULL fission op, χ-broadcast, measure-absorbed, a SCALAR). The new `evaluate` is axis-0 group-collapsed density `(1,*spatial)` — NOT a twin of any. S6 (`F = M_χ ∘ ProductionRate ∘ M_νΣf`) is where this becomes the shared middle. Build-ahead JUSTIFIED: imminent named consumer (S6 in-flight) + bit-identity pin (`TestEvaluateEquivalentToRankOneInner` 0-ULP vs `RankOneOperator` `inner`) — distinguish from speculative P6 anti-pattern.
- `_default_production_estimator` confirmed NOT a twin (full-op scalar vs χ-free measure-free density); category-honesty test correctly asserts bare `(L,S,F,ψ)` 4-arg callables are NOT `Functional[V]` (1-arg field→scalar) and skips the optional unshipped `ProductionFunctional` wrapper = correct restraint.

**NITS (do-before-commit, none blocking).**
- ⭐ NIT-1 (rationale, not code): brief claims the `phi: Field|NDArray` + `isinstance(phi, Field)` narrow (`production_rate_functional.py:150`) "mirrors `MultiplicationOperator.apply`" — FALSE. That operator takes a STRICT `TimedFullField`, reads `psi.bulk.values` unconditionally, NO union/narrow. The REAL precedent is `sn/scattering.py:618,665` (`phi.values if isinstance(phi, ScalarFlux) else phi`). Idiom is sound + well-precedented; only the CITED sibling is wrong → rationale-comment-asserts-false-coupling tell (a future "align to MultiplicationOperator" would break the raw-array path). Fix the attribution in closeout/plan (source file asserts no false coupling, no code edit). STANDING TELL: the `x.values if isinstance(x,<FluxLeaf>) else x` family lives in `sn/scattering.py`, NOT in the multiplication operator.
- NIT-2 WITHDRAWN (verified against source, NOT memory): docstring `:114` cites `MaterialXSField.fission_production_field` — CONFIRMED REAL at `material_xs_field.py:408`. ⭐ CORRECTION to [[issue-257-s2-typed-accessors]]: the accessor TRIO is asymmetric — `total_cross_section_field`/`absorption_cross_section_field` carry the `_cross_section` infix but `fission_production_field` does NOT (νΣf is a production quantity, the `_cross_section` infix would mis-name it). S2 memory over-generalized to `fission_production_cross_section_field` — WRONG. The S5 cite resolves; Sphinx-clean. STANDING LESSON: verify accessor names against source before flagging a stale-xref nit; a memory naming a method is a CLAIM it exists under that spelling.
- NIT-3 (optional): unbounded `R_co` + single ndarray instance invites a future tighten to `bound=np.ndarray` re-introducing the rejected union; docstring already defends it, a forward-pointer to S6 float-instances would self-document.

**Arch-opp:** the 3-site collapse is the S6 target — confirm `.claude/plans/issue_257_coefficient_field_promotion.md` names SN:1086 / CP:674 / iteration:269 as collapse targets so a fresh session inherits the trigger; the bit-identity test is the de-risk pin.

Series: [[issue-257-s1-coefficient-fields]] (Frame-2 mirror; emergent-gate-via-replace), [[issue-257-s2-typed-accessors]] (the `*_cross_section_field` accessors NIT-2 references), [[issue-257-s3b-multiplication-operator]] (the `M[σ_t]` sibling; the FALSE-cited precedent), [[issue-256-vector-protocol-step1]] (the Vector this co-vectors).
