# Elegance Enforcer — memory index

Durable elegance verdicts from the SN typed-field / operator-algebra review series (Wave O #208,
Phase 5 windowing, affine flux algebra — all LANDED on `refactor/field-role-typing`). The
cross-cutting PRINCIPLES and recurring smells are promoted into AGENT.md § "Institutional
knowledge"; these notes hold the concrete instances for verification.

- [#257 S10a EmissionSpectrum value-object + χ-guard](issue_257_s10a_emission_spectrum.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, UNCOMMITTED). ⭐ guard predicate keys on WRONG field: `is_fissile=np.any(SigF>0)` but χ is consumed via PRODUCTION (χ·νΣf·φ, all 7 prod consumers read SigP not SigF) → flip to `np.any(SigP>0)` (Isotope: `nubar*sigF`); REVERT the billiard SigF=νΣf hack (dimensionally-wrong apology-comment symptom). ndarray-subclass vehicle PASS (no-op `__array_finalize__` safe — validators invoked-not-automatic, never on a slice; verified 4 prod call-sites all source full-vector). NIT: 2-instance `__post_init__` twin → extract `enforce_emission_spectrum(chi,*,is_producing)` NOW (trigger met in-diff); `is_emitting` dead (0 consumers, 3rd spelling of is-producing). STANDING TELLS: guard-keys-X but consumers-read-Y = wrong spelling masked by real-data coincidence (grep consumers); hack-fakes-field+apology-comment ⇒ guard is wrong; no-op array_finalize safe iff validators never hit a slice.
- [#257 S9 LD boundary moment honesty + coherent-promise gate](issue_257_s9_boundary_moment_honesty.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, UNCOMMITTED, verification-centric FINAL behavioral stage; design settled — no field type #263, no value gate sub-floor). `SN2DCartesianLDStressMMSCase.prescribed_inflow` now EMITS the moment slot via case-owned `_project_inflow_to_face_moments` (leggauss+`_drivers` only), closing a #251 PRODUCER-blindness (consumer threaded slot-1, producer emitted scalar). ⭐ RULING-1: `face_moment_count` gating = dispatch-on-STRUCTURAL-FACT (reads the `moment_layout.py:64` SSOT the trace producer+cochain key on), NOT boolean-flag anti-#3 — arms produce distinct SHAPES, byte-id DD/Step arm (verify the `_drivers` call args reduce identical, not just closeout). ⭐ RULING-2: L11 split (production projector ⊥ test `_face_transverse_legendre`) is CORRECT not duplication — collapsing via import makes GATE C pass TAUTOLOGICALLY + a double-applied transverse-mass normalization drift slips silently; bulk `_project_scalar_to_tensor_legendre` is TEST-ONLY so no prod projector to extract toward. STANDING TELL: a gate pinning "prod == hand-ref" REQUIRES the two be independent; an import single-sourcing them is the laundering habitat. ⭐ RULING-3: `_solve_with_boundary_slope` re-baseline (`None`→test-side scalar, decoupled from now-slope-honest `prescribed_inflow`) is principled — a helper that inherits the prod behaviour it's the controlled baseline FOR = measurement-laundering (null result goes vacuous); Mode-11 sentinel guards `zero==flat` byte-id + `mom≠flat` consumed. ⭐ RULING-4: Deliverable-4 named accessor SKIP right (one `slot[...,1]` write below its `AVERAGE_MOMENT` sibling; no scattered reads; constant = gold-plate anti-#10; broad predicate=#246). 3rd Mode-10 sub-floor-companion-unavailable (#240 D5b-S4→#247 LegA→#251 LegB→S9). NITS do-now: N1(CONCERN anti-#11) 3 parallel transverse-Legendre loops (2 MUST stay independent/L11) — add docstring naming the 3-D-face rule-of-two→three collapse trigger (folds into #263); N2(opt) `_amplify_boundary_slope:110` hand-lists dataclass fields, drops a future field silently. PASSES: math reads as `⟨ψ,Pₗ⟩/⟨Pₗ,Pₗ⟩`, `W=weights.sum()` computed not hardcoded, promoted gate Mode-8-clean (`pytest.fail`/`np.testing` -O-safe) + sibling import shim + verdict-flip triggers in fail msgs, docstring updates in-module honesty NOT Sphinx scope-creep. 35✓+590✓.
- [#257 S8c @overload fibration (Pattern M)](issue_257_s8c_overload_fibration.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, UNCOMMITTED, behavioral-NEUTRAL; FIRST `@overload` in orpheus/, precedent-setting). Makes the heteromorphic `Fission/Scattering.apply` singledispatch fibration honestly typed: rename dispatcher→`_apply_impl` (`-> "Any"` base), keep register arms at NATURAL indentation, append `if TYPE_CHECKING: @overload… else: apply=_apply_impl`. ⭐ RATIFIED **Pattern M over Pattern C** — master-standard Beck ordering "reveals intention > fewest elements"; C buries ~150-215 lines of R·Λ·M/(n,2n)/1/W math in a double-indented `else:`. M is NOT foreign: `LinearOperatorMixin` already uses `if TYPE_CHECKING: def apply(...)->V` (`operator.py:400-408`). ⭐ VERIFICATION METHOD: stash-baseline pyright delta **33→17 errors, net −16, ZERO new** (categorize survivors SEMANTICALLY not by line — diff shifts lines); RUNTIME alias proof = `Cls.__dict__['apply'] is Cls.__dict__['_apply_impl']`==True (NOT `Cls.apply is…` — descriptor `__get__` artifact); STATIC gate = pyright-only uncalled `_c6_static_typing_pins` of `assert_type`, passes iff 0 errors in its band. cast()×3 HONEST (target=exact runtime invariant, both carriers→AngularSourceSink, 3rd→`object` fallback fail-loud). 17 survivors ALL pre-existing root gaps S8c unmasks (`FullField.bulk:BulkField` over-broad `full_field.py:164` + `AngularFlux.integrate_angular()->"object"` `angular_flux.py:116` Pattern-3 viol). NITS: N1 do-now document "Pattern M" idiom before #261's 3rd copier; N2 C6 static pins HarmonicMomentField arm but runtime cases omit it (covered elsewhere). Issue recommended under #256 for the root under-typing. 17✓+85✓.
- [#257 S8a apply(FullField) re-point](issue_257_s8a_apply_full_field_codomain.md) — PASS (`feature/field-typed-operator-algebra`, baseline `90ea1cd`, UNCOMMITTED, behavioral-NEUTRAL; C5 14✓/1xfail + sn/solve+transport 327✓; 7 operators-dir reds + all pyright errors PRE-EXISTING baseline, byte-verified NOT in S8a hunks). Cofree-comonad: operator matvec OUTPUT re-pointed `TimedFullField`→TIMELESS `FullField` (base arrow `FullField→FullField`), comonad lives ONLY on the driver; drops `_history=()`/`history_depth=psi.history_depth` on L/C/M[f]/B + S/F TimedFullField output arms. ⭐ LOAD-BEARING RULING: the role-grid discriminator REFINES from "operator-output vs solve-trace" to ITERATE-BUILDER vs BASE-ARROW — `InvertibleOperator.solve` (WDD sweep, driver-facing resolvent that BUILDS the iterate) STAYS `TimedFullField→TimedFullField` (`operator.py:1154` body intentionally timed, NOT a half-seam), but `MultiplicationOperator.solve` (`q/f→AngularFlux`, S3b engine inverse, VERIFIED ZERO graph callers via `mcp__nexus__callers`) FLIPS timeless — both right; `solve` is NOT uniformly timed. STANDING TELL: on a `solve` re-type run `callers` — zero-caller `.solve`=algebraic leaf (flip timeless), driver-facing resolvent stays timed; do NOT flag the M[f].solve/InvertibleOperator.solve asymmetry as inconsistency. Driver re-attach (choice #1) PASS but ORDER-DEPENDENT: SI fold `rhs=q_ext; for g: rhs=rhs+g.apply(psi)` (`iteration.py:525-527`) is TIMED-FIRST → `TimedFullField.__add__`→timed `_recombine`→stays timed every step; `gain+q_ext` (timeless LEFT) would silently drop history-depth → fold is order-correct, verify the type-bearing operand is the left accumulator. No explicit `advance` is CORRECT (steady-state never advances; `_history=()` always). #2 `_require_typed_composite` widen→FullField (Pattern-4 SSOT, one guard accepts timed+timeless, rejects ndarray) + #3 solve-timed/apply-timeless (documented) + #4 `full_field_space._rebuild` `replace(x,_history=())`→`x._recombine(...)` (`:197`, METRIC analogue of dunder recombine, unifies SAME hook NOT parallel path, 0 new pyright on already-duck-typed `x`) ALL PASS. Scope CLEAN: `(L+C)−C` arithmetic + Scattering/Fission `@apply.register def _(psi: TimedFullField)` INPUT dispatch UNTOUCHED (only `→FullField` output annot + ctor). Test migrations STRENGTHEN (`isinstance TimedFullField`→`isinstance FullField`+`not isinstance TimedFullField`). C5 test genuine: C5a timeless codomain × geometry × depth 0..4, Mode-11 `.apply` direct (zero graph callers); C5b pins driver-reattach to structurally-independent `k_inf=νΣf/Σa` (sphere k_inf PASS = structural proof the stale sphere snapshot red is NOT a neutrality break). STANDING TELLS: when a re-point relies on a dunder fold to reattach a type, the verdict HINGES on operand order in the driver — read the actual `rhs=rhs+g` fold; the S4.5 `_recombine` hook is the SSOT that makes this carve cheap.
- [#257 S8b pure-L σ-free StreamingOperator](issue_257_s8b_pure_L_sigma_free.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, baseline `9316321`, UNCOMMITTED, composite behavioral-NEUTRAL; 61 S8b + 141 broad ✓). Drops the `(L+C)−C` fold: `StreamingOperator.apply`→`loss_representation.streaming_action(psi)`=`loss_action(_zero_sigma_for(psi),psi)`; `sigma_t` FIELD DELETED (~106 sites→`StreamingOperator(sn_mesh)`); 3 solver sites. ⭐ RULING-1: "name the σ-free primitive but route through `loss_action(0)` rather than refactor `loss_action=streaming+σ·ψ`" IS principled — refactoring the kernel re-baselines the HOT byte-pinned `(L+C)` COMPOSITE (=`InvertibleOperator.apply`, prod SOLVE) for zero gain; chosen factoring keeps streaming ONCE in `loss_action` (P2, no twin) + names the leaf (P3). VERIFIED: `streaming_action==loss_action(σ)−σ·ψ` 16-52 ULP; `streaming_action` byte-repeatable; `(L+C).apply==loss_action(σ)` BYTE-EXACT. STANDING TELL: elegant boundary for moving a term out of a leaf = "byte-id COMPOSITE + ≤few-ULP LEAF drift", NOT "refactor shared kernel to make decomposition explicit at cost of hot-path re-baseline"; implicit-in-kernel does NOT bite when explicit in docstrings+structural test AND prod consumes the byte-id composite. ⭐ RULING-2: P4 σ-removal COMPLETE (field GONE not ignored → `StreamingOperator(sn_mesh)` only form → σ-carrying-L unspellable; anti-#5 satisfied structurally); migration grep-clean (ZERO sigma-bearing sites; `ReducedStreamingOperator` is a DIFFERENT class, don't false-positive). ⭐ RULING-3: re-baseline STRUCTURALLY GROUNDED (textbook) — het test renamed→`test_pure_L_plus_C_recovers_loss_action_het` pins `(L+C).apply==loss_action(σ)` byte-EXACT + `L.apply≈loss_action(σ)−C·ψ` ULP vs SEPARATELY-built `CollisionOperator` (pure-L pinned to `(L+C)−C` NOT "whatever pure-L emits"); new `test_pure_L_sigma_free.py` C1 tests the DEFINING σ-freedom DIRECTLY on the zero-graph-caller leaf (Mode-11) + a TEETH/mutation test (monkeypatch σ-re-reading stub→C1 reddens). NIT-1 (recommended): `test_bc_extraction_2d.py:193` re-captures `.npy` via `np.save(out.bulk.values)`=freezes WHATEVER pure-L emits, structural ground in PROSE only → laundering habitat BUT downgraded (the het composite pin + C1 teeth run every session, redden a capture-time leak); fix=gate capture on `≈(L+C)−σ·ψ` before save. STANDING TELL: snapshot regenerated by dumping the SUT's own output = latent laundering UNLESS an independent structural pin runs every session — verify it exists. NIT-2 (trivial): `loss_representation.py:398` func-local `import numpy as _np` vs module `np`; shape contract CORRECT (AngularFlux `(N,ng,*spatial)`→`shape[1]`=ng, robust multigroup). PASS-as-NIT: `if TYPE_CHECKING:` abstract `loss_action` sigs on base = honest "abstract-on-base/concrete-on-leaf" for dataclass-on-Protocol (dodges `reportRedeclaration`, MRO dispatches to concrete walk). Scope CLEAN (scattering/fission/iteration/invertible_operator byte-untouched; `loss_action` BODIES not in diff; S/F `@apply.register` untouched=S8c out). The 1 red (`TestTwoDCartesianRaises`) is PRE-EXISTING `trace_space.py:241` rank-guard (Mesh2D + 1-D gauss_legendre, reproduced from `SNMesh.trace` w/ NO operator) — STANDING TELL: a `_LC_matvec` shim in-diff doesn't make a red in its USERS an S8b finding; reproduce at the raise site first.
- [#257 S7 scipy single-source](issue_257_s7_scipy_single_source.md) — PASS (`feature/field-typed-operator-algebra`, HEAD `9ccfec7`, UNCOMMITTED, behavioral-NEUTRAL). Cleanest S-carve: textbook P2+P3 bundled. 3 scipy-LinearOperator sites→1 (`_as_scipy_linop(carrier_matvec,template,n)` @iteration.py:228 SOLE construction in all orpheus/). Factoring GENUINE not procedural — ravel-WRAP is the true shared primitive; A=`loss_minus_gains`/`solution_template`(flux-space) vs M=`_preconditioner`/`q_ext`(source-space) ASYMMETRY load-bearing (B.5.2) + correctly preserved. P3 win: old anon `A_matvec` flat closure → named carrier `loss_minus_gains(psi:V)->V` reading as `(L+C−S−B)·ψ` (plumbing leaving the closure = WHAT makes math visible). Retired `as_scipy_linop` = genuine twin (FLAT-only, ZERO prod callers, rmatvec/dtype were TEST-ONLY → P5/6 drop RIGHT; Issue-15 migration rationale was P11 untriggered-temporary). Retirement CLEAN (def+docstring¶+spla import+2×__all__+5 tests+3 doc xrefs; capability symbols `_has`/`CAP_*`/`MissingCapability` + fixtures `NoApplyOperator`/`matrix_apply_only` confirmed STILL used — surgical). SI path scipy-FREE confirmed (fixed-point, no linop handoff — not a missed target). 55✓+18✓; the 3 pyright `:228` reportCallIssue = PRE-EXISTING scipy stub artifact (identical ctor at HEAD ×2, now ×1). NITS opt: N1 adapter docstring says `(L−Σgᵢ)` while comment says honest `(L+C−S−B)` (drops +C); N2 `except TypeError` comment @:814 still names old `A_matvec`/`M_matvec` (stale after this very rename). STANDING TELLS: public util w/ zero-prod-callers + test-only extra cap = twin to RETIRE not keep "for future migration"; extract-shared-plumbing + name-the-carrier simultaneously satisfies P2+P3; on a closure-rename carve grep surrounding error-handling comments for OLD closure names.
- [#257 S1 CoefficientField + role marker](issue_257_s1_coefficient_fields.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, pre-commit, pure-addition). Frame-2 cone/simplex = the COEFFICIENT-side mirror of flux/displacement. 4 judgment calls ALL KEEP: (J1) `CoefficientRole` dunder-EMPTY marker = NOT Pattern-6 — 2nd consumer in-diff (rule-of-two met), earned on LIVE taxonomy (`issubclass` is the discrim axis) + named-home-for-S3-`*`-with-tracked-trigger; long docstring on empty body is ELEGANT here (content = absence-of-gate, can't self-document via code). (J2) σ≥0 NOT enforced (cone-as-property) — multiplier embedding `M:L^∞→B(L²)` is LINEAR on the FULL SIGNED space, `Σ−Σ′` is a legit signed element, enforcing would make inherited `__sub__` sometimes-raise-on-values; asymmetry vs simplex PRINCIPLED (signed Σ meaningful, off-simplex χ NEVER). (J3) `χ+χ` rejected via EMERGENT `__post_init__` (verified: `__add__`→`replace()` re-runs post_init→ValueError) NOT explicit `__add__` — MORE elegant: single-sources the simplex law (an explicit override = Pattern-2 dup of the invariant). STANDING PATTERN: when a construction invariant exists, prefer emergent-gate-via-`replace` over hand-written dunder. (J4) `mix` parallels `affine_combination` = KEEP do-NOT-extract — affine(Σλ=1,signed-λ) vs convex(Σλ=1,λ≥0) define DIFFERENT ops; a `_blend(require_convex:bool)` helper = anti-#3 flag-dispatch habitat + cross-role-family dep; collapse trigger = 3rd-same-constraint NOT 2nd-different. NITS do-now: N1 `mix` docstring "analogue + extra restriction" UNDERSELLS the affine→convex identity-change (Cardinal-3, invites J4 wrong-refactor); N2 `isinstance(χ,Vector)` True (runtime_checkable = method-PRESENCE only) but `χ+χ` raises → add clarifying comment xref `test_addition_leaves_simplex`; N3 units.py:183 pint `**`-stub PlainUnit err CONFIRMED matches convention (grep: 0 `# type:ignore`, 4 pre-existing bare) → file uniform-cleanup issue, don't patch. STANDING TELL: `runtime_checkable` Protocol conformance ≠ operational closure (gated-arithmetic leaf passes isinstance, raises in-loop).
- [#257 S3a DiagonalOperator N-D broadcast engine](issue_257_s3a_diagonal_engine.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, HEAD `1ce727a`, pre-commit, DEAD-CODE reshape: ZERO prod `DiagonalOperator(...)` sites; 1-D byte-id; from_measure/self-adj/CAP_SOLVE-iff-nonzero survive; 0 new type:ignore; 49✓). Generalizes `(weights:1-D,axis)` → N-D `expand_dims(c,broadcast_axes)*x` engine the S3b transport `MultiplicationOperator(σ_t)` will delegate to (`broadcast_axes=(0,)`≡`σ[None]*ψ`). 4 calls ALL KEEP: (J1) dual-mode NOT boolean-flag anti-#3 — `X|None` where None=structural-ABSENCE-of-the-data-N-D-needs (1-D reshape can't name its complement, carrier rank unknown at construction → rank-agnostic deferral) ≠ `bool` selecting two complete behaviours; "always broadcast_axes+from_axis ctor" WEIGHED+WORSE (kills rank-agnostic W). (J2) `weights` @property RAISING in N-D mode = Pattern-4 textbook (N-D under 1-D name=units/rank lie); RETAINED as back-compat alias, ZERO prod readers (grep: all `.weights` prod=quad/measure/emit/context, none a DiagonalOperator). (J3) `_broadcast`/`_check_shape` SSOT — both shared apply(`*`)+solve(`/`); diff REMOVES old inline-axis-check TWIN (Pattern-2 win). (J4) name KEEP (diagonal-in-full-basis; numerics-engine vs transport-`M[σ_t]` = two LAYERS not two names). ⭐ NIT-1 do-now (Cardinal-3): docstring :1506-1510 + property :1586 ASSERT `TensorProductOperator` reads `weights`/`axis` — FALSE (VERIFIED: TP.apply/.solve route purely through `op.apply`/`op.solve` @1312-1340, read NEITHER; `.axis` consumed only by `_broadcast`) = the rationale-comment-asserts-false-coupling tell ([[issue-236-phase2-stepc-tau-retirement]]); reword to "back-compat alias for from_measure+1-D sites", drop TP claim. Minor: vector.py:46 "flat axis-primitive on one axis" note now stale (not in diff, sweep at S3b). STANDING TELLS: `X|None`=absence-of-data ≠ flag anti-pattern; property-raises-in-illegal-mode = right conservative move for zero-prod-reader legacy alias; DOCSTRING-asserts-a-coupling → grep the named collaborator before trusting the doc.
- [#257 S4.5 FullField base extraction](issue_257_s4_5_full_field_extraction.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, HEAD `93aa016`, UNCOMMITTED, behavioral-NEUTRAL; 51 file-local + 237 transport✓; pyright 0/0/0 both files; cross-method rejection probed LIVE). Extracts timeless `FullField` (`bulk ⊕ boundary`) base out of `TimedFullField`; retires S4 `TransportState` Protocol. `TimedFullField(FullField)`=`Cofree(FullField,d)`. ⭐ LOAD-BEARING RULING: `_recombine(self:T,*,bulk,boundary)->T` = the polymorphic-CONSTRUCTOR hook — ALL 6 dunders + from_flat + copy funnel through it; base spells `replace(self,...)`, subclass OVERRIDES to thread `history_depth` + RESET `_history=()`. STANDING REMEDY for "base+subclass share an algebra but differ in a ctor field" (algebra ONCE, correct concrete return per subclass, Liskov-correct; generalises the emergent-gate-via-`replace` of [[issue-257-s1-coefficient-fields]] J3). 4 calls ALL PASS: (a) hook right+well-named, `copy` drops history MATCHES prior bit-id; (b) generic `from_flat(cls,flat,template:T)->T` via the hook REMOVED a `# type:ignore[override]` that papered a Liskov template-NARROWING — STANDING TELL: a factory override narrowing its template param = Liskov violation behind the ignore, collapse to base-generic-via-hook; (c) `zeros` delegates leaf-alloc to `FullField.zeros` so the duck-typed `zeros_on` `# type:ignore[attr-defined]` lives on the BASE ONLY (count 2 not 4); (d) `_check_partner` widen `type(other) is not Timed`→`isinstance(other,FullField)` SOUND — load-bearing for BDF `at_lag(0)-at_lag(1)` (timed−timeless), opens NO cross-method hole (member-type rejection SSOT on the LEAVES, probed `AngularFlux−ScalarFlux→TypeError`). NOT-a-twin: `multiplication_operator.py:208,233` inline `TimedFullField(...)` = OPERATOR OUTPUT w/ role change (flux→source/source→flux), CANNOT route through type-preserving `_recombine` — same role-grid discriminator. Retirement clean (Protocol gone, structural→nominal isinstance check = strengthening). NITS (none blocking): (1) `timed_full_field.py:124` only surviving `TransportState` token, note the S4 Protocol now also retired; (2) `full_field.py:341` func-local `import numpy` vs sibling-leaf module-level; (3) `to_flat:319-327` SN-specific 1-D-boundary assumption, Pattern-6-defer caveat.
- [#257 S6 IntegralKernelOperator (§5.6 Kernel)](issue_257_s6_integral_kernel_category.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, HEAD `7b463bb`, UNCOMMITTED, additive bit-id; 20✓ S6 + 85✓ matvec-arm regression). NEW `transport/integral_kernel_operator.py` (L2) `IntegralKernelOperator(LinearOperator[V],Protocol[V])` = §5.6 Kernel = REFINEMENT of LinearOperator (adds `kernel`, KEEPS apply+caps) asymmetric to S5 Functional (DISJOINT); discriminator=LOCALITY (Frame 3, diagonal MultiplicationOperator has NO kernel). + `fission.production_rate` + `scattering.kernel`=`OperatorProduct(R,OperatorProduct(Λ,M))`. 4 decisions ALL PASS: D1 `cast(LinearOperator,...)`×3 RIGHT (projection mixins subclass UNPARAMETRISED `LinearOperatorMixin` w/ concrete `apply(ndarray)`→pyright can't unify `OperatorProduct[V]`'s V; `micro_xs` precedent real (PACKAGE not .py); cast>ignore, runtime correctness pinned 0-ULP not maskable; parametrise=#226); D2 kernel∥`_aniso_source_from_moment_values` (Cardinal-2) PASS+do-NOT-route-arms (twin single-sourced AT LEAVES, kernel=2nd assembly; routing NET-NEGATIVE: `/W` lives at apply boundary L18, fresh-alloc per access on hot path, collapse trigger unmet); D3 `skip_l0=True` honest (ℓ=0 P0+(n,2n)=LOCAL components, `order==0→ValueError` Pattern-4); D4 Protocol form correct (`LinearOperator` IS Protocol→re-declare `Protocol[V]` required; invariant V verbatim per #256-S3 endo). Verification note-#3 textbook: fission B.1 hand-loop CORRECTNESS + B.2 0-ULP de-risk; scattering EQUIVALENCE-only de-risk pinned by EXISTING aniso MMS gate. ⭐ NIT-1 do-now (`fission.py:319-326`): production_rate docstring asserts cross-module byte-id as STRUCTURAL invariant ("same numpy primitive/axis/keepdims 0-ULP") — demote to test-pinned fact (cite B.2), bring to scattering's `:606-609` standard (docstring-asserts-false-coupling tell). ⭐ NIT-2 do-now (`scattering.py:~492`): `_aniso_source_from_moment_values` has NO reciprocal pointer to `kernel`+gate (note-#1 remedy: reciprocal xref+keep-gate-green trigger). NIT-3 opt: docs todo `SumOfTensorProductsOperator` un-orphaning deferred per plan-file→file issue. STANDING TELLS: fresh-alloc semantic-reading @property correct (frozen-lens) but NOT for hot matvec; equivalence-only de-risk legit IFF structural physics anchor exists elsewhere (verify it); cited cast precedent may be a PACKAGE → `find -path` not Read; bring sibling docstrings to same gate-citation standard.
- [#257 S5 Functional category (§5.6 suffix law)](issue_257_s5_functional_category.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, HEAD `b404ae1`, UNCOMMITTED, bit-id additive; 21✓/1skip; 0-ULP byte-id verified). NEW `numerics/functional.py` (L1) `Functional(Protocol[V_contra,R_co])` co-vector mirror of Vector, SIBLING of LinearOperator (disjoint surface, no apply/caps) + `transport/production_rate_functional.py` (L2) `ProductionRateFunctional.evaluate(phi)=(νΣf.values*phi).sum(axis=0,keepdims=True)` group-collapsed density NO measure/NO χ. PASS: 2-typevar (`-V_contra`/`+R_co` unbounded, variance verified) over `float|V` ELEGANT not over-eng (density is neither float nor carrier); disjoint-surface=P4, category test BOTH directions+2 discriminator foils; ⭐ CARDINAL-2 3-site non-collapse RIGHT (SN:1086 group-last+measure+(n,2n)RATE / CP:674 group-last+measure RATE / iteration:269 `Σ(Fψ)` full-op scalar — NONE a twin of the χ-free measure-free density; S6 makes this the shared middle, build-ahead JUSTIFIED by imminent named consumer + bit-id pin). NIT-1 do-now: brief's "mirrors MultiplicationOperator.apply" FALSE (that op takes strict TimedFullField); real precedent for `phi.values if isinstance(phi,Field) else…` is `sn/scattering.py:618,665` (ScalarFlux narrow) — fix attribution in closeout/plan not source. NIT-2 WITHDRAWN: `fission_production_field` cite @:114 CONFIRMED real (material_xs_field.py:408; accessor trio asymmetric — fission drops `_cross_section` infix; corrected S2 memory). NIT-3 opt: unbounded R_co single-instance invites future tighten. STANDING: verify accessor name vs source before flagging stale-xref.
- [#257 S3b MultiplicationOperator C=M[σ_t]](issue_257_s3b_multiplication_operator.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, HEAD `c1da42d`, pre-commit, carve bit-id; 11✓ + 63 resolvent-leg✓). §5.7 promotion: NEW `transport/multiplication_operator.py` (`MultiplicationOperator(LinearOperatorMixin["TimedFullField"])` `@dataclass(eq=False)`) delegates `f·ψ`/`q/f` to the S3a `DiagonalOperator(broadcast_axes=(0,))` engine, owns ONLY the typed codomain (apply→AngularSourceSink RATE, solve→AngularFlux, apply_transpose==apply); `CollisionOperator` retyped THIN subclass (apply/solve/apply_transpose bodies DELETED→inherited, MRO-verified); 3 solver.py sites rewired `total_cross_section`→`_field`. 4 axes PASS: P2 multiply lives ONCE in engine (deleting old `sigma[None]*psi` removed REAL dup, no twin); P1 docstring law-suite mirrored test-for-test (homomorphism correctly drops to bare engine — σ·σ' cm⁻² is a numerics fact not a XS field); P4 `__post_init__` inherits `engine.capabilities` WHOLESALE (CAP_SOLVE-iff-min|σ|>0 single-sourced at engine, behavioural STRENGTHENING over legacy silent-NaN); LAYER CONTRACT not-a-wart (`MultiplicationOperator` L2 no `__add__`→`OperatorSum`; only `CollisionOperator` L3 `__add__`→`InvertibleOperator`; numerics↛transport↛sn respected, MRO-confirmed). ⭐ NIT-1 do-now P5 DEAD IMPORTS: `CAP_APPLY`/`CAP_APPLY_TRANSPOSE`/`CAP_SOLVE` at :101-103 appear in body ONLY in docstrings/comments, never a runtime symbol (caps inherited :171); the IMPORT LIST asserts a coupling the code lacks (inverse of S3a docstring-asserts-coupling tell) → drop the 3, keep BlockRole/DiagonalOperator/LinearOperatorMixin. ⭐ NIT-2 do-now P2/P7 `_engine()` REBUILD-PER-CALL: SAME `DiagonalOperator(...,broadcast_axes=(0,))` built 3× (`__post_init__:168` discarded + `_engine():183` per apply + per solve); NOT free — `__init__` runs `np.asarray(dtype=float)` + O(size) `np.all(coeff!=0)` spectrum scan PER matvec recomputing the frozen capability → STORE-ONCE (`engine: DiagonalOperator = field(init=False)` in `__post_init__`, apply/solve read `self.engine`, DELETE `_engine()`); 3 spellings of `broadcast_axes=(0,)` is the habitat (future re-home lands in one not both → caps/action diverge). STANDING RULINGS: a dead IMPORT (not just a docstring) can assert a false coupling — grep body for each import as runtime symbol; build-once-store > build-per-call when the built object freezes a capability AND is stateless-derived-from-immutable (frozen-lens, can't go stale — distinguish from S2's don't-cache-the-FIELD which was a 2nd MUTABLE slot). Minor: StreamingOperator still takes raw σ at the 3 sites (intra-expr asymmetry HONEST — not yet field-promoted, retires at a future S-stage).
- [#257 S2 typed CrossSectionField accessors](issue_257_s2_typed_accessors.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, HEAD `505e1b7`, pre-commit, pure-addition, bit-id; 10✓). 3 `@property` typed lenses (`total_cross_section_field`/`absorption_cross_section_field`/`fission_production_field` — ASYMMETRIC, fission drops `_cross_section` infix, verified @S5) wrapping the cached raw views zero-copy → the FIELD side of S3 `C=M[σ_t]`. J4 layering KEEP (L3→L2 runtime import CYCLE-FREE both orders verified; runtime-not-TYPE_CHECKING CORRECT — class used at runtime). J3 construct-on-access KEEP do-NOT-cache (expensive step already cached behind raw view; field cache = 2nd mutable slot needing lockstep invalidation = stale habitat; frozen-lens-over-cache can't go stale). J1 `_field` suffix KEEP this stage (2 different return types same σ → must differ; terminal name is UNSUFFIXED, the flip-stage RENAMES not accumulates a 3rd spelling). ⭐ J2 σ_a-with-no-consumer CONCERN→KEEP: Pattern-6 defer applies to ABSTRACTIONS not to a 3rd INSTANCE of a proven pattern — the wrap-pattern's rule-of-two is met in-diff (σ_t→S3, νΣf→S6); σ_a is symmetry-completing (co-equal XS family), asymmetric surface invites future open-code (Pattern-2 habitat). NIT do-now: add why-all-three rationale to comment (:378-387) so future DRY-sweep doesn't delete σ_a (anti-#11 for build-ahead). NIT: "(1/cm, the cone)" :383 risks reading as ≥0 GUARANTEE (S1: cone is property not invariant). PASS test: `.values is raw_view` (`is`-not-`==`) = correct zero-copy-LENS gate; `not isinstance` negative control proves pure-additive; `ACCESSORS` table single-sources pairing + exercises σ_a. STANDING TELLS: symmetric-build of co-equal family ≠ Pattern-6 violation when wrap-pattern rule-of-two met in-diff (demand rationale-comment); typed-lens-over-cache introduces NO twin (no 2nd computation) — distinguish from a 2nd realization route.
- [#256 Step 3 apply endomorphism](issue_256_apply_endomorphism_step3.md) — PASS-WITH-NITS (`feature/field-typed-operator-algebra`, uncommitted). Retype `LinearOperator.apply(x:np.ndarray)->np.ndarray` → endomorphism `(x:V,/)->V` via class-level `Generic[V]` bound to `Vector`; removes 10 Mixin dunder ignores; −9 reportArgumentType (q_ext cascade). ⭐ Q1 KEEP THE STUB: brief's premise WRONG — the 2 `reportIncompatibleMethodOverride` (fission:223/scattering:962) PRE-EXIST at HEAD (INVARIANT across HEAD=48/stub=39/no-stub=59/self-typed=50), a `@singledispatchmethod[NoReturn]` pyright limit orthogonal to the PR. Prototyped self-typing (`self:LinearOperator[V]` on dunders) on a TEMP copy → 50>39, regresses the −9 cascade, breaks `__rmul__→__mul__`+`__pow__ result@self`+5 SNMesh, clears NOTHING extra → discarded, working tree UNTOUCHED. Stub = right elegance shape (`if TYPE_CHECKING` no-runtime, declares the endo contract the dunders rely on). Q2 PASS (`/` positional-only correct, lets leaves keep `psi`/`moments` names). Q3 PASS (pre-existing `__pow__`/`__call__`/`adjoint` ignores correctly out-of-scope). Q4 PASS (single-`V`-not-`Generic[F]` is the honest endomorphism; carrier fixed while inner leaf role flips). ⭐ NIT-1 (do-now CONCERN): `Generic[V]` retype INCOMPLETE inside iteration.py — `_solve_with_seed(self,rhs,psi_prev):443` untyped→`Unknown` unions into `psi` (`Unknown|V|NDArray`) → +3 NEW iteration.py errors (`:506`,`:547`,Krylov`:751/753`); type the private helper `->V` (SAME missed-driver-surface lesson as step-1's eigenvalue.py, now the iteration.py PRIVATE helpers). Annotation-only (614 passed; 7 reds = baseline SPHERE #232/#250 + ny=1 mu_y, ZERO kernel touched). Step-1 BLOCKER (add `Vector.__truediv__`) LANDED in `cfb651b` + all 4 dunders sharpened to `->Self`.
- [#256 Step 1 Vector Protocol](issue_256_vector_protocol_step1.md) — CHANGES-REQUESTED (`feature/field-typed-operator-algebra`, uncommitted). New `numerics/vector.py`: structural `runtime_checkable` `Vector` Protocol (`__add__`/`__sub__`/`__rmul__`) + `V=TypeVar(bound=Vector)` to later retype `apply(x:V)->V`. ⭐ BLOCKING: the "honest minimal dunder set" audit was scoped to operator.py+iteration.py but `KEigenvalue` DELEGATES to `power_iteration` (eigenvalue.py) → eigenvalue.py IS in the driver surface → MISSED two `vector/scalar` `__truediv__` sites (`iteration.py:1021` Fψ/k, `eigenvalue.py:226` production renorm ERR-052). Carriers all provide `__truediv__` so the gate doesn't catch it, but Step-3 retype of Fψ/k won't type-check → add `__truediv__`+negative test. PASS: `__rmul__`-over-`__mul__` fix exemplary (left-mul only, cites `ScaledOperator`/`ZeroOperator`); Protocol-over-ABC/Generic[F] CORRECT (np.ndarray can't inherit; carrier fixed while inner leaf role flips); axis-4 Protocol-returns-`Vector`-not-`V` is a NON-issue (self-typed carrier dunders narrow). `.copy()` (power_iteration:203) correctly OUT of scope (aliasing util, not vector-space op). STANDING TELL: flat-axis-primitives (`DiagonalOperator`/`RankOneOperator`) coerce to ndarray internally → impose NO carrier-Protocol requirement.
- [#251 Leg B moment-resolved boundary trace](issue_251_legB_boundary_trace.md) — PASS-WITH-NITS (`refactor/sn-foundation-cleanup`, uncommitted). The BOUNDARY twin of #247 Leg A: carry the `2^{d-1}`-transverse face-slope through the trace for 2-D Cartesian LD. ⭐ Leg-A NIT-1 lesson INTERNALIZED — `is_moment_valued_by_flat_rank` minted in moment_layout.py, `is_moment_valued_by_rank` delegates, Leg B REUSES it (zero open-coded rank arithmetic). ADJUDICATIONS: producer 2-arm shape-discrim (`arr.shape==view.shape` vs `==[:-1]`) CLEAN + NOT a twin of the consumer rank-discrim (2 distinct boundaries: producer parses caller-intent WITH view.shape in hand → shape-eq no-S4; consumer parses trace shape WITHOUT view → rank S4-safe); 4 outflow-collapse drops COMPLETE (grep-clean, DD byte-id by construction `n_face_moments==1→tail==()`, OLD `if >1` branch already skipped for DD); trace_space.py untouched = RIGHT layering (capture alloc'd `zeros_like(face_view)` inherits widened `boundary_face_layout`; `_MovingFrontier.shed` basic-slices axes 0-2 → trailing moment axis spanned for free — "moment-ready by accident" CONFIRMED). ⭐ NIT-1 (do-now, Pattern 2): the FACE count `per_axis^{d-1}` now has 2 spellings — `geometry.py:1070` (NEW, open-coded) vs `loss_rep._n_face_moments:323` — diff's docstring CITES `_n_face_moments` but doesn't call it (Leg-A NIT-2 tell); fix = `face_moment_count(per_axis,ndim)` helper in moment_layout.py both call (cell `per_axis^d` has MANY pre-existing spellings = Arch-Opp issue). CONCERN (oracle-only): FFW seed `_octant_face_cochain:1263` discriminates with the primitive but LACKS the width-validation the windowed `_inflow_to_moments:402` has → a width-1 trailing axis SILENTLY broadcasts across n slots (probed); route FFW seed through `_inflow_to_moments` or shared `assert_face_moment_width`. Gates: 11 foundation✓ + L1 mutation✓ + sweep 672✓ + spatial/regression 88✓ (6920-ULP aniso-DD drift pre-existing).
- [#247 Leg A moment-resolved external source](issue_247_legA_moment_source.md) — PASS-WITH-NITS (`refactor/sn-foundation-cleanup`, uncommitted). Typed-union widening: `_build_fixed_source_rhs` accepts flat `(N,ng,*spatial)` OR moment-resolved `(N,ng,*spatial,2^d)` bulk, RANK-discriminated (S4-safe, cites #246); `_lift_external_source_to_moments` threads slope rows Q̂ through unchanged (was: zero them). ADJUDICATIONS the brief asked: (1) lift re-check `:1980` = JUSTIFIED self-defense KEEP (helper guards own postcondition; "shared by fixed-source+eigenvalue" docstring ACCURATE — 1 direct caller but both entries route through it `:2157`); (2) Pattern-6 PASS (no callable-projection in prod; `moment_projection :379+` is unrelated Phase-5c ANGULAR output); (3) test inline single-cell projector vs mesh-wide helper = ACCEPTABLE (the inline IS the L11 independence oracle — collapsing = tautology, do-not-de-dup). ⭐ NIT-1 (do-now, the one real gap): lift open-codes the rank test `bulk.ndim==flat_ndim` (`:1973`) = THIRD spelling of `is_moment_valued_by_rank` (sweep_graph:931 + loss_rep:515) — and the lift's OWN docstring `:1949` CLAIMS it uses the primitive but doesn't. Fix = add `is_moment_valued_by_flat_rank(array,flat_ndim:int)` core in moment_layout.py, both forms delegate (source path has no cross-section ref, only a shape → flat_ndim is what it actually wants). STANDING TELL: a SOURCE/shape-only consumer can't reach the array-taking `is_moment_valued_by_rank(array,reference)` → open-codes a 3rd spelling; the reference-array signature is the friction. Teeth EXEMPLARY (struct-not-flux, `lifted==Qm` + flat negative-control same test; Mode-10 asymmetry pinned IN the mutation test).
- [#246 _reframe intent-keyed](issue_246_reframe_intent_keyed.md) — PASS-WITH-NITS (branch `refactor/sn-foundation-cleanup`, uncommitted). `_reframe` keys the moment-frame involution on `is_moment_valued` (TYPED ORIGIN) not the coincidental `shape[-1]!=frame_signs.shape[0]` (S4 hazard: d=2 non-moment trailing-len `2^d==4` mis-fires). Named `is_multi_moment` (scheme) single-sources `per_axis>1` at the 4 threaded sites; tri-spelling `is_multi_moment⟺frame_signs not None⟺moment_tail!=()` VERIFIED provably exact. ⭐ NIT-1 TRIM `has_spatial_moment_axis` (`_bases.py:255`) — ZERO prod consumers, docstring says prod MUST NOT use it = Pattern-6; P4' pin retargets onto PRE-EXISTING `spatial_moments_per_axis==1`. NIT-2 source-site rank test `Q.ndim>sigt.ndim+1` (`sweep_graph.py:929`) = 2nd spelling of `_moment_broadcast_sigma:515` → extract `_is_moment_valued_by_rank` or cross-ref+trigger. PASS: Base-not-Protocol CORRECT (`scheme.py:553-559` minimal-mock contract — Protocol would be the VIOLATION); non-uniform per-callsite mapping honest (`_MATVEC_ZERO_SOURCE` always rank-3 flat → static False verified); `#246:` rationale docstring exemplary.
- [#245 moment_layout relocation](issue_245_moment_layout_relocation.md) — PASS-WITH-NITS (byte-id, cycle-broken). Moment-layout policy (`AVERAGE_MOMENT`/`face_moment_tail`) relocated DOWN from `sn.spatial._ubld` → NEW leaf `numerics/moment_layout.py`, killing the numerics→sn deferred-import band-aid. HOME right (layout primitive next to `face_layout.py`, NOT `numerics/spaces/`); re-export idiomatic (AXIS_NAMES precedent `axis.py:72`, OBJECT IDENTITY verified — not a twin). ⭐ STANDING LESSON: AXIS_NAMES-style down-moves leave STALE single-source CLAIMS in SN consumers that import THROUGH the re-export — they assert "single-sourced from `_ubld.X`" but `_ubld.X` is now a re-export not the source (`loss_representation.py:350`, `_bases.py:180`); diff is BLIND to them (different files). + test name still `..._matches_ubld_single_source` while body checks `moment_layout`. Grep WHOLE tree for "single-source"+moved-symbol after every such move.
- [#249 mms/ L2-norm single-source](issue_249_l2_norm_singlesource.md) — PASS-WITH-NITS (resolves the #236-Ph3 MUST-FIX; test-only, bit-id; non-slow 25✓ + migrated slow 8✓). 8 `_l2*` copies → `volume_weighted_l2` SSOT; `scalar_flux_l2_ladder`+`MMS_MAX_INNER/INNER_TOL` hoisted; `_aniso_sphere_l2_ladder` clean thin-wrapper. HOME RIGHT (`_test_helpers.py` already solver-coupled; mms/conftest hook-only). ⭐ NIT-1 (do-now) the SLOW-SET 7th copy the non-slow gate is BLIND to: `test_mms_heterogeneous.py:45` `_cell_l2` = byte-id `sqrt(sum(widths*err²))` SAME mms/ dir, `@slow` → invisible to "25 non-slow" + my Phase-3 count-of-6 → migrate at :95/:96. STANDING LESSON: a de-dup sweep scoped by a NON-SLOW gate has a slow-set blind spot — grep the WHOLE dir. NIT-2 anti-#11 migration-completes-the-followup stale docstring (`:60-64` present-tense "carry private copies ... migrate here (follow-up)" — #249 IS that follow-up). Sibling leave: `analytical/test_mms_prescribed_inflow.py` `_l2_error` (diff dir) → cross-dir sweep.
- [#236 Ph3 ST5 separability gate](issue_236_phase3_st5_separability_gate.md) — PASS-WITH-NITS (NEW test-only, 6✓ in 2.93s, the LAST #236 piece). MUST-FIX = the recurring `_l2_1d` directory-wide twin: byte-id `np.sqrt(np.sum(volumes*diff*diff))` now in FOUR `tests/sn/verification/mms/` files (`test_mms_curvilinear.py:49`, `test_curvilinear_aniso_convergence.py:65`, new `:134`, `pole_cell_characterization.py:155` as `_l2`) +2 local-named → hoist to shared helper, make THIS file first consumer not 7th copy. STANDING TELL: every new MMS gate re-mints `_l2_1d` — flag on sight; convention drift habitat (V-power/sqrt-vs-RMS). RULED OK: scalar vs per-ordinate ladder NOT a twin (3 real diffs, the `1/W` asymmetry load-bearing AT consumer — `psi_exact` un-/W by contract); `_scalar_l2_ladder` solve-recipe = 2nd twin vs mirror but ACCEPTABLE (this file's `(case,n_cells)` is the better case-polymorphic form, hoist-when-mirror-refactored). NAILS: band consts named-with-rationale (Pattern-3), `@catches` declined on no-mutation-proof test (rigorous). Micro-nit: `_mixed_second_difference` hard-assumes 2 cols, doc says so but unenforced → add `assert L2.shape[1]==2`.
- [#236 Ph2 STEP C geometry-τ retirement](issue_236_phase2_stepc_tau_retirement.md) — PASS-WITH-NITS (terminal τ-carve step; 1 prod file `reduced_operator.py` +56/−113, rest 11 tests + 5 memory). DELETES the DEAD geometry-side τ producer + 5 fields (B3 made the live paths read closure-owned τ/c off CellVisit first). Surgical (Q1 only-τ-removed, τ-only locals dropped, α-dome/redist/faces survive read-like-domain); `mu_start=-1.0` NOT magic (Hébert §3.9.4 inward pole, old `mu_edge[0]` was itself −1.0); `_c_surrogate.py` clean Pattern-5 split, L11-independent (`contamination.morel_montry_weights` imports nothing from closure), mutation-verified non-tautological; no twin across 7 migrated test files; retirement COMPLETE; T5 (`__call__`-arg) deliberate documented deferral (#248). ⭐ STANDING LESSON: migrate-then-delete leaves a STALE-DOC BLAST RADIUS in untouched-but-ADJACENT hot files — discriminate by TENSE/CITE: PRESENT-TENSE claim about a deleted data contract = MUST-FIX (`scheme.py:84-85/:273`); DELETED LINE-NUMBER cite ("Replicates reduced_operator.py:681-688 EXACTLY") = MUST-FIX (`pole_angular_closure.py:793/845/863`, the dead bit-id bridge); HISTORICALLY-framed "instead of `StreamingTerms.tau_mm`" = FOLLOW-UP (true as history, contrast target gone). Grep the WHOLE tree for deleted-symbol/line refs — the false claims hide where the diff doesn't reach.
- [#236 Ph2 STEP B3 the LIVE τ fold](issue_236_phase2_b3_tau_c_live.md) — PASS-WITH-NITS (NONE block, uncommitted, 141✓). Makes the LIVE sweep+scan+matvec CONSUME closure-owned τ/c so geometry-τ goes dead (Step C deletes it). 6 seams PASS. ⭐ Step-C PRECONDITION MET: grep-verified zero LIVE `.tau_mm` reads in the 3 consumers (all comment-only); c-formula now ONE prod site (closure ctor `:1108-1109`); `cell_balance.py` rebuild DELETED. RULINGS: τ primitive/`tau_inv`+`mm_a_in_coeff` consumer-hoist split RIGHT (Pattern 5, L16); DD raw-τ `(ψ̄−(1−τ)ψ_in)/τ` vs scan pre-split = L21 ONE-op-two-applications (algebra-identical, both read single τ — NOT collapse); DD geometry-AND-closure-blind (visit DATA); `CellVisit.tau=1.0` neutral-not-÷0; kw-only `c_in/c_out` reads like domain. Q5: assembly twin (`cell_balance_for_streaming`/`diamond.py:289-303`) NOT in diff, not worsened, no new dup. NITS follow-up: (1) theory `.. todo::` = anti-#11 EXCEPTION (file convention, build-safe, names Step C); (2) raw-τ test-ref now N=2 (surrogate=c, test bodies=raw-τ, independent) → extract on 3rd; (3) "verified-in-process 0-ULP" docstring cites no named test (snapshots pin it operationally). Exemplary phased-carve consumer step.
- [#236 Ph2 STEP B2 c-fold P2(DD.residual)→CellVisit + ABC bind](issue_236_phase2_b2_c_fold_residual.md) — PASS-WITH-NITS (none block, uncommitted, 0-ULP). Folds P2 (`DD.residual` matvec twin) onto frozen `CellVisit.c_in/c_out` via NEW single stamp `SNMesh._make_cell_visit` (all 4 dag_walk yields funnel → Pattern 2). ⭐ THE ABC-EXPANSION = RIGHT, RESOLVES the B1-flagged asymmetry (NOT scope creep): retypes `SNMesh.pole_angular_closure` Protocol→ABC, forcing `precompute_psi_state`/`cell_contribution`/`angular_adjoint` `@abstractmethod` on the ABC (matches the cited `DiscretizationSchemeBase` precedent; `psi_state:Any` contravariance minimal+correct; both concretes implement → instantiable). SSOT VERIFIED at ONE source: matvec(`cell_contribution`) + sweep(`DD.residual`←visit←`_make_cell_visit`←accessor) both read `self._c_*_per_level` (11 matvec≡sweep green, 460 sweep-core, 62 diamond+cell_balance). Defaulted-0.0 sound (Identity neutral zeros = correct slab, free). NITS (all follow-up): (1) the ASSEMBLY-DUP brief asked — 2-line `dAw·c` assembly twin in DD.residual(scalar)+cell_contribution(vec); c-FORMULA single-sourced, only thin granularity differs → Pattern-2-acceptable-for-now (`diamond.py:289-303` TODO=Phase-6 route-through), latent on a 3rd site/one-sided ΔA-w reform; (2) test surrogates `_visit_c`/`_c_from_streaming_terms` = 2 copies of the RETIRED formula (L11-justified independent ref, but 2=unify-trigger → extract); (3) ARCH-OPP: `PoleAngularClosure` Protocol now ZERO prod consumers (ABC won) → retire-or-document the dead parallel contract (closes B1 ARCH-OPP). P1/cell_balance.py=tracked B3, no untracked leak.
- [#236 Ph2 STEP B1 c-fold P3→closure](issue_236_phase2_b1_c_fold.md) — PASS-WITH-NITS (none blocking, uncommitted). Folds the 1st of 4 c_in/c_out rebuild sites (the `sweep_cache.GeometryCoefficients` populator) onto the closure via NEW public `c_in/out_per_ordinate` accessors; producer SSOT = M-M `__init__:905-920`; bit-id STRUCTURAL (same `quad.level_indices` both sides + 0-ULP τ + pure-perm gather). B2/B3/C tracked crosswalk §7 (anti-#11 OK). ⭐ KEY RULING: mint-on-both CORRECT locally BUT the cited `transverse_coupling_is_facewise` precedent binds its consumer to the **ABC** (Protocol=plain-`bool`, pure runtime_checkable) while `pole_angular_closure` binds the consumer to the **Protocol** (`geometry.py:460`) — THAT asymmetry is the sole reason the `is_linear:bool→ClassVar[bool]` fix was forced. is_linear ClassVar = CORRECT keep (genuine class trait, ABC+docstring agree, revert reintroduces `geometry.py:465` error). ARCH OPP (out-of-scope, FILE issue): type `MeshSN.pole_angular_closure` against the ABC to unify the two mint-on-both bindings. NITS: `_gather` `np.empty`+full-cover overwrite → silent garbage on a future non-covering partition (fix `np.zeros`/coverage assert, anti-#16); N recomputed not `_N`; prose-not-symbol comments.
- [#236 Ph2 Step A τ carve](issue_236_phase2_step_a_tau_carve.md) — PASS-WITH-NITS (uncommitted). Relocate Morel-Montry τ PRODUCTION (BMC Eq.43) from streaming-GEOMETRY factory → angular closure (`morel_montry_tau_per_level`). KEY RULING: the line-for-line replica of `reduced_operator.py:681-688`/`:798-815` (τ now 2 places) is the CANONICAL bit-identical parallel-run de-risk bridge — ACCEPTABLE because single-sourced at CONCEPT (Eq.43), twin PINNED 0-ULP by Leg-1 gate to factory+independent ref (L11), retirement COMMITTED (anti-#11: plan §1 names 4 c_in/c_out sites + Step B; theory stub). EXEMPLARY temporary marker (comment explains the L11 no-call-contamination-in-prod reason). Cartesian neutral-τ by TYPE (Identity) not branch (Pattern 4); coord-branch selects edge-convention BELOW type dispatch. NITS: pyright `Quadrature`≁`AngularMeasure` Protocol `:87/:148` = PRE-EXISTING suite-wide (test_diamond 6× same, no ignore) → route #226 NOT isolate; c_in/c_out 5th-twin owned by Step B; line# cites→symbol. 9✓ -O + diamond 53✓.
- [#236 Ph1b honest curvilinear selection](issue_236_phase1b_curvilinear_selection.md) — PASS-WITH-NITS (uncommitted). `supports_curvilinear` trait (DD T/LD F/base F) + `_curvilinear_capability(mesh)→Compatibility` single-source gate at 3 sites closes LD-is-affine-scannable→curvilinear-LD-passes-supports→mid-sweep-raise dishonesty. RULING: keep gate SEPARATE from 1a's `pairing.py` (selection-capability Compatibility/dispatcher vs physics-pairing bool/frontend — diff return types argue AGAINST unify; NOT SNMesh-Pattern-4, curvilinear-LD IS legal-to-construct+query-graceful-False). Keep ALL 3 defenses (selection=frontend-honest / affine_scan_coeffs:741=construct-bypass / `_require_slab`:194=kernel) — distinct boundaries. DO-NOW NIT: top-`default_for` check justified (specific reason, test-pinned) but comment must warn future-DRY-delete + name pin (anti-#11). 24✓ / 7✓ -O.
- [#240 D5b-S4 LD 2-D STRESS MMS](issue_240_d5b_s4_ld_2d_stress_mms.md) — PASS-WITH-NITS (new L1 MMS for S3 bilinear-UBLD closure; 8 foundation -O 8/8). EXEMPLARY: `_LD2D_STRESS_COEFFS` `(num,den)` single-sources amplitudes Branch-1 `Rational`/Branch-2 float (cross-check pins EVALUATORS agree); `prescribed_inflow` reuses Phase-4.6 primitive clean (2-D slot IS `(N,ng,ny)`); honest-scope documented 3× (anti-#11-EXCEPTION-right); quad-exactness VERIFIED. NIT-1 (CONCERN) Branch-2 `external_source` hand-transcribed twin of `Q_closed` (JUSTIFIED L11, NOT collapse) but cross-check pins only g=0/1-cell/folded-`sig_s_eff` → g=1 `c_g` scaling + 2G downscatter-transpose blind to foundation gate → parametrize over g. NIT-2 struct-indep guard checks type-not-import-graph (OK, NAME over-promises→rename). NIT-3 `build_materials` BYTE-DUP of `SN2DCartesian2GHeterogeneousMMSCase` (infra boilerplate, 2-copies=unify trigger)→extract `_build_per_cell_hetero_materials`. Standing tell: MMS-case family twins `build_materials` per-cell-Mixture loop.
- [#240 D5b-S3 ERR-061 + WHOLE-DELIVERABLE](issue_240_d5b_s3_err061_moment_frame.md) — PASS-WITH-NITS (complete unified-moment-matvec+ERR-061+OWED-2 scan, 23 files). All 5 bars hold: branch-free GENUINE (pure `spatial_basis_per_axis` trait, no `if d==1`); `_slope_fold`+`_reframe`+`octant_moment_frame_signs` each PROVABLY one source (3 slope consumers delegate, no scan-vs-DAG twin); M-norm NAMED+explained (residual_kernel_batch:639); `_kernel_terms` DELETED (kernel_rhs = orphan flat-source oracle, foundation-pinned, keep); reads-like-math (φ̂ einsum, __post_init__ Pattern-4 guard). Prior ERR-061 DO-NOW RESOLVED (closure gone; remaining = 2-line binding dup, NIT not twin — `_OneDimScanWalk` can't reach the method). Gates: 34+60(DriftWarning-err)+7✓. 1-time -O flake on frame-consistency test = NOT a defect (probed: GLOBAL slope under both modes). #246 confirmed filed for the `_reframe` shape-probe (+2 new same-family probes in solver helpers).
- [Nexus elegance diagnostics](nexus_elegance_diagnostics.md) — TOOLING (not a verdict): 5 Nexus structural-smell tools (twin_paths/native_place/discriminations/dead_functions/protocol_conformers) that mechanize my axes 1-4+7; tool→pattern map, FP classes, the `nexus-elegance` skill draft, PR vs debt-sweep routing.
- [#240 D5b-S3-A0 SpatialMomentSpace](issue_240_d5b_s3_a0_spatial_moment_space.md) — PASS-WITH-NITS. Mint `SpatialMomentSpace` (typed within-cell tensor-Legendre DG peer of `SphericalHarmonicSpace`) + optional field-space factor (default-OFF, construct-general) + `find_factor` (#207). MUST-FIX Q2: `find_factor(self,factor_type:type)->FunctionSpace` LOSES the queried type (`.L`→object); fix=PEP-695 `find_factor[T:FunctionSpace](type[T])->T` (py3.14 native). DO-NOW Q4 twin: `from_mesh_and_L:266` RE-INLINES the "append iff !=()" gate (negated spelling) that `BulkField._compose_spatial_moments` SSOTs; HMF IS-A BulkField → route through it. Q3 LAYERING verdict (the one asked): deferred numerics→sn import is RIGHT (cycle is the `sn.__init__` PACKAGE, `_ubld` is numpy leaf) BUT `AVERAGE_MOMENT`/`face_moment_tail` are physics-free layout policy MIS-HOMED in `sn.spatial._ubld` → file issue to relocate to numerics (Pattern-6: when 2nd numerics consumer lands). PASS: true-peer idioms; MomentDisplacement ripple = affine-torsor symmetry WORKING (field-copy mint guardrail, inst-#6; field-vs-space-factor is the ripple discriminator); einsum `fc...->gc...` textbook Pattern-7 producer broadcast byte-id at no-axis. 34 foundation green.
- [#240 D5b-S2 UBLD d≥2 kernel wiring](issue_240_d5b_s2_ubld_wiring.md) — PASS-WITH-NITS. Closes LD d≥2 raise → wavefront via `_ubld` primitive. `spatial_basis_per_axis` lattice = GENUINE reduction (DD/Step byte-id by default-trait=1→`tail=()`, no length-1 axis; 38+38 green). ELEGANT: Q_cells HONEST scalar-lift (NOT the crosswalk's shape-sniff "accepts BOTH"; full vec→S3); d=1 fast-path cleanly branched (never per_cell_solve); `_CellResidual` d≥2 raises NotImplementedError (S3 iterate defer, illegal-state-LOUD); closure-consistency out==in-of-next pinned (D5b.1/.4 NON-flat both-dir). NITS: (1) MUST-FIX doc-regression — `spatial_basis_per_axis` inserted BETWEEN `is_affine_scannable=True` and ITS docstring → AST-verified orphan (the wavefront-not-scanmarch rationale = dead 2nd literal); (2) slot-0 "average moment" convention re-spelled 6× / 3 files (2 verbatim `capture[...,0]` twins) → name `AVERAGE_MOMENT=0` in `_ubld`; (3) `tail=() if n==1 else (n,)` idiom dup'd sweep_graph:267 + loss_rep:1084 (storage-B/-A twin) → extract `_face_tail`; (4) 2 gate-spellings (`len(s_axes)>1 and per_axis>1` vs `_n_face_moments!=1`) coextensive only under `len(s_axes)==ndim` (holds via zip(str_axes,cell_idx)) — STRENGTH today, smell on a future partial-s_axes caller.
- [#240 D5b-S1 UBLD HINDSIGHT audit](issue_240_d5b_s1_ubld_hindsight.md) — PASS (whole-deliverable, not per-diff). Placement: `d1_closed_form` KEEP in `_ubld.py` ESCALATED (deciding axis = co-location-with-the-reduction-it's-proven-==-to, NOT one-class-consumer; moving it splits a proven-equal pair across a module boundary → S2-drift). Dense primitive in S1 w/ ZERO prod consumer = NOT Pattern-6 premature, it's the reference-oracle exception (shared kernel + @foundation 1e-12 pin + d2 exact-on-bilinear catches ERR-060); shipping in S2 would be WORSE. sympy↔numpy twin JUSTIFIED algebra-of-record — do NOT single-source the 4 factors (defeats independence); `balance.py` clean (no Kron/no LD mass). ONE real dup (COLLAPSE do-now): `schur_xV`/`scan_xV` both recompute `g*V`+`V*eff_denom` (`_ubld.py:346/348/371/372`) → extract `_xV(V)`; latent until ÷V→×V convention change (curv V_eff / D6 w(Σ)). `derive_*` hand-transcription = necessary L11 (importing prod = vacuous). CONCERN: numpy-vs-symbolic MATRIX pin fires only d=1; d≥2 numpy pinned only by physics → S2 add d2-A==d2-A. Line-number cites → symbol cites (recurring tell).
- [#240 D5b-S1 UBLD Branch-2 PRODUCTION](issue_240_d5b_s1_ld_ubld_branch2.md) — PASS-WITH-NITS. The numpy production counterpart of Branch-1: NEW `_ubld.py` (d-generic dense Kronecker primitive + vectorized `d1_closed_form` fast path) + collapse of the 3 slab-LD views onto ONE helper. 4 load-bearing axes PASS PROVABLY: (1) Rule-of-Three collapsed — ALL SEVEN coeff crosswalks `sp.simplify(diff)==0` (D₂'/S/eff_source/eff_numer/scan-a/kernel_rhs/w), only diff=FP-assoc ~1ULP; (2) d=1 STAYS fast-path L16 — grep-clean, prod imports ONLY `d1_closed_form`, zero `per_cell_solve`/`linalg.solve` in prod LD; (3) primitive reads like math, zero dead dataclass fields; (4) ×V↔÷V crosswalk `D₂'=θ·V·d2` (extra-θ habitat) single-sourced as ONE named intermediate, proven==legacy. Gates: new 10✓, spatial+sweep-core 506✓ (DD bit-id negative-control held), LD two-paths+MMS 5✓. NITS none blocking: test :284 numpy.bool→bool (cheap), pyright :175/:263/:527 all benign (#226 staleness + A_total contract-uniformity-not-smell, DD consumes it diamond.py:587), `assemble_inflow_axis` axis∈{0,d-1} else-raise = honest-per-phase capability boundary NOT anti-#7. S2 carry: d2 inflow |μ_axis| Mode-3 habitat, d=1 blind, gate has teeth.
- [#240 D5b-S1 UBLD Branch-1 SymPy ref](issue_240_d5b_s1_ld_ubld_branch1.md) — PASS-WITH-NITS (NEW, ships nothing). The d-generic UBLD (2^d bilinear/trilinear DG-P1, NOT simplex-P1) algebra-of-record + 6 @foundation oracles (all green). 2 load-bearing axes PASS PROVABLY: (1) Kronecker SSOT — 4 one-D factors defined ONCE, `assemble_ubld` composes d-generic zero per-d branch, `xy` coupling EMERGES; (2) canonical-source-NOT-copy — symbolic imports ZERO prod, prod proven to REDUCE TO it via d=1 Kron identity (`diff_A==zeros`). CONCERNS: L11 THINNESS (3 SYMBOLIC d=1 oracles compare hand-transcribed prod-formula COPIES, real independence rides on ONE numeric test calling live `LinearDiscontinuous().update`; d=2 oracle IS independent MMS + ERR-060 proves it caught a d=1-blind bug); prod LINE-NUMBER citations (:342/:393/:435 → use SYMBOL cites). NITS: pyright :275/:277 Expr-not-Matrix return lie (fix), :290 unused `axis` dead-weight (drop), test :145/:146/:177/:178 float|None narrow (mirror prod :321), test :138 Quadrature/AngularMeasure + #226 import = NOISE (don't fix). Exemplary -O-safe guards + foundation tagging + catches(ERR-060) grounded.
- [#240 Ph1 matvec_via_kernel RETIRE](issue_240_matvec_via_kernel_retirement.md) — PASS clean (`001c836`). REVERSES the Inc-B compromise & SUPERSEDES the trait-PASS below per [[feedback-principled-over-bit-identical]] (user: never compromise architecture for bit-id). Flag DELETED; Cartesian matvec → polymorphic `residual_kernel_batch` UNIFORM DD+LD (zero scheme branch; `s=2|μ|/Δ` shared, LD halves `g=0.5*s` internally). RULINGS: cartesian-vs-curvilinear = GEOMETRY branch not scheme; curvilinear `2ψ̄−ψ_in` = honest single-occupant (LD-curv RAISES, predates commit); `assert_regression(kind="direct",nulp=nx)` = anti-pattern-#17-RIGHT (bit-id kept as escalatable DriftWarning, struct-indep refs green). Phase-2 `streaming→g` defer correct (couples #239). RAN: SLB 6/6 pass; 3 SPH reds = pre-existing #195/#209 deliberately-flagged. 2 nits (latent Pattern-6 diamond-march twin → defer to LD-curv occupant; nulp-vs-64ULP docstring).
- [#240 Ph2 Step D1 outgoing_face](issue_240_phase2_step_d1_outgoing_face.md) — PASS-with-nits. ONE `outgoing_face_from_average(ψ̄,in,w)=(ψ̄−(1−w)in)/w` (inverse of `cell_average`) single-sources 7 inlined outflow reconstructions; grep-clean (only survivor=β scan-source :1435 + residual balance terms, all correctly deferred/excluded). LD w/k algebra VERIFIED EXACT (`k=g_over_theta/d2` ≡ scan-coeff `k=p/d2`; reconstruction `w` ≡ `affine_scan_coefficients` `w` — ONE concept). DD w=½ byte-id real, LD ~1-ULP principled. Free-func kept correct for D1 (D2 homes onto Base — not premature). NIT-1 (Pattern-2 acceptable): LD `w=1/(1+g_over_theta/d2)` VERBATIM twin at cell_kernel:463 + residual_kernel:492 (SOLVE/APPLY pair, shares `_kernel_terms` but not the w-line) → collapse dest = `_kernel_terms` returns `w`; hazard = future `w(Σ)` Péclet blend lands one-not-other. NIT-2: `.. todo::` stub = anti-pattern-#11 EXCEPTION (tracked D6 + memo). Runtime green 505/1083/3.
- [#240 Ph2 Step D2 homing](issue_240_phase2_step_d2_homing.md) — PASS clean. The HOMING D1 predicted: 3 reconstruction ops (`source_emission`/`cell_average`/`outgoing_face_from_average`) move from dangling free-func module `affine_closure.py` → `@staticmethod`s on `CellUpdateBase` (DiscretizationScheme Base); `affine_closure.py` `git rm`-deleted. staticmethod RIGHT (pure, zero instance state); `CellUpdateBase` RIGHT home (diffusion subclass inherits; mixin = premature). Homing COMPLETE (grep-clean: 0 live imports; survivors = 1 history comment + repointed `test_affine_closure` module name + stale `_build/html`). 3 caller idioms each correct: scheme `self.<op>`, loss-rep `self.mesh.cell_update.<op>` (verified dominant idiom ~12×), scan.py `CellUpdateBase.<op>` (no instance). Rich docstring PRESERVED verbatim into class docstring (Rule 3). LD `w`-fold RESOLVES my D1 NIT-1 at predicted dest (`_kernel_terms` returns `w`, both arms consume; byte-id hoist; `w(Σ)` Péclet hazard closed). `w: float|np.ndarray` widen string-only. −1 test (5250→5249) = BENIGN (module `git rm` drops auto-discovered `test_layer_imports` param by 1 — recurring tell, not coverage loss). 1 latent NIT (D6 `w(Σ)` must land in `_kernel_terms`). Runtime 505/1/4 + 1083.
- [#240 Ph2 Step B σ single-source](issue_240_phase2_stepb_sigma_singlesource.md) — PASS-WITH-NITS. `InvertibleOperator` OWNS its matvec (override the OperatorSum leaf-sum); σ single-sourced from C; `loss_action(operator)`→`loss_action(sigma)` UNIFORM across 9 classes (14 sigs typed, grep-clean). RULINGS: override REMOVES the pre-carve solve-fused/apply-distributed asymmetry (not "sometimes distributes" confusion); Resolution-A two-conv web COHERENT (L-leaf subtracts σ_t, composite doesn't, same loss_action); `_require_typed_composite` good Pattern-2 (4 consumers); tests EXEMPLARY (build σ_r≠σ_t prod can't yet → correct-by-construction); re-baseline HONEST (5 .npy ≤5 ULP). NIT-1 (do-now): `loss_action(sigma)`→`_apply_walk(sigma_t=...)` — internal helper param named `sigma_t` RECEIVES σ_r in removal form (latent, prod=σ_t today); rename along the thread. NIT-2 (anti-pattern-11): `InvertibleOperator` CLASS docstring still says "inherits OperatorSum apply" + "apply_transpose reserved for Phase H" — both refuted by THIS commit.
- [#240 Ph2 D5a ScanMarch→coeff-model](issue_240_phase2_step_d5a_scanmarch_coeff.md) — PASS-WITH-NITS (1 BLOCKING). `cartesian_scan_coefficients` NOT a twin (genuinely distinct decomposition: ONE coupled axis → scan transmission `a` no batch kernel computes; `_CellSolve`/`_CellResidual` asymmetry). BLOCKING: DD `2g` cell-balance diagonal now the 3rd Cartesian transcription + `w=½` hard-stamped 5× → collapse to `_cartesian_streaming_diagonal`+named `_DD_W` (crosses the "third fold" tripwire; `w(Σ)` Péclet hazard). CONCERN: `reflect_scan_coefficients`≡`outgoing_face_from_average` in (α,β) form, hard-codes w=½ (no scheme `w` accessor exists) → make w-generic base. PASS: Protocol placement (base-only, matches precedent), zero inline DD in consumers (check 4 headline), transverse-source fold (check 5).
- [#240 Ph2 D5-0 scheme-trait routing-honesty](issue_240_phase2_step_d5_0_scheme_trait.md) — PASS-WITH-NITS (1 blocking). `transverse_coupling_is_facewise: ClassVar=False` (DD True, LD inherits) kills the SILENT 2-D LD→ScanMarch(inline-DD, slope-dropped) misroute; Pattern-4 textbook, `False` conservative-default, SSOT (2 values/1 read site), e2e honesty VERIFIED (wavefront LD raises real NotImplementedError d≥2), docstrings+tests exemplary (anti-#11 both-ways + strategy-free trait probe). BLOCKING RULING: drop the trait OFF the `@runtime_checkable` Protocol → Base-only; runtime_checkable checks PRESENCE-not-type/value → broke 2 fixtures + latently re-opens narrower silent-misroute (`="yes"` passes isinstance, reads truthy); NO prod `isinstance(x,Protocol)` so Protocol data members = pure cost. 2 non-blocking (D6 todo tracked-exception; LD docstring 1-clause disambig). 37/37 green.
- [#240 Step A diamond-2 relocation](issue_240_step_a_diamond_relocation.md) — PASS-WITH-NITS. The DEFERRED Phase-2 `streaming→g` from the matvec_via_kernel-retirement note above, now landing (couples #239). Move DD's diamond `2=1/w_DD` from producer (`SNMesh.streaming`→raw `g=|μ|/Δ`) INTO scheme kernels; LD drops its `0.5*s` un-bake; both DD kernels apply `2` UNIFORMLY in the left fold (denom AND numer — non-uniform-2× hazard documented in diamond.py docstring). Producer SSOT grep-verified (zero remaining pre-scaled producers); both DAG walks single-source the `2` through `cell_kernel_batch`/`residual_kernel_batch`. 2-D ScanMarch inline-DD = ACCEPTABLE deferral (#239 OPEN+accurately-titled, the ONLY non-kernel-routed seam, DD-only, bilinear-2D-LD→#158 Inc D). Bit-id real (×2/×0.5 power-of-2 commute → 505/1/4 green no-rebaseline; only hand-calc + kernel-sha goldens move). Hand calcs HONEST (denom 18=2+2·3+2·5 / 4.7=0.5+2·(0.3+0.7+1.1) / 1.9=0.5+2·0.7 all verified). sweep_regression keeps OLD `2|μ|/Δ` as struct-indep oracle. NIT-1 (Q3) `alpha=2.0*sx2` reads "2×2g" (comment-only clarity, value bit-id). NIT-2 `:2058 abs_mu/V[i]` re-derives `g` (Pattern-2 dup, in-code `# NOTE(#240)` flags collapse to `streaming(0)[..,i]`).
- [#158 Inc B coefficient model](issue_158_increment_b_coefficient_model.md) — PASS-WITH-NITS. `affine_scan_coefficients`→`(a,inverse_denom,w)` 3-tuple; NEW `affine_closure.py` free funcs; closure-method HOOKS retired; LD `is_affine_scannable=True`+slab-guard. SETTLED: coefficient-model coherence HONEST; matvec=group2/scan-solve=group3 SPLIT CORRECT (apply/solve _CellResidual/_CellSolve asymmetry — solve has no concrete inflow, apply has probe ψ̄); `A_total` unused on LD = contract-uniformity NOT smell (geometry-blind shared contract, DD uses all); 2-D ScanMarch inline = justified bounded DD-only seam (α/β bake w=½). 4 NITS: NIT-1 (do-now Smell-#7) `affine_closure` docstring APPLY formula `(ψ̄−(1−w+w·a)ψ_in)/inverse_denom` does NOT reproduce DD's residual_kernel_batch (×V scan inv vs ÷V kernel diag); the test pins SOLVE not this — "verified" overstated; NIT-2 (do-now Rule-3) docstring "DD snapshots re-baseline" FALSE — DD byte-id 505/1/4 GREEN under DriftWarning-error, principled-equiv is LD-only; NIT-3 (do-now #11) 3 stale `:meth:` refs to RETIRED methods on LIVE code (diamond.py:133 ALSO asserts now-false "LD NOT affine-scannable", :485 "triple"→single, loss_repr:1886) +1 historical (test baseline:19); NIT-4 (record) "SymPy-verified" w/ no checked-in artifact. Carry-fwd: all pins at Q̂=0 → Inc-C slope-source sign-trap.
- [#158 Inc B matvec_via_kernel trait](issue_158_matvec_via_kernel_trait.md) — PASS (2 nits). Follow-up: uniform-routing DD-via-÷V-kernel drifted ~1 ULP on NON-power-of-2 widths → 11 strict snapshots broke. Fix = `matvec_via_kernel: ClassVar[bool]=False` (matvec analog of is_affine_scannable); LD overrides True. RULINGS: (1) NOT a twin — DD `cell_balance` (geometry-spanning, M-M curvilinear) vs LD ÷V Schur are different per-scheme math, SSOT-per-scheme intact; (2) flag RIGHT vs re-baseline — `cell_balance` structurally required anyway; re-baseline would couple DD goldens to LD convention, lose resolution (anti-pattern-17 defends the DriftWarning gate); Pattern-4 makes "DD via kernel" unrepresentable; (3) docstrings accurate, stale "kernel for DD and LD alike" GONE, affine_closure note CLOSES old Inc-B NIT-2. Pins: LD arm e2e `test_mms_ld_slab::krylov_matches_si`, DD byte-id `slab_*_apply_bit_identical` under DriftWarning-error. NITs: NIT-1 do-now reword "geometry-blind to this flag"→"on non-Cartesian branch"; NIT-2 Inc-C seam (trait geometry-entangles when curv LD publishes). Carry: pins ride Q̂=0.
- [#158 LD spatial occupant](issue_158_ld_spatial_occupant.md) — `LinearDiscontinuous` review series (first non-DD `CellUpdate`, slab/2-moment). STEP-1 (group-1 per-cell) PASS-WITH-NITS: `(2,ng)` moment-source ACCEPTABLE staged carrier (scheme-is-dispatch not shape-dispatch); Schur-scalar contract CORRECT (slope cell-LOCAL, eliminate locally; `_schur_terms` Pattern-2 SSOT); NIT-1 (slope-row twin→bundle `_LDCellTerms`) CONFIRMED FIXED. **INCREMENT A (group-2 ÷V kernel + solver guard) PASS-WITH-NITS:** (a) group1(×V)≡group2(÷V) SEPARATE = ACCEPTABLE Pattern-2 (different convention layers, NOT a twin; LD group-2 TIGHTER than DD — shares `_kernel_terms`, DD inlines fold twice; pinned by `test_group1_equals_group2_flat`). (b) solver guard `reduced and cell_update.is_affine_scannable` (solver.py:907) PRINCIPLED — capability-TRAIT not identity, SAME predicate scan-`supports` reads (single-sourced concept), Pattern-4 makes "scan cache for non-affine scheme" unrepresentable. Dispatch verified e2e: `default_for`→`FullFieldWavefront` d-generic spine, ZERO new strategy (rejected `OneDimPerCellWalk` twin correctly absent). NIT-1 (do-now): comment `:360-363` OVERSTATES the pin (Q̂=0 flat-slice ONLY; the UNTESTED slope-Q̂ path carries the LM-1989 slope-sign trap) → scope the comment + optional parametrize pin over (mu,h). NIT-2: positional `s_axes[0]`/`psi_in[0]` = Pattern-7 trigger when Inc-D wires bilinear multi-D. ARCH watch: DD batch pair now less-elegant sibling (don't touch — fold-order=d=2 bit-id contract).
- [W1 M-M τ-clamp unclamp](w1_mm_tau_clamp_unclamp.md) — sphere clamp removal PASS-WITH-NITS: SSOT single-sourced at producer (`tau_mm` one write, all-reads — no recompute twin); drop `tau_raw` name CORRECT (no distinct identity left); sphere-unclamped/cyl-clamped = PRINCIPLED divergence (cyl τ_raw=0 ÷0 structural, sphere non-singular) NOT a twin; static-not-dynamic removal preserves SI≡Krylov linearity; doc-drift (module docstring + theory `:label:morel-montry-clamp` + matrix.rst + `cell_balance.py:310` stale comment) is anti-pattern-11 exception DEFERRED-to-W5, tracked. NIT: tighten cell_balance:310 comment now; equation-label duplicated .py-docstring+.rst is latent drift.
- [A1 diamond face-closure seam](a1_diamond_face_closure_seam.md) — #206 Phase A1 PASS-with-nits. 1-D analogue of S6.3b `_OctantWalk`: diamond FACE closure → ONE seam (`cell_average_from_faces`/`outgoing_face_from_average` on CellUpdate); supports gate on `is_affine_scannable` = Pattern-4 (correct OUTER-`and` composition, defense-in-depth w/ NotImplementedError defaults). FINDING (CONCERN, out-of-scope) NOW RESOLVED by scanmarch-2d carve below. NIT: `psi_face_in` rebinds to its own output (pre-existing, defer to A2). A2 denom-routing deliberately deferred — do not flag.
- [ScanMarch-2D diamond-closure routing](scanmarch_2d_diamond_closure_routing.md) — #206 A follow-up PASS-with-nits; CLOSES A1's CONCERN. Routes `:1422` matvec `out_y` + both `_scanmarch_row` sweep closures through the `cell_update` seam; concept back to ONE site (grep-proven: scan.py zero inline closure arith, all surviving `2.0*` are scan COEFFs). β=2ψ̄ `:1410` correctly LEFT (it's the apply-recurrence COEFFICIENT, not a closure; seam is single-cell, coeff feeds a chain). scan.py→cell_update TYPE_CHECKING import OK (same package, scan-consumes-closure layering, DI param). `_scanmarch_row` should NOT dissolve — sweep recovers-ψ̄ (both seam methods) vs matvec residual-from-known-ψ̄ (one method, discards out_x); the apply/solve difference = the _CellSolve/_CellResidual asymmetry; dissolving = unify-over-the-difference trap; defer to Phase B shared-frame is correct. Docstrings IMPROVED. No anti-pattern-#11 dangling.
- [#206 Phase B `_OneDimScanWalk` frame](b_one_dim_scan_walk_frame.md) — PASS-with-nits. Extract the 1-D analogue of `_OctantWalk` (frozen sole-`mesh` holder) + relocate `_sweep_1d_unified`/`_ensure_*_cache`/`_run_1d_sweep` INTO it, bit-id (whole-word `sn_mesh`→`self.mesh`, awk-verified zero leftover; verbatim body). Carries weight NOW (2 live consumers = unify-after-two; apply-kernel DEFERRED to Phase C, documented-not-prebuilt = Pattern-6 OK). `_initial_guess_values` kept module-level CORRECT (pure extractor). `is_slab` geometry-fork SOLVE-only correct; clean Phase-C seam. B2 tripwire = faithful clone of octant AST `is_solve` guard. NIT-1 (CONCERN): 3 LIVE `:func:` rst xrefs to retired `_sweep_1d_unified` MISSED (loss_representations.rst:273, index_convention.rst:1176/1422) — fix; the .py refs WERE updated. Discriminator: dangling `:func:` ROLE=fix, historical literal=leave (index_convention:405 PR-INDEX row + 4 test docstrings = leave). NIT-2 (cosmetic): 0 blank lines between the 4 frame methods (PEP8 + `_OctantWalk` want 1).
- [#206 Phase C 1-D matvec relocation](issue_206_phase_c_1d_matvec_relocation.md) — PASS-with-nits. Fwd matvec + transpose + decomposition split move OFF `_compute_LpC`/`_compute_LpC_transpose`/`_compute_decomposition` (operator.py) INTO `_OneDimScanWalk._apply_walk`/`.loss_action_transpose` (the 1-D S6.3b analogue; L21 matvec≡sweep a code fact). 4 RULINGS: (1) `emit_angular:bool` = ACCEPTABLE emit-policy fork NOT Smell#3 (both arms apply-dir, identical recurrence; tripwire forbids only is_solve/is_apply/is_matvec; project's own 2-D tripwire docstring sanctions "KERNEL OBJECT + EMIT policy"; perf +14-19%; buffer-presence-driven store; -O-safe None-guard); (2) `operator`(protocol)/`sigma_t`(private helper) asymmetry PRINCIPLED (avoids fake-operator duck-typing; tracks protocol-vs-internal boundary); (3) transpose-as-separate-body CORRECT (fwd-sub vs reverse-sub + angular_adjoint = genuinely different walks; folding=unify-over-difference; shared denom is the right SSOT); (4) L21 = DEFENSIBLE MILESTONE not incomplete twin (sweep cumprod-scan vs apply per-cell-loop differ because solve≠apply; co-located sharing diamond seam+denom is sufficient — that's where Phase-F #7 drift lived; deeper sweep+apply cell-kernel fold correctly DEFERRED as nULP denom-fold, do NOT flag). Bit-id RE-RAN 9@0ULP; 2 sphere-apply reds VERIFIED pre-existing #195 via throwaway worktree at base. 2 in-scope NITs (approval conditions): NIT-1 stale `NotImplementedError` msg `:2211` names DELETED `_compute_LpC_transpose` (carve-authored `7300f3e`, fwd twin `:1934` WAS fixed — one-not-the-other); NIT-2 PHANTOM ψ-keyed-cache narrative in ~5 operator.py sites (`acbcddc` admits it never existed; `_compute_decomposition` is bare delegation, prod `apply` bypasses it entirely; `AngularRedistributionOperator.apply` re-walks while docstring claims cache-hit). Out-of-scope: dead diag_p42 refs in 2 untouched files. ARCH-Q: M_spatial/M_angular_redist leaves now have NO prod-hot-path consumer — retire-to-helper question (pin TestT4b/T4c).
- [Field-role + block-role attrs](field_role_and_block_role_attrs.md) — per-leaf role-determined constants; the dataclass-field trap (ClassVar on non-dataclass base vs plain-attr on frozen-dataclass leaf); value-based isinstance metaclass; composite block_role DERIVES not hand-stamps.
- [Wave O operator algebra](wave_o_operator_algebra.md) — the role grid (.apply=source/sink, .solve=flux, from_balance=residual); the G-adjoint capability-lattice illegal-states win (C/S/F=None sound, no CAP_APPLY_TRANSPOSE); the phased-carve twin-DELIVERY smell + recurring tells.
- [Phase 5 windowing carves](phase5_windowing_carves.md) — 5a/5b/5c angular→face→moment windows; 5c = first twin-collapse; the window≡full reference-oracle retirement-exception (shared kernel + corner-probing pin); DI-over-flag + polymorphic-sink rejection; FaceField unify-after-two deferral.
- [#208 affine flux algebra](issue_208_affine_flux_algebra.md) — FluxRole torsor (flux+flux→TypeError); the residual/displacement asymmetry principle (construction-engine vs methods-mixin); box-7 evaluate_residual; sha256 bit-identity golden tooling; the loss-matvec fold twin.
- [Composite fixed-source API](composite_fixed_source_api.md) — "what good looks like" (Union parse-at-boundary, prescribed_inflow Pattern-4); the SNMesh re-home seam that defeats the mesh-identity guard via .copy().
- [N-D generic carve: tuple/axis](nd_generic_carve_tuple_axis.md) — C3 d-generic sweep: Cartesian-vs-curvilinear two-family SSOT is NOT a twin; positional axis tuple → WavefrontFlux.axes at the WALK not the KERNEL; `|None` accelerator fields; the `s`(slice)/`s`(streaming) same-scope kwarg shadow.
- [SweepStrategy carve](sweep_strategy_carve.md) — S1 sweep-side PASS-with-nits: Protocol+`_SweepStrategy` ABC NOT a twin; `_DAGWavefront` shared-supports earns it; honest-per-phase `ndim==2`; moment-guard MOVE to CumprodScan; 2 optional nits (reciprocal lazy-import comment, concrete-leaf registry invariant). S2 lens = twin-delivery plumbing.
- [S6.4(c)+(d) DAG-ownership + oracle fold](s6_4_dag_ownership_move.md) — both PASS. (c): mesh→`_DAGWavefront` per-shape `lru_cache(8)` `for_shape`, illegal `=None` retired, MappingProxyType follow-up. (d): `_OctantWalk` ×3→×1 (`_sweep_full_field` DELETED); in-edge-only-seed≡ι_* BYTE-id proof + per-octant-shed≡absorb; d-generic `_sweep_2d_scheduled`. CONCERNS: 3 stale WavefrontFlux orchestrator docstrings + a bundled @slow cylinder un-xfail.
- [S6.4(e) walk×level-op collapse](s6_4_e_walk_levelop_collapse.md) — PASS-with-CONCERNS. 4 graph walks (apply/residual×full/windowed)→2 storage walks × `_CellSolve`/`_CellResidual` level-op (direction-as-OBJECT); diamond→pure cell algebra, `SweepCellSlice` retired; kernel-pair stub complete. CONCERNS: `_CellSolve` MISSING `_SweepEmit`'s exactly-one-mode `__post_init__` guard (illegal half-wired build succeeds, fails deep in walk); 8 prod + ~11 test stale `:meth:` refs to retired graph methods (.rst already fixed). Test-migration FAITHFUL (2 documented dissolutions only). Moment-branch now ×3 emit sites = pattern-#2 acceptable.
- [S6.4(f) module geography + WavefrontFlux retire](s6_4_f_module_geography_wavefrontflux_retire.md) — PASS clean. sweep.py DISSOLVED into loss_representation (verbatim bodies, byte-diffed) + scan.py; `_sweep_2d_scheduled`→`_sweep_scheduled` rename; lazy-import cycle GONE. WavefrontFlux/InteriorFaceSpace retired w/ adequate succession notes (every named symbol grep-real). Spy seam verified (patch target==call-time resolution site). De-roling discipline (live→`:func:` new home, dead-pre-session→literal) sweeps up (d)/(e) stale-role CONCERNs. Grep gate for module dissolve: `:func:`[~]?orpheus\.<mod>\.` TRAILING-DOT.
- [S6.5 one representation instance](s6_5_one_representation_instance.md) — PASS clean (the #222 CAPSTONE). FOUR `default_for` construction doors → ONE operator instance; L21 matvec≡sweep becomes a TYPE FACT (stateless frozen dataclass). Dropped-`AngularSourceSink`-wrap bit-id VERIFIED at storage (`from_mesh` by-ref + `_unwrap_source`→`.values`). `transport_sweep` retained for the 1 operator-free caller (needs `from_isotropic` `/W`). Plain-property delegation to streaming-leaf cached_property = correct SSOT (caching forward would re-mint the coincidence). Spy migration `transport_sweep`→`patch.object(CumprodScan,"sweep")` = false-green-after-relocation class. Helper promotion = unify-after-two. Grep gate: the rep-construction inventory must reduce to {cached_property door, retained free entry}.
- [S5.2 ScanMarch end-to-end gates](s5_2_scanmarch_end_to_end_gates.md) — PASS clean. The CONSUMER-side proof of S6.5: one `patch.object(lr,"default_for")` forces all 3 doors (verified call-time module-attr resolution at each). Context-manager-not-fixture forcing (reference leg unforced; fixture→scan-march≡self vacuous). Non-vacuity spy immune to window interception (ScanMarch._sweep_interior ≠ window's). RULING: independent-witness config duplication vs a frozen/hash-pinned reference is a PRINCIPLED Pattern-2 exception (sharing→silent co-movement). G4.b config-fix = anti-pattern-#17 textbook (guard worked, config changed, not threshold loosened).
- [S6.9 Fork-B2 default flip](s6_9_fork_b2_default_flip.md) — PASS-with-conditions. NO retire (user: peers ARE the point); window→ScanMarch multi-D default via REGISTRY REORDER (sole `default_for` door, `__post_init__` guard, zero fresh prod `MovingFrontierWindow(` constructions). Inverted-forcing `not is_1d` = redundant-but-defensive (stops illegal `window(1-D)` if registry breaks). 4× 2-D golden regen = G5 textbook (history block + output-identity evidence + 1-D blast-pin). CONCERN: stale-default DOC cluster (~9 test + ≥5 prod docstrings assert "2-D prod drives MovingFrontierWindow.loss_action"); sharpest = `operator.py:1403` apply-docstring TWIN of the FIXED `:1538` cached_property selection line (one fixed, twin survived same file). DISCRIMINATOR: ref to what 2-D *production* drives = STALE; ref to explicit window-vs-oracle equiv test = STILL FINE (window is a live peer).
- [C4 FaceLabel + bc dict](c4_facelabel_bc_dict.md) — PASS clean (#220, the N-D campaign BOUNDARY tail; sibling to C3.6 the SWEEP tail). HEADLINE = d=3 reflection-partner CORRECTNESS bug killed (`"y" if face in ymin/ymax else "x"` hand-list → `AXIS_NAMES[label.axis_index]`). SSOT `face_labels→FaceLabel.face_name→{boundary_face_layout, bc}` single-sourced; 2 pre-C4 "outer"→"max" sites GONE, `_ENDPOINT_SUFFIX` sole. `_OUTWARD_ENDPOINTS` is a NON-twin (outflow-sign layer, don't unify in C5). Pattern-4 pole-as-absence FULLY realized (0 `bc[...] is None`, detection = `"xmin" in layout.faces`). face_name ValueError = principled boundary (override only in tests; constructor-unrepresentable is C5). Plain-dict+KeyError CORRECT (siblings `_trace`/`_streaming_axes` are plain attrs — no MappingProxyType). Retirement COMPLETE (`mesh.bc_*`=input OK; `sn_mesh.bc_*`=retired). Tests EXEMPLARY: 2-clause inventory pin (together-AND-apart), strengthened y-placeholder migration w/ HISTORY note.
- [C5.1 axis-primary SNMesh](c5_1_axis_primary_snmesh.md) — PASS-with-nits (#225, the keystone before 3-D). Two ctor surfaces (`__init__` legacy-adapter / `from_axes` via `cls.__new__`) funnel into ONE `_init_core`; axes stored VERBATIM (round-trip GONE → custom-label fail-loud Pattern-4 win). SSOT = `self.mat_map` (legacy `mesh.mat_ids` = inbound provenance). NITS: (1) mat_map shape validated TWICE on from_axes (legacy_mesh_from_axes + _init_core; the adapter copy dissolves C5.2-5); (2) AxisCoord↔CoordSystem map in 3 sites (coord_system primitive + 2 adapter dirs; defer); (3) test mat_map-swap over-claim. `__new__` CORRECT (SNMesh is plain class). Tests exemplary.
- [C5.2 phantom-shim retirement](c5_2_phantom_shim_retirement.md) — PASS-with-nits (#225, sibling to C5.1). Retire LIARS+dupes keep HONEST sugar: `ny`/`dy` (d=1 phantom #214 class) + `dx` (dup of axis_widths[0]) RETIRED; `nx` survives (spatial_shape[0] sugar, 23 prod 1-D reads). `_axis_widths→public axis_widths` (instance-attr like siblings). DIVIDEND: retirement let `test_unified_sweep_dispatch` DELETE a Σ_t twin branch → `np.ones((ng,*spatial))`. 3 phantom-pins rank-honest (krylov `np.prod(spatial_shape)` MODEL; sweep-regression keeps `Mesh2D.dx/.dy` DATACLASS oracle ≠ retired SNMesh.dx; field props→retirement negatives). volume_measure pure-delegation HONEST (d≥3 arm explicit-deferred C5.5, removes .mesh reach-through from keff). NITS: (1) CONCERN 6 transport docstrings name RETIRED `mesh.ny` in from_mesh prose (predate commit, this commit makes stale; prod CLEAN; literal-attr-ref=fix, layout-name-placeholder=leave); (2) cosmetic nx-sugar/spatial_shape[1] mix in one loop nest. Idiom catalogue: `nx,ny=X.spatial_shape` is GOOD (d=2 self-assert).
- [C5.3 geometry-blind trace](c5_3_geometry_blind_trace.md) — PASS-with-nits (#225, the C5 trace/boundary tail). AXIS_NAMES RELOCATED `sn.axis→numerics.face_layout` (1 def, re-export = NO twin-import; face_layout = stdlib+numpy-only bottom, no cycle); `_FACE_NORMALS` 4-hand-list→AXIS_NAMES comprehension (+zmin/zmax; old list silently lacked z = d=3 blocker; sha256 byte-id structural). `_build_omega_dot_n` mesh param DEAD-gate retired (`from_mesh_and_quadrature→from_quadrature_and_layout`); curvilinear-Mesh2D unreachable VERIFIED via axes_from_legacy_mesh `NotImplementedError match="non-Cartesian"` at SNMesh ctor. trace UNCONDITIONAL, `SNMesh.trace` always-non-None. Test migration FAITHFUL: cyl-raises retired-with-gate → refusal-pin at construction surface; "zmin"→"wmin"; C5-G9 HAND-MIRROR expected dict (mirror-not-import RIGHT); G11 swap-detector + non-vacuity guard. `a9a0faf` CLOSES my C5.2 CONCERN 1 (6 `mesh.nx,mesh.ny`→`*mesh.spatial_shape`, discrimination held). NITS: (1) CONCERN forward — `_quadrature_axis` `getattr mu_z` zero-fallback = silent all-tangential on rank-mismatch; can't misfire today, flag for 3-D admission to fail-loud; (2) out-of-scope ARCHIVIST — stale `InflowTraceSpace.from_mesh_and_quadrature` rst refs (retired class, predates this commit).
- [C5.4/5.5/5.6 d=3 admission](c5_4_5_6_d3_admission.md) — PASS-with-nits (#225, the 3-D Cartesian admission; d≥3 constructs+solves mesh-LESS via from_axes, NO Mesh3D). HEADLINE FINDING (CONCERN, latent): the C5.3-carry fail-loud guard in `_build_omega_dot_n` is DEAD CODE — `mu_z` is an always-present @property (zero-fallback via `axis_cosines`), so `getattr(q,"mu_z",None) is None` NEVER fires; VERIFIED the named "z-face on 2-D quad" rank-mismatch passes straight through zero-padded; sibling AngularAverageOperator already hit this. Not-active today (prod d=3 = genuine 3-cosine LS); fix = discriminate all-zero-cosines not `is None` + add a test. C5.4 PASS-clean (gates off `reduced` proxy → genuine `is_cartesian/ndim`; 1-D sweep-cache `reduced is not None` KEPT = data-keyed not dim-keyed, correct). C5.5 nits: `isinstance(RadialAxisMesh)` BC-WRITER reaches behind the polymorphic `ax.bc` reader (1 site, defer to `Axis1D.with_default_bc`); `mat_map`+legacy-mesh raise = least-bad illegal-state-representable (no Mesh3D to hang it on). C5.6 CONCERN = stale "d=3 unconstructible / when Mesh3D lands" doc cluster the SAME commit refuted (test_sweep_schedule_nd.py header sharpest; default_for + ScanMarch docstrings; axis.py:685 message). Tests EXEMPLARY (k_inf 3D≡2D≡1D structurally-independent; Mode-9 degenerate-breaking box). Gates 26+21 green.
- [C3.6 honest d-dispatch](c3_6_honest_d_dispatch.md) — PASS-with-conditions (#220, the N-D campaign TAIL). HEADLINE = `_sweep_jacobi` twin-KILL: 3 multi-D `sweep` doors inlining `_sweep_scheduled(...jacobi...,reflect=None)` → ONE policy door (each supplies `interior` kernel only; `interior=` REQUIRED preserves S6.5). `ScanMarch.supports` narrow `is_1d or (is_cartesian and ndim==2)` = "select narrow" HONEST INVERSE of S3 widen (kernels unpack d=2; d≥3 falls through to FullFieldWavefront spine, pinned `TestD3SupportsMatrix` duck-types). `AXIS_NAMES` SSOT closes d=3 ERR-056 EXISTENCE hole (z-face never shed). streaming(axis) tuple = bit-id-by-construction (`mu_x` IS `axis_cosines(0)` view, G-b3 EXACT array_equal). `_FACE_OF` mirror-not-import RIGHT (nd test imports oracle → test side single-sourced). Duck SimpleNamespace OK (no Mesh3D; Pattern-6 defer). ONE CONCERN: `loss_representations.rst:760` "deferred until 3-D **quadrature**" STALE (2 siblings already fixed to **Mesh3D**; quad is the NON-blocker, already 3-cosine). DISCRIMINATOR for C3/C5 docs: d=3-blocker ref "3-D quadrature"=STALE, "Mesh3D"=correct.
- [Nexus workspace resolution](nexus_workspace_resolution.md) — SIBLING repo sphinxcontrib-nexus: _switch_workspace single-source PASS; KnowledgeGraph(graph) wrap + edge-key continuation; resolve_checkout_root 3-form order; _auto_align dict-shape re-probe nit (typed-result deferred Pattern 6).
- [Nexus runtime overlay](nexus_runtime_overlay.md) — SIBLING repo #26 dynamic exec-flow overlay, BOTH reviews. P1+2 PASS-with-fixes (sidecar/pure-wrapper/resolve_node-decorator-window/runtime_edges-mode-factoring all CORRECT; the flagged missing `node_id in self._g` guard was SINCE FIXED). P3 CONCERNS-no-blockers: RuntimeRun 4-family flat NOT tagged-union = RIGHT (orthogonal capabilities); kind→ingester dict + filter-before-limit + merge intersection-of-missing-arcs CORRECT. Conditions: SHOULD-FIX (1) `_load_runs`/`_runtime_load_many` plumbing-twin → extract `load_and_merge`, (2) viztracer depth end-time stack mis-nests zero-dur/shared-end children; CONCERN (3) `_is_accessor` ≤2-line threshold drops genuine 2-line dispatchers, (4) self-disproving "exactly one family" docstring.
