---
name: issue-206-phase-c-1d-matvec-relocation
description: #206 Phase C 1-D (L+C) matvec→_OneDimScanWalk PASS-WITH-NITS; emit_angular emit-policy OK, sigma_t asymmetry principled, phantom ψ-cache narrative + stale NIE message
metadata:
  type: project
---

#206 Phase C (`refactor/sn-cellupdate-seam-slab`, commits `eaafbe1`→`7300f3e`→`acbcddc`→`c1edaf7`, base `03d5c02`): the 1-D SN `(L+C)` matvec relocated OFF `_MSpatialOperatorSum._compute_LpC`/`_compute_LpC_transpose`/`_compute_decomposition` (operator.py) INTO the shared 1-D frame `_OneDimScanWalk` (loss_representation.py). The 1-D analogue of the 2-D S6.3b `_OctantWalk` apply-frame carve. **VERDICT: PASS-WITH-NITS** (2 in-scope doc/message NITs; structure is clean, merge on structure alone).

**Why:** proactive elegance trigger — operator↔loss_representation boundary crossing (normal↔adjoint, single↔dual emission). Carve goal = Cardinal Rule 2: the 1-D `(L+C)` walk (sweep, fwd matvec, transpose, decomposition split) has ONE source so L21 "matvec ≡ sweep" is a code fact.

**How to apply (the four standing rulings — reuse these for the next phased SN carve):**

1. **`emit_angular: bool` on a private apply-walk = ACCEPTABLE emit-policy fork, NOT Smell #3.** Discriminator: the FORBIDDEN axis is the solve/apply tripwire (`is_solve`/`is_apply`/`is_matvec`, AST-gated by `test_one_octant_walk.py:84` + `test_unified_sweep_dispatch.py:425`). Both `emit_angular` arms are the APPLY direction with identical spatial recurrence → not a twin branch. The project's OWN 2-D tripwire test docstring sanctions "a KERNEL OBJECT **+ EMIT policy**" fork (`test_one_octant_walk.py:85`). Perf-load-bearing (always-emit-m_ang = measured +14-19% on the Krylov hot path that discards m_ang). Per-cell store driven by buffer-presence (`out_ang_g_first is not None`) not the bool → bool only sizes the alloc once. Alternatives all worse: separate pass DUPLICATES level/cell iteration; two-methods re-splits the unified walk; injected-emit = Pattern-6 ceremony at 1 call site. The `if m_ang_cell is None: raise AssertionError` (loss_representation.py:1856) is a clean -O-safe illegal-states guard. **This is the same emit-granularity precedent as `moment_projection: ...|None` and `reflect=None`.**

2. **The `operator: StreamingOperator` (protocol surface) vs `sigma_t: np.ndarray` (private helper) asymmetry is PRINCIPLED.** `loss_action`/`loss_action_transpose` ARE the public `LossRepresentation` Protocol (loss_representation.py:212/230) → honor the operator signature. `loss_action_decomposed`/`_apply_walk` are private, only caller is `_MSpatialOperatorSum._compute_decomposition` which holds `.sigma_t` but NO StreamingOperator handle. Passing sigma_t directly AVOIDS manufacturing a fake operator just to read one array — duck-typing the array-holder as an operator would be the anti-pattern, not the cure. Asymmetry tracks the protocol-vs-internal boundary; correct.

3. **The transpose staying a SEPARATE relocated body (not folded into `_apply_walk`) is CORRECT — folding would be unify-over-the-difference.** Forward = fwd-substitution (`out=2ψ̄−in` via `outgoing_face_from_average`); transpose = reverse-substitution (`reversed(cells)`, `psi_bar+=2·f_bar; f_bar=−f_bar`, + `closure.angular_adjoint` SECOND triangular factor + swapped boundary block). Genuinely different walks. What MUST be shared IS: the ψ-independent `cell_balance_for_streaming` denom (no twin denom). Same `_CellSolve`/`_CellResidual` asymmetry the 2-D family already accepts.

4. **L21 "one walk" is a DEFENSIBLE MILESTONE, not an incomplete twin.** The carve claims the matvec FAMILY (fwd+transpose+decomp) is one source AND co-located with the sweep sharing the diamond-closure seam (`cell_average_from_faces`/`outgoing_face_from_average`, cell_update.py:710/730, Phase A) + the denom primitive (`cell_balance_for_streaming` ≡ `affine_scan_coefficients` denom). It does NOT claim sweep-iteration ≡ apply-iteration — they differ (vectorized cumprod `ordinate_scan` scan vs explicit per-cell loop) BECAUSE solve (recover ψ̄ via fwd-sub) ≠ apply (form residual from known ψ). Co-locating so they can't drift on closure/denom is SUFFICIENT — that's where L21 drift (Phase F manifestation #7) actually lived. The deeper "fork sweep+apply at a shared cell-kernel" is correctly DEFERRED as the optional denom-fold (would be nULP not bit-id → own commit + own pin). **Do NOT flag the deferred deep-unification as a gap — flagging it pushes toward the unify-over-the-difference trap.**

**Bit-identity & pre-existing-reds: VERIFIED, not taken on faith.** Re-ran strict gate (`test_affine_carve_bit_identity` + `test_affine_carve_baseline`) → 9 pass 0 ULP under `-W error::DriftWarning`. The 2 `TestT4cPreT4RegressionSnapshotCurvilinear::test_sphere_{1g,2g}_apply_bit_identical` reds = PRE-EXISTING (#195): created a throwaway worktree at base `03d5c02`, they fail IDENTICALLY there. Carve did not introduce them. (Technique worth reusing: `git worktree add /tmp/... <base>` to confirm a pre-existing-red claim on a path the carve touched.)

**The 2 in-scope NITs (approval conditions):**
- **NIT-1 (CONCERN, carve-authored):** `loss_representation.py:2211` — the relocated transpose's `NotImplementedError` MESSAGE STRING still says `"_compute_LpC_transpose: the multi-D Cartesian adjoint is deferred..."`, naming the method `c1edaf7` DELETED. `git log -S` confirms `7300f3e` authored the line. The FORWARD twin's guard message WAS updated (`:1934` says `_OneDimScanWalk._apply_walk`). Classic "edit lands on one-not-the-other" — fix one twin's diagnostic, not the other's. A user who hits it greps a dead symbol. Fix → name `_OneDimScanWalk.loss_action_transpose`.
- **NIT-2 (CONCERN):** the PHANTOM "ψ-keyed cache" narrative survives ~5 sites in operator.py the carve touched: `:386-394` (`_compute_decomposition` docstring), `:475-477` (`_MSpatialOperatorSum.apply`), `:583-585` (`AngularRedistributionOperator.apply`), comments `:156-161`/`:960-961`. Commit `acbcddc`'s OWN message admits the cache "was aspirational — grep confirms no cache code". VERIFIED: `_compute_decomposition` (:416) is a bare delegation, no memoization; production `StreamingOperator.apply` (:829) routes via `loss_representation.loss_action` (emit_angular=False), NEVER touches `_compute_decomposition`. So the docstring describes a non-existent cache AND a non-production "hot path". `AngularRedistributionOperator.apply` (:606) calls `_compute_decomposition` a SECOND time (full re-walk) while the docstring claims a cache hit. Sphinx-is-the-brain failure: a future optimizer builds on a false premise. The carve rewrote these methods and reinforced the false narrative — missed retirement of a claim its own commit flagged. Strike all 5; state the true single-walk-per-call mechanism.

**Out-of-scope (do NOT block):** dead `diag_p42_adjoint_oracle.py` refs in `psi_half_angle_seed.py:349,699` + `pole_angular_closure.py:954` (file gone, but those 3 files are OUTSIDE the Phase C diff; carve correctly cleaned the refs in the 2 files it touched). Archivist sweep.

**ARCHITECTURAL OPPORTUNITY (tracked-question, not this carve):** after Phase C the only `_compute_decomposition` consumers are the 2 leaf `.apply` slow paths + `_SpatialSweepDirection` slow path — NONE on the production hot path (which uses fused `loss_action`). Worth a `module:sn` issue: are `M_spatial`/`M_angular_redist` as separately-applicable leaves still earning their keep, or a fuller-view to retire down to the decomposed-emit helper? Pin = `TestT4b/T4c` decomposition invariant if they stay.

Related: [[a1_diamond_face_closure_seam]] (Phase A1 — the seam this carve consumes), [[scanmarch_2d_diamond_closure_routing]] (the 2-D sibling), [[b_one_dim_scan_walk_frame]] (Phase B — extracted the `_OneDimScanWalk` frame this carve attaches the matvec to), [[s6_4_e_walk_levelop_collapse]] (the `_CellSolve`/`_CellResidual` apply/solve asymmetry precedent).
