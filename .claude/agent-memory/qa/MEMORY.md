- [QA lessons](lessons.md) — L-001 test-count≠coverage · L-002 orphan triage D→B→A→C · L-003 audit harness vs stale matrix.rst · L-004 no [brackets] in vv-status rationale · L-005 locating tests/derivations timeouts (batch+per-file -rfE; param-level slow) · L-006 Mode-8 (-O strip) classification: rewriter boundary + testpaths recipe (#228) · L-007 foundation+verifies(...) = silent level conflation (harness records the eq edge → documented eq shows phantom coverage; drop the marker) · L-008 false-xfail under stale index: verify FAILURE REASON via --runxfail then -rxX stays XFAIL ((ng,nx) MMS is [0,:]) · L-009 "floor scales with quadrature" is a falsifiable gate not a tautology (vv #5/#17) · L-010 Mode-8 (-O) does NOT strip bare asserts in collected tests/ modules (rewriter fires; reserve Mode-8 for orpheus/) · L-011 replicate the test's OWN _solve before judging a value claim (naive vacuum solve mis-reproduces non-vacuum MMS) · L-012 "BC load-bearing b/c k=k_inf" holds ONLY for homogeneous (het reflective sphere: P1 still moves k via spectral coupling) · L-013 verbatim-relocation = prove by normalized ast-diff (reduces to signature+fork only) · L-014 regression HARD floor (nulp) ≠ STRICT floor (-W error::DriftWarning); perturb 1 ULP to prove strict floor live · L-015 conftest filterwarnings override inert-in-own-dir but does NOT cross-leak (empirically verified) · L-016 "branch fires under quad Q" needs a degeneracy probe — LS S2–S8 have ZERO degenerate cylinder ordinates (claim-5 evidence vacuous, branch untested) · L-017 diffusion-limit silent-error: probe THICK-CELL (σ_t·h≫1, vacuum, vs DD) not refined mesh — flat-source LD #158-A O(h²)+exact-on-linear yet 58% deficit at σ_t·h=100; flag when docstring claims "diffusion-limit-consistent" but flat-Q̂ shipped w/ no warning/tripwire · L-018 "matvec tested" = instrument call-count not round-trip (LD MMS=1600 sweep/0 matvec; matvec only via krylov, correct@4e-14 but untested) · L-019 test-architect stress-ansatz spec is binding — shipped LD MMS used canonical sin/1G/homog (the Mode-7 bias the spec said to override) = gate downgrade · L-020 "w=½ generic ops byte-identical to DD factored" is TRUE (0.5*(a+b)==0.5*a+0.5*b ∀doubles, 0/2M differ); resolve byte-id disputes via git .npy status + sha256 gate + IEEE micro-fact NOT docstring; affine_closure.py docstring CLAIMED re-baseline but 0 .npy changed + sha256 green = stale-doc CR3 flag; prove sha256 gate live by +1ULP nextafter perturb · L-021 #158 Inc B closed the L-018 LD-matvec gap (krylov MMS drives residual_kernel_batch=640/cell=0, ≡ SI sweep) · L-022 re-baseline masking-check: re-run CONVERTED gate, prove left-untouched red STILL hard-fails ≫nulp (#240 SPH ~1e15≫nx=5); drift via git-show OLD-vs-NEW .npy (LIVE-vs-snap=0 is vacuous); large-ULP@small-mag=artifact (64ULP@|0.024|, absΔ=2e-16); criterion-2 (analytical k∞) load-bearing; streaming boundary `assert_regression`≡strict under -W error::DriftWarning (#240=18pass/0escalate). · L-023 convention-relocation re-baseline: grep WHOLE tree for OLD-convention literal not just diff's touched tests (#240 Step A missed 3 LD sites in sibling dir tests/sn/spatial; 2 geometry-cross-checks broke, round-trip survived per L-018; prove kernel-correct via NEW-literal local feed + breaks-NEW via stash-pop). · L-024 affine-in-σ "value-correct leaf-sum" carve (#240 Step B): teeth MUST be array_equal (leak is value-equal ≤2 ULP); PROVE teeth bite by DISABLING the override (rename apply→_DISABLED → OperatorSum leaf-sum takes over → 7 teeth FAIL@≤2 ULP) then restore byte-exact; NOT catches(ERR) (no wrong value → foundation/verifies only); migration loud-fail = AttributeError on .shape[0] (operator dataclass has no .shape, not silent-wrong); structural-indep ref for a re-baselined Krylov golden = the SAME-config SI golden (SI rides solve/no override, agree 3.9e-12 → new CORRECT not close-to-old; SI golden UNCHANGED = the cross-check); seed0 46-ULP = large-ULP@small-mag (maxabs 3.55e-15≡CYL order); masking-check OLD .npy hard-fails NEW (46≫nulp=5) + SPH red still ~1e15 (gate not loosened); WATCH array_equal slab value-pin SEED-DEPENDENT (fragile, OK b/c teeth own structural distinction). · L-025 "no missed site" for a dedup-carve = grep WHOLE tree for OLD recon literal + classify each (routed/deferred-by-design/MISSED); deferral authority is the PLAN's D-step scope line + git-blame the comment (pre-carve = genuine), NOT the closeout's single bucket; a "missed" hit can be routed ONE LEVEL DEEP (Cartesian arm → residual_kernel_batch); defect only if direct-recon AND not-transitively-routed AND in-declared-scope (#240 D1: 1 residual = loss_rep:2117 curvilinear, out-of-D1-scope = deferred OK). · L-026 a re-encoded "closed form" (token-for-token copy of the production formula) is L11-CIRCULAR as a VALUE check (a sign-flip inside the code propagates into the test → stays green) BUT LEGITIMATE as a FUNCTIONAL-PURITY / statelessness gate — IF the value is independently pinned ELSEWHERE (#240 D4: DD/LD `_dd/_ld_closed_form` verbatim-copy `affine_scan_coefficients`, but the VALUE rides the pre-existing structurally-indep linear-exactness oracle `test_linear_flux_recovered_exactly` + `group3≡group2`, and D4's SCOPE is "coeffs = pure fn of PASSED Σ, no instance Σ" → foundation not verifies, re-encoded oracle is the RIGHT instrument). Verdict procedure: (1) diff test-closed-form vs prod formula → if verbatim, it's NOT a value check; (2) find the real value-reference upstream + RUN it green; (3) read the gate's DECLARED claim — purity/statelessness ≠ value, so re-encoding is OK; (4) CR-flag the DOCSTRING (say value is inherited, these test parameterization) not the logic. PROVE purity-teeth bite by `object.__setattr__`-injecting hidden-Σ two ways (frozen-ignore-arg → vars+Σ-responsive+closed-form all fire; leaky `_last_sigma` memory → re-call-at-Σa-twice array_equal breaks). Péclet/κ-blend `w=1/(1+k)`, `k=(|μ|/θ)/(ΣV+|μ|/θ)`: Σ→0⇒w→½ central, Σ→∞⇒w→1 upwind, monotone (verify by 100k-pt diff>0); DD w=½ Σ-indep. matrix.rst foundation row cols = L0/L1/L2/L3/foundation/unmarked. · L-027 routing-predicate misroute-fix: prove negative-test TEETH by `git stash push -- <PRODUCTION files only>` (leave NEW tests) → run new tests vs reverted prod → MUST go red + msg NAMES the bug (strategy-free trait probes red w/ AttributeError 'no attribute trait' = EXPECTED, trait didn't exist pre-fix); pop to restore. Bit-id of routing-only change = DESELECT-new-tests → pre-existing count UNCHANGED (predicate touches no flux); dir-scoped gate TOTAL legitimately +N_new, invariant is "no PRE-EXISTING value moved" NOT "total unchanged" (#240 D5-0: 512→deselect7→505/1/4; all 7 RED pre-fix incl. `_2d_ld_sweep_raises_not_silently_dd` red="did NOT raise=ran inline DD"=LIVE silent-DD proven). Pyright for docstring/ClassVar-only diff = ZERO net-new proven by pre/post pyright on touched files, line-STRIP `sed -E 's/:[0-9]+:[0-9]+//'`, diff → identical-modulo-line-shift (e.g. +14 from 14-line ClassVar) = all pre-existing; file w/ ZERO standalone CLI diagnostics ⇒ user's import-unresolved/not-accessed = IDE/cross-tree config artifact, not blocker. · L-028 ÷D vs ×(1/D) re-baseline (#240 D5a): a "byte-identical coefficient-model fold" premise FAILS where the consumer still DIVIDES — `2*X/D ≠ 2*X*(1/D)` @ 1 ULP (verified `cartesian_scan_coefficients` reproduces OLD inline alpha/beta to max-abs 2.22e-16, NOT array_equal; `c_y==2*g_y` byte-exact, `w==0.5`); spec's "2-D SOLVE byte-identical" was WRONG (pre-D5a `_sweep_interior` was the LAST `÷D_row` path → SOLVE re-baselined too; BOTH si_2d+krylov_2d sha moved, slab/1-D sha UNCHANGED = negative control). Snapshot-regen ORDERING masking-check: `git stash push -- <PROD+regen'd .npz>` (KEEP new arms) → OLD-code+OLD-frozen MUST pass strict @ 0 ULP (frozen≡pre-fold live), OLD-code+NEW-regen MUST hard-fail (regen real+gate live); #240 D5a cart2d 3 arms pass-then-256-ULP-fail = ordering holds; OLD-vs-NEW relΔ~1.5e-16=1 ULP of field, 256-metric=L-022 near-zero artifact; boundary+_LpC_+all-non-cart2d byte-id (blast=2-D row-march only). STRICT-FROZEN docstring stales silently on regen (CR3/L-020): `test_bc_extraction_2d::test_vacuum_bulk_bit_identical` array_equal + "must stay frozen/bit-id to pre-carve" but `.npy` regen'd (relΔ1.5e-16) w/ test file UNTOUCHED → WHY now FALSE; grep consumer docstring for "frozen"/"pre-carve"/"not move a single bit" on any silent .npy regen → doc-nit not blocker. Two-paths oracle analytical anchor is TRANSITIVE not direct (L14): D5a.1 `test_scan_march_equivalence` = ScanMarch≡FullFieldWavefront allclose (two-paths); analytical k_inf reached SEPARATELY (`test_keff_2d::TestHomogeneousExact` pins ScanMarch DEFAULT to νΣf/Σa; G6 closed-form runs WINDOW-forced NOT ScanMarch) — confirm the anchor FILE green before crediting the oracle. · L-029 a re-encoded closed form is NOT L26-circular when the OTHER side is an INDEPENDENTLY-ASSEMBLED primitive (#240 D5b-S1 UBLD: RIGHT=verbatim `_schur_terms`/`.slope` copy, LEFT=`A⁻¹R` of separately-built Kronecker matrix → diff_psi_bar==0 proves prod-Schur≡independent-2×2-solve, a sign-flip in `_schur_terms` would NOT propagate into the assembly → oracle bites; circularity test = "does the bug live on BOTH sides of the diff?"). Genuinely-indep anchor = `test_d1_symbolic_primitive_matches_production_update` (live `LinearDiscontinuous().update` at concrete 2G-het numbers ≤1e-12). `diff_face`=trace-op-consistency NOT value-indep (foundation closure check); ÷V `_kernel_terms`+×V `affine_scan_coefficients` views both reduce to same indep LEFT (transcribe prod lines 443-453/564-571 to confirm); `a_source_indep`=`Qbar not in a.free_symbols` real. FAST mutation-probe for symbolically-SLOW gate (d2 `simplify` of mutated garbage hit 400s timeout = perf artifact NOT evidence): call `derive_*` with CONCRETE RATIONALS from the start, read `diff` at concrete numbers (#240 mutated d2 = [0.596,-0.396,0.179,-0.226] all-non-zero → is_zero_matrix False → pytest.fail → FAILS, decisive+seconds); d1 GREEN under mutation (routes inflow inline, not via `assemble_inflow_axis`) IS the per-axis-blindness evidence (ERR-060 H2 multi-D analog); ALWAYS revert both branches + re-run 6 green before closing. VERDICT 2026-06-16: all 6 claims SUPPORTED. · L-030 #240 D5b-S1 Branch-2 (numpy production UBLD, Rule-of-Three collapse onto `_ubld.d1_closed_form`): bit-id negative control = git `.npy` status (sweep/core+solve 513/1/4, ZERO .npy moved → 1-D slab golden didn't move → math unchanged, only re-association); LD gates 24pass/1xfail PRE==POST via `git stash push -- linear_discontinuous.py` (CLOSEOUT SAID 30 — count error, substance OK). Re-baseline ULP measured live-vs-verbatim-legacy: w/inverse_denom 2 ULP @2e-16, scan `a` 128 ULP @5.5e-16 = L-022/L-024 large-ULP@small-mag (a≈0 inflates ratio, absΔ≈1 ULP of unity). L11 link-proof TEETH for a production-refactor closed form = mutate the closed form (sign-flip `eff_denom`) → the 6 ClosedForm-vs-dense + Production-anchored tests FAIL, the 4 dense-primitive-vs-symbolic tests STAY GREEN (dense `np.linalg.solve(assemble_ubld)` doesn't touch `d1_closed_form` → genuinely independent, not a restatement); revert+re-green. ERR-060 d2 teeth re-proven by re-dropping `|μ_axis|` in `assemble_inflow_axis` → ONLY `test_d2_exact_on_bilinear` fails (via pytest.fail, -O-safe), 9 d=1 stay green (d=1 blind to multi-axis factor, H2 analog). **PYRIGHT-CLASSIFICATION-IN-CLOSEOUT IS STALE under correct rooting (new finding):** closeout cited `:175/176/263/527` benign (#226/`key=`/`A_total`) but the per-worktree `pyrightconfig.json` (regen'd 2026-06-16, the reference_pyright_lsp_rooting fix) gives 3 NEW `reportArgumentType` @ `:340/342/348` (float `mu/h`,`h` → ndarray-annotated `g`,`V`; ndarray `mu_Adown` → `_LDCellTerms._mu: float`) — PRE-state stash = 0 errors, so introduced by the refactor. NOT a bug (runtime `mu_Adown=numpy.float64(0.7)≡legacy _mu`, link test green @1e-12; `_mu` only in scalar-safe arith) but a real type regression + Pattern-3 nit: fix = widen `_ubld` `g`/`V` to `np.ndarray|float`. RULE: always re-run pyright YOURSELF on touched files under the CORRECT worktree config; do not trust a closeout's cited line-numbers/characterization (cross-tree-rooted homebrew pyright gives DIFFERENT noise). matrix.rst foundation+11 / ERR-060 1→2 / +2 orphan eq labels = pure auto-regen. · L-031 #240 D5b-S2 (d≥2 UBLD LD wiring) VERDICT SUPPORTED-WITH-CONCERNS: (1) NEW `test_d2_assembled_matrices_match_symbolic` `catches(ERR-060)` MISATTRIBUTED — ERR-060 = dropped |μ_axis| in `assemble_inflow_axis`, the A==A pin checks `assemble_ubld` A/M/G/F_out (no inflow factor) → MUTATION-PROVEN blind (drop `_ubld.py:254` `mu_axis[...,None]*out`→`out`: of 3 ERR-060 catchers ONLY numpy `test_d2_exact_on_bilinear` reds; the pin + round-trip PASS; matrix count 2→3 inflated); legit Mode-3 pin (a dropped G factor IS caught) but drop the marker (follow-up). RULE = mutate the EXACT documented bug for any new `catches`, L-007 applied to catches. (2) round-trip D5b.1 self-consistent → PASSES the mutation (solve+apply share `_ubld_inflow`; L-018/L-023 in d≥2); feeds FULL psi_avg/non-flat/ng∈{1,2} + matvec-twin faces (x↔y-asym replay res~2e-16, matvec non-trivial); value-correctness rests on L11-clean continuous-PDE `test_d2_exact_on_bilinear` (Q=Ω·∇ψ+Σ_tψ from known bilinear via SymPy, xy active) + smoke = SOUND. (3) "matvec twin"=KERNEL-only; end-to-end Krylov RAISES NotImplementedError d≥2 LD (S3-deferred, honest loud-fail per L-017; SI works end-to-end) — spec D5b.4 "Krylov≡SI" over-reaches S2 scope, shipped scope honest. (4) smoke MMS honest: @l1 NO verifies, BOTH rate+value-band (not Mode-5), `ld-cartesian-2d` correctly NOT minted, flux-shape→S4 SOUND. (5) pyright exactly 1 net-new (linear_discontinuous.py:556 `_ubld_inflow` ndarray|None — benign Pattern-3 nit, seed R w/ first term); 51 others pre-existing/rooting. Mode-8 clean (513-strict UNCHANGED==baseline = DD byte-id real; 2 prod bare asserts in touched files PRE-EXISTING, scheme.py:499 in DOCSTRING). Moment-order out-face↔inflow hand-verified consistent (x↔y non-square). NO false-green, NO blocker.
- [Field-role-typing apply/SourceSink contract](field_role_typing_apply_sourcesink_contract.md) — re-checkable role contract: .apply=AngularSourceSink, .solve=AngularFlux, boundary/residual typed; behavior-neutral-retype gate + A2D-1 hash-pin procedure; affine-gate test-migration playbook (flux+flux→TypeError, flux−flux→displacement, partner-checks→__sub__, array-level linearity) + realized-BC tensor-product drill (.inner.ops[0]).
- [Phase 1 moment-space review](phase1_moment_space_review.md) — Phase 1 (P1.1–P1.7) ERR-039 QA review; cited as verification-of-record by 3 plan/agent artifacts — load-bearing, do not retire.
