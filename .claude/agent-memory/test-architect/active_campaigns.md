---
name: active-campaigns
description: Full in-flight state for every open test-architect campaign — the detail MEMORY.md §2 points at (headlines, open findings, measured numbers). Git-true; reconcile "unmerged" claims against git before acting.
metadata:
  type: project
---

# Active / in-flight verification campaigns — full detail

## P4-remainder step 2 — the producer binds the angular axis (⏹ PLAN DELIVERED 2026-08-29)

**Deliverable**: `scratch/p4rem_step2_verification.md` — the CONTRACT-CHANGE
half only (AR sheds `quadrature`; closure ctor takes the axis third;
`_weight_of` retires). G5/G7 inherited from `scratch/cs5_verification_plan.md`
§3.5/§3.7 and NOT redesigned; this plan supplies the measured decoy catalogue
G7 needs and the landing rows G5 needs. Branch
`feature/p4rem-producer-binds-axis`, HEAD `1a1aa60f`, tree clean; ground memo
`scratch/p4rem_ground_measure.md` re-verified (only a docs commit past its
`7d1129cc`).

**The V1 sets, measured.** (a) AR field removal: **3** production ctors (all
inside `angular_redistribution`), **0** test ctors, **0** string-forms, **0**
reflection walkers, **7** `.quadrature` reads on an AR receiver (5 production +
2 test ORACLES), **1** doc `:attr:` that dies silently. (b) closure ctor: the
banked **8** test sites are `[M]` **10** — the extras are a SUBCLASS ctor
(`_MutantMM`, `tests/sn/operators/test_operator_feeds_the_walk.py:161`, i.e.
P4.9b's own keystone mutant factory) and a `AngularClosureBase.create(**kwargs)`
registry call (`test_angular_closure.py:129`); production prose is **6**, not 2.
(c) `_weight_of`: `[M]` **0** references in `tests/` and `docs/`; **2** in
`orpheus/` (the `def` + a body comment at `:536`), BOTH present-tense-false.

**The V3 verdict.** ⛔⛔ Three-way `is`-identity survives the change
(`axis.generator is angular.quadrature is sn.quad`) ⟹ every value gate is
`X == X` and **the route rows are the ONLY discriminators**; existing anchors
witness the LOUD modes (dropped `generator=`, wrong N) and the un-moved-object
claim, nothing else. The silent mode nobody had named is the **wrong LABEL**.

**Open findings the ruled design missed** (plan §7, 10 items): the CS5 decoy is
REFUSED by the α-dome guard; a **second mint site** on the d ≥ 2 branch where
no producer exists; the generator UNION cannot answer the reads (pyright); the
obvious oracle migration demotes ~10 committed comparisons; G7 cannot be a
separate step. Five open rulings at §8.

Detail + all measured numbers → `lessons_archive.md` **L66**.

## CS5 — an axis can name the generator that made it (⏹ PLAN DELIVERED 2026-08-29)

**Deliverable**: `scratch/cs5_verification_plan.md` (~920 lines; 9 gates G1–G9 +
P, per-gate homes/fixtures/exact assertions, a 3-arm measured mutation battery,
7 design risks, 5 open rulings, 5 refuted candidates). Phase §5.5 of
`.claude/plans/space_and_kernel_binding_campaign.md`; opener
`scratch/cs5_ground_measure.md`.

⚠ **The tree moved TWICE during the dispatch** — step 1 landed while §1–§9 were
being written, steps 2+3 landed while §0 was being written. The plan carries
both reconciliations in place (§0, §0.1) per `plan-authoring` §3; the
"NOT STARTED" rows are kept beside their dated repeal.

**Headlines** (full detail → `lessons_archive.md` L65):

- ⛔⛔ **The whole landed surface was UNGATED**: `[M]` a 3-arm in-process
  mutation battery over the 184-test anchor set found **0 genuine catchers** —
  `generator ∈ _identity_key` → 1 red that is a HARNESS ARTEFACT (the
  cross-process digest gate's subprocess leg is unmutated); mint `MODAL` → 0;
  drop the provenance upgrade → 0.
- ⭐⭐ **The identity-exclusion mutation is LOUDER than "the name changes"**:
  `[M]` `Axis.__eq__` **RAISES ValueError** and `hash(Axis)` **RAISES
  TypeError: unhashable type: 'Quadrature'` — `DiscreteMeasure` is
  `frozen=True, eq=True` over ndarrays and `Quadrature` is `frozen=False,
  eq=True` (⟹ `__hash__ = None`). The exclusion is STRUCTURALLY MANDATORY,
  not a taste ruling.
- ⭐⭐ **The re-point is a VALUE TAUTOLOGY** — `[M]` the generator **is**
  `sn_mesh.quad`, so every value gate over step 2 is `X == X` through a
  wrapper. The keystone must be a ROUTE gate: a **weight-preserving
  node-rolled decoy quadrature**, `[M]` space name and `==` IDENTICAL,
  `generator.mu_x` DIFFERENT ⟹ visible only through the generator.
- ⛔⛔ **R1 (caught pre-landing)**: `DiscreteMeasure.axis` mints `(n_points,)`
  by construction; `[M]` d=2 gives `spatial(12,)#3712…` vs the shipped
  `spatial(3, 4)#1dcb…`. Handled in-tree by an `isinstance(mesh, Mesh1D)`
  branch — which creates a **P4.9a-shaped blindness** (the generator path runs
  on 1-D carriers ONLY), so G6/G8-spatial were re-scoped to parametrize over
  the BRANCH with the d≥2 row asserting the honest `generator is None`.
- ⭐ **The §6c witness is stronger than the brief assumed**: `[M]` three SHIPPED
  production sites stay generator-less (homogeneous counting point, every
  `EnergyAxis`, the MODAL moment axis) — the refusal needs no manufactured input.
- ⭐ **The intrinsic law worth gating is the SECTION law**
  `a.generator.axis(a.label) == a` — `[M]` holds 4/4 angular rules, and it is
  exactly what fails at d≥2, so it is R1's standing gate.

**Open rulings**: Q1 refusal home (Axis vs consumer) · Q3 four names vs the full
reach-past set (`+N`, `+weights`) · Q4 the missing independent literal-volume
anchor · Q5 is the `Mesh1D` branch the CS2 seam (contract) or a shim (xfail)?

---

## P4.9b — the operator is posed with its two closures (⏹ PLAN DELIVERED 2026-08-28, PRE-carve)

**Deliverable**: `scratch/p4_9b_verification_plan.md` (~1100 lines; 12 findings,
4 §6c witnesses with the keystone measured red-today, an 11-arm battery whose M1
denominator is already TAKEN, 7 blocking rulings). At `10314dfa` on `main`,
post-P4.9a. Design record `scratch/p4_9b_design.md` §§7–8 (all ruled); migration
surface `scratch/p4_9b_row45_recount.md`.

**Headlines** (full detail → `lessons_archive.md` L64):

- ⭐⭐ **The keystone is a ROUTE gate and it is RED TODAY**: pose the operator,
  swap the hub's `scheme`/`pole_angular_closure` for a mutant subclass, drive
  `(L+C).solve` through the ALREADY-POSED operator. `[M]` rel **5.000e-02**
  (slab/scheme), **4.596e-02 / 5.313e-02 / 1.196e-01** (cyl fp(4,6), fp(4,8),
  sphere / closure). Three measured traps: `cell_contribution` alone is an
  INSUFFICIENT mutation (`array_equal=True` on all 3 curvilinear rows);
  `sweep_once` re-poses internally at `:814` so it is the wrong driver; a
  surviving cache masks the swap.
- ⛔⛔ **The design's "measured-cheap" table cost is REFUTED**: `[M]` 6 operators
  per slab solve × 8.78 ms on a 284.8 ms solve = **16.8 %** (24.65 % at 8),
  against **exactly 1** build today. The gate is a COUNT, not a wall clock.
- ⛔ **Step 2's §6b set is 79 sites, not ~28** — 22 `default_for` + **58 direct
  representation ctors in 11 test files**, all single-positional-arg.
- ⛔ **THREE mesh-attr memos, not one**, and `test_cache.py:295-340` (the ONLY
  witness of the two-stratum rebind contract) asserts on the retiring solver
  slots ⟹ it DIES and its re-pose owes a net-new staleness leg.
- ⛔ **21 of the 67 reads are SPACE facts the ruling puts hub-side** — ship the
  partition as an executable read-set gate, not as a count.
- ⭐ **Activation INVERTS vs P4.9a**: `[M]` 26 of 27 frozen artifacts construct
  an operator (nothing new must be built), but only 15 of 27 build the scan
  cache; and the step's two halves have DISJOINT activating geometries.
- ⛔ **The ruling's own hazard prose is refuted**: the wrong-family closure
  RAISES (sphere `TypeError`, cylinder `IndexError`), it is not silent. Ruling
  survives; the sentence must not reach the ctor docstring.
- **M1 denominator TAKEN** (frozen corpus, 27 tests): `m1_scheme` **20** reds /
  `m1_closure` **16**; 10 both · 10 scheme-only · 6 closure-only · 1 never-red
  (an arm-composition gap, not blindness). Compare PER ARM, never the union.

**Blocking rulings** Q1 memo lifetime (the 17 % cost) · Q2 the 58 ctors ·
Q3 does `sweep_once` take an `operator=` · Q4 is strategy selection
operator- or hub-side · Q5 the `spatial_closure`/`angular_closure` naming
asymmetry · Q6 `_pole_mirror_cache` · Q7 the permanent AST gate.

## CS4b — fields are space elements (⏹ PLAN DELIVERED 2026-08-21, PRE-carve)

**Deliverable**: `scratch/cs4b_verification_plan.md` (987 lines, 16 sections; 30+
gate specs, a 36-arm mutation battery with 4 MUST-STAY-GREEN anti-claims, 9
findings, 8 open rulings, a 7-step decomposition). Branch
`feature/cs1-energy-space` @ `34df88cb`. Design record
`.claude/plans/cs4b_fields_design.md` (rounds 0-2, F1/F2/F5 ruled).

**Headlines** (full detail → `lessons_archive.md` L62):

- ⛔⛔ **8 of 22 mesh-identity guard call sites redden NOTHING** over a measured
  3936-row denominator; two of the eight are the `apply_transpose` / second-
  `.solve`-arm TWINS of witnessed forward arms. F2 rewrites all 22.
- ⛔⛔ **F1-(A) is not a re-point, it installs a metric**: `[M]` `AngularFlux.l2`
  `4.9929 → 2.9593` (ratio 0.5927), moment family 0.8214. The CS3 gate
  `test_si_diagnostic_trajectory.py` predicts this red and assigns it to **CS2**;
  CS4b now owns it.
- ⭐⭐ **The two isotropic embeds are the ADJOINT and the SECTION**, exactly `Σw`
  apart (`R.H == Σw·E` bit-exactly; `⟨Rψ,φ⟩=⟨ψ,R.Hφ⟩` at nulp 1.0; `R∘E=id`
  bit-exact). Naming them apart kills the ERR-051 factor class by construction.
- ⛔ Migration surface **909 sites / 122 files**, not "632 / 86" (+121 composite
  `zeros(mesh=)`, +116 `.mesh` reads, +25 BC factories).
- ⛔ A **22nd** tolerant guard (`sn/solver.py:338`, unwitnessed) — promoted into
  `vv-principles` #28 as the temporal twin.
- ⭐ #399's red witnesses already exist (3 of 5 widened-moment members raise today).
- ⛔ LD arm works but flips `cone_violations` from ANSWERING to REFUSING.
- ⛔ `FullFieldSpace.__eq__` is BLOCK-BLIND ⟹ a composite `space ==` gate is
  designed-green.
- ⚠ **8 blocking/open rulings** — Q1 (`MomentResidual`), Q2 (re-point
  `full_field_space.interior`?), Q3 (scalar quadrature-blindness permission),
  Q4 (CS4b owns the CS3 re-derivation), Q5 (adjoint/section naming), Q6 (LD cone
  refusal), Q7 (`_StubMesh` seam), Q8 (battery amendment, `[M]` +175 rows/+2.90 s).

**Tree hygiene**: 22-arm mutation battery run with copy-aside outside the repo;
byte-`diff` clean after every arm and after a killed background run; the 14-path
battery re-reads **1436 / 1 / 15** baseline-exact.



`MEMORY.md` §2 carries ONE line per campaign; this file carries the rest.
**Merge-status: trust git, never a frozen claim here**
(`git merge-base --is-ancestor <hash> HEAD`). When a campaign merges, cut its
section: the lesson goes to [[lessons]], the architectural milestone to the
theory page's Development-history changelog.

- **CS1 — the Energy axis / axis-composed spaces (campaign 1, PRE-carve, dispatched 2026-08-20 @ HEAD `4e11731b`) — PLAN DELIVERED.** `scratch/cs1_verification_plan.md` (~1260 lines: 40 gates A1–A12 / B1–B11 / C1–C2 / D1–D12 / E1–E3 mapped to the five landing steps, a **23-mutation battery with 3 positive controls AND a MUST-STAY-GREEN column**, a byte-stability protocol with the payload `[M]` captured, 15 rejected alternatives, 7 open questions, and 12 findings against the design record). Design record `.claude/plans/cs1_energy_space_design.md`; campaign plan `.claude/plans/space_and_kernel_binding_campaign.md`. **Lessons → L59.** Baselines re-measured at `4e11731b`: `tests/sn/architecture` **105 passed / 21 xfailed 1.89 s**, `tests/homogeneous` **23 passed 2.40 s** (both match §P at `273d431a`). ⛔⛔ **The design's own `.H` CONTROL is a provable non-catcher** — `[M]` a one-cell mesh with `V=2` reads BIT-IDENTICAL to `w=1` and to the `None` path (`[-0.08, 0.2]`); a rank-1 point axis forces every metric SCALAR and a scalar `G` commutes with everything (vv#12, space-side dual). Only a per-GROUP `w=[2,5]` moves it (`[-0.38, 0.2]`), so the control must be a deliberately non-physical toy. ⟹ the quotient-vs-one-cell distinction is carried by SPACE IDENTITY alone (gate B9). ⛔⛔ **`EnergyAxis.from_grid` has NO production witness**: `[M]` `get_mixture(r,k).eg is None` for all 12 shipped pairs; the only `eg`-bearing homogeneous mixture in the repo is manufactured at `tests/homogeneous/test_homogeneous.py:415-417`. ⛔ **Two `@verifies` MIRROR tests cannot migrate as §P item 3 says** (`test_homogeneous.py:284/:359` build `F` BARE and no-default-derivation is ruled) — and `:359` is the line-for-line mirror of the production `K = M⁻¹ @ F`, so keeping it pins a RETIRED idiom (gate D12). ⛔ Two `test_matrix_inverse_operator.py` sites misclassified as None-path (they call `from_mesh` on the degenerate carrier). ⭐ **The sharpest single witness**: `[M]` `mix.energy_grid is mix.energy_grid` → False AND `== ` → False (`EnergyGrid` is `eq=False`, the property mints fresh), so an identity-comparing `from_grid` would make two `bulk_space` mints from ONE mixture UNEQUAL and raise inside a legitimate solve (gate A3). Also: `of_axes`/`__mul__` is a Pattern-2 densification fork (F4); `_R1_XFAIL` decorates TWO tests so the constant survives with a falsified reason (F6); a 550 GB densification gate is OOM-KILLED not raised (F7); the widen has no runtime witness (R6); marker migration = **none found** (`foundation` only, no registry entry). **Open rulings: Q-T1..Q-T7** — the name's weight-digest mechanism, whether the energy arm reads UNUSED materials, `weights=None` vs `ones` identity, `of_axes` return type vs `find_factor`, the byte-gate population's non-fissile rows, whether to gate the ruled no-default-derivation (proposed D11), and D5's shelf life.
- **CS1.5 — the Medium un-weld (campaign 1, PRE-carve, dispatched 2026-08-20 @ HEAD `a5d59425`, branch `feature/cs1-energy-space`) — PLAN DELIVERED.** `scratch/cs15_verification_plan.md` (**47 gates** across the design's 4 steps, a **34-arm battery** — 31 code mutations + 3 declared obligations, 3 positive controls, MUST-STAY-GREEN column throughout — **11 findings**, 8 open rulings, 7 declared untestables, 13 rejected alternatives). Design note `.claude/plans/cs15_medium_unweld_design.md`; census `scratch/cs15_grounding_census.md`; campaign plan §2.5. **Lessons → L60.** Baselines `[M]`: battery scope (homogeneous+transport+diffusion+sn/{mesh,primitives,architecture}+4 numerics files) = **1258 passed / 1 skipped / 17 xfailed in 25.30 s**; `tests/homogeneous` **43 passed 2.08 s** (⚠ the design record's "23 / 2.4 s" is PRE-CS1); `tests/numerics` whole **329.66 s**; `tests/sn` whole **≈80 min**; pyright ratchet `total 1` at `transport/operators/scattering.py:761` (not a CS1.5 file). ⛔⛔ **HEADLINE: the design §4.2 + census promise-1 both point at deleting `MaterialMesh.bulk_space`, and `getattr(mesh,"bulk_space",None)` SWALLOWS the typed AttributeError** ⟹ `C` goes space-anonymous, the OperatorSum guard skips again, CS1's D1 floor un-does with NO exception. The union has TWO attribute classes (5 absent / 5+`bulk_space` legal-on-both); promise 1 must be re-worded from *migrates* to *is added at the Medium tier*. ⛔⛔ **The conformity guard cannot use `==`**: `[M]` 4902 random interfaces — SLB and all `uniform` bit-exact (0 ULP), CYL/SPH `equal-volume` **1 ULP** (`4.441e-16`/`8.882e-16`, 10 of 4902) via the `sqrt`/`cbrt` round-trip ⟹ derived 4-ULP band, margin 13 orders, and M3.6 (`==`) reds exactly the 2 curvilinear-equal-volume rows. ⛔ `tests/test_docstring_xrefs.py:391`'s `not hasattr(SNMesh,"mesh")` PREMISE dies at the property conversion; `[M]` replacement witness `SNMesh.quad` satisfies all three assertions. ⛔ eg-coherence can NEVER fire on the infinite member (1 assigned material) — structured-only witness. ⛔ `eq=False` on Medium is FORCED (`[M]` frozen+dict ⟹ `hash` always `TypeError`; `Mixture.__eq__` raises on arrays). ⛔ Retiring `from_materials` deletes `requires_material_id_zero` with no successor unless `InfiniteHomogeneous` carries `mat_id`. ⛔ The DiffusionMesh re-key is UNFALSIFIABLE at its tier (the `ndim` guard preempts it); re-posed at the carrier via a d=3 axis-native SNMesh (`[M]` builds, `mesh is None`). ⭐⭐ **Byte-gate partition MEASURED end-to-end**: space weight ×2 ⟹ `k_inf`/`flux`/`sig_prod`/`sig_abs` BIT-IDENTICAL (D5 green); volume ×2 ⟹ flux halves `397.946→198.973`, rates double (D5 red); `k_inf` blind to both. **Open rulings Q1–Q8; Q1 (one space instance or two — delegate `bulk_space` from `QuotientPoint` to the Medium?) is BLOCKING** for G3.8a's `is`-vs-`==` and D7's second poison arm.
- **CS3 — the cone overturn (`refactor/cone-field-algebra`, PRE-carve, 2026-08-19 @ `000cf144`) — PLAN DELIVERED + capture gate SHIPPED.** Plan `scratch/cs3_verification_plan.md` (687 lines: value-neutrality matrix over 7 consumers, migration map over the 11-file union, new-algebra gates, cone-predicate spec, §6b/§6c audit, 7-arm carve battery, 6 refuted brief items, 4 open user questions). Gate `tests/numerics/test_si_diagnostic_trajectory.py` — `[M]` **5 passed 0.46 s**, pyright 0, **5-mutation battery all as predicted**; `tests/numerics/` whole dir `[M]` 2349 passed. **Lessons → L58.** ⛔⛔ The brief's *"SI computes the diagnostic trajectory from flat norms"* names TWO conventions: interior-leaf metric→flat is **2.29e-16** but whole-composite flat (the CONVENIENT spelling) is **4.71e-3**. ⛔⛔ A ratio-valued pin is Mode-12 blind to a uniform scale (`[M]` ×(1+1e-9) → ρ trajectory GREEN, only `‖Δψ‖` reds). ⭐⭐ A 1-ULP bit-identity wall ALREADY EXISTS: `test_affine_carve_bit_identity.py` under `-W error::…DriftWarning`, `[M]` 3 passed 1.60 s / control 3 failed; DD regression `[M]` 11 of 13 bit-exact, the 2 exceptions named. ⛔ DSA's input-type guard has **zero** negative tests and DSA/adjoint have **no** value gate. ⛔ The brief's cone witness is CELL-tier; a production solve gives `min ψ = −6.40e-01` one parameter from a `+2.18e-01` sibling. ⚠ **Blocking user ruling (Q1): is the relocated ρ on the SPACE norm or the EUCLIDEAN norm?** — CS2's metric move shifts it by **1.12e-3** either way.
- **#344 step 7 (promote the reflective-box `ker A` characterization out of `derivations/diagnostics/`) — SHIPPED 2026-08-15**, branch `refactor/track-b-remainder`, uncommitted (main agent reviews). **Lessons → L49.** Deliverable: `tests/sn/_singular_loss_box.py` (shared NON-collected builders, zero asserts) + `tests/sn/operators/test_loss_nullspace_reflective_box.py` (12 rows: the operator-side dense-SVD oracle on BOTH sides of the predicate, pure-trace, the `T+R` split, the kernel mode's currents, canonicality, the LD closure contrast) + `tests/sn/solve/test_boundary_gs_is_a_coherent_splitting.py` (13 rows: `M−N≡A`, the G-S subspace inverse, 4-mechanism kernel-free coherence + the ERR-056 control, the two-cold-start certificate experiment, the re-titled DRIVER rows, and a NEW public-entry counterpart). `[M]` **25 passed in 64.69 s** (the diagnostic was 24 in **166.58 s**), pyright **0**, `88 passed` with the two shipped gauge suites. **13-arm battery, baseline 0, positive control 13/25.** ⛔⛔ **HEADLINE: the diagnostic's flagship "both convergence functionals are blind to `ker A`" gate was a TAUTOLOGY** — its null vector came from `svd(A)`, so `‖Av‖≈0` is a fact about the factorisation; re-posed as two cold starts a kernel apart through the production driver (L49a). ⛔ Two DOC defects found: the shipped `test_loss_kernel_gauge.py` docstring's SVD table records `T+R` in its `mine`/`law` columns for **3 of 3** T-bearing rows (`224/464/242` where the truth is `0/16/18`), contradicting its own prose and the theory page; and `dd-null-counting-law`'s `+#{tangential DOFs}` term had **no** executed gate (every `_SINGULAR` fixture is `level_symmetric`). ⛔ `SNMesh.reflective_axis_pairs` and `loss_kernel_gauge._reflective_axes` are a Pattern-2 line-for-line twin (L49c). ▸ **Residual: the diagnostic is now fully triaged and `tests/derivations/_promotion_policy.md` §1/§2 says DELETE it** — left in place (927 lines) for the main agent's ruling; leaving it keeps two tier-false names readable in the tree.
- **#340 R5 (retire the imperative `pytest.xfail`) — DELIVERED 2026-08-10, `tests/` + 2 `docs/` pages, `orpheus/` untouched.** Report `scratch/n5_r5_xfail_conversion.md`; **lessons → L45**. Acceptance: `grep -rn '^\s*pytest\.xfail(' tests/` → **0**; the 3 prose mentions in `test_advertised_degree_is_measured.py` untouched; `[M]` `39 passed` (was 36 passed + 3 xfailed) and `35 passed, 1 xfailed` (the 1 now DECLARATIVE strict); pyright 29 = 29 (all pre-existing, measured on HEAD siblings); Sphinx `build succeeded` under `-W`. ⛔⛔ **HEADLINE: 4 of the 5 exclusions had ALREADY healed, and not one by the fix it cited.** All four named #200 (block-inverse preconditioner) as their re-enabler; **#200 is still OPEN** (`_within_group_krylov` still ships `preconditioner = lambda q: q`), yet all four pass — `[M]` `rel` 3.582e-15 / 4.462e-13 / 1.184e-14 vs the closed-form `A⁻¹F` eigenpair, 2–5 orders inside their own gates. The cure is the GMRES `restart`-sizing lineage: ERR-053 (2026-05-28, dropped `restart=min(50,·)`) then #282/#280 route (a) (2026-07-04, sized from the full augmented ravel — its own gate names *"the poorly-conditioned curvilinear-eigenvalue inner … returns a WRONG keff"*). Which was decisive is UNDISCRIMINATED (needs a production revert) and the report says so. Exclusions stamped `R-1 Step D 2026-05-19` ⟹ they outlived their cure by **eleven weeks**, because the imperative form can never `XPASS`. ⛔ **The reason strings are false a SECOND way**: `max_inner` on the krylov path is scipy's `maxiter` = restart CYCLES with `restart == n_dof` (`[M]` 112 for sphere-2eg), so `max_inner=2` returns the `max_inner=1000` answer bit-for-bit — the named budget was never a live knob there (2nd confirmation of N6's side-finding S1). ⛔ **Row 2 (`test_removal_form_kinf_independent_reference_2g`) could not be converted literally** — its body was ONLY the xfail call, and a strict marker over an empty body XPASSes ⟹ reds. Blocker verified against the TREE (no `solve_sn*` takes a removal-form param; `grep removal_form orpheus/` → 0; `foldable_part` exists only at the OPERATOR tier), citation confirmed legitimate via `gh issue view 200` (#200 stacks the σ_r-foldable precond). Body = a concept-level capability probe (grep the issue's WORDS `removal`/`fold`, not a guessed symbol); both mandatory checks run — `--runxfail` reds for its documented reason ONLY, and a flip control injecting `solve_sn_removal_form` in-process yields `[XPASS(strict)]` ⟹ proven self-retiring. ⛔ Scope had to widen: `curvilinear_multigroup.rst` §Gotcha called sphere-4G *"the xfail'd cell"* and NAMED the file (anti-#21 aggravated state) — rewritten past-tense with the measurement; `loss_representation.rst:574` still true, sharpened to name the strict form. Corroboration carries its own control, and the OBVIOUS one was impossible (krylov cannot be truncated by `max_inner`) ⟹ the `ConvergenceWarning` control lives on the **SI** path (`max_inner=2` ⟹ warns + `rel=1.139e-02`; `1000` ⟹ silent + `6.492e-14`) and the report says which path it covers. Side-finding: `docs/theory/verification/matrix.rst` was **stale at HEAD** (`9491`→`9492`; `3f76d651` added tests without regenerating it) — my 4 modules collect 75 before and after, so the delta is not R5's.
- **#340 N6 commit 1 (the truncation warning re-sourced onto `record.first_failure`) — plan DELIVERED + GATES SHIPPED 2026-08-10.** ⏹ **21 new rows** (`tests/sn/solve/test_convergence_contract.py` +19, `tests/numerics/test_iteration_record.py` +2): `[M]` **150 passed, 1 xfailed in 22.83s**, pyright **0**, and a **14-mutation battery all-as-predicted** (control 12 reds; M1/M2/M3 partial-carve 2 each; SWAP 10; forget-knob 15; power_iteration-only 6; guard-flip 2 incl. the xfail XPASSing; field-guard 1; signature 1; + 2 labelled non-catchers: the unreachable fallback **0**, the `AttributeError` over-powered one 13). Harness `scratch/mutate_convergence_contract.py` REPAIRED (it could not import at HEAD) and rebuilt on `inspect.getsource` source-transformation so a missing mutation target RAISES. ⭐⭐ **The SWAP prediction landed exactly, blind spot included**: membership reddened ONLY the 2 fixed-source rows; all 4 eigenvalue rows (which expose both knobs) were caught by the exact-expectation leg alone. ⛔ **Concurrency: the coordinator's M8 probe went live mid-dispatch** and nearly became a false bug report — see L44h; the collision also HANDED me M8 (`[M]` 130 passed flipped vs 130 un-flipped ⟹ **0 reds**). ⛔ Two production additions my pre-carve spec never predicted, both now gated (a level PREFIX discriminated by `is`, and a dropped `tol=` clause for a criteria-less level — which also closes spec blind-spot B5). ⛔ Pre-existing, NOT N6: 3 golden-sha reds in `test_affine_carve_bit_identity.py` (Signature 10, the `level_symmetric` family #337 re-seeded) — attributed by auditing the diff for arithmetic; left for #337's re-baseline sweep. Original plan, `scratch/n6_verification_plan.md` (9 gates G1–G9; 11-mutation battery + a positive control + 2 labelled non-catchers; §7 six blind spots; §8 three side-findings). Plan of record `.claude/plans/archive/nested_iteration_diagnostics.md` §N6. **Lessons → L44.** HEAD `52650a86`, design-only (a full-tree run was live). ⛔⛔ **HEADLINE: the brief's "move three parameters" is really "re-point EIGHT reads"** — `[M]` `_warn_if_unconverged` reads the TOP record at five more body sites (`solver.py:473` `binding_criterion` → feeds distance/rate/projection/branch, `:493` `min_iterations`, `:496/:498/:501` `n_iterations`); the literal three-fact carve emits the inner's knob+budget welded to the OUTER's criterion and projection — worse than today because it looks level-correct. The carve is ONE named intermediate `failing = history.record.first_failure or history.record`. ⛔ **All four committed audibility fixtures are 1-LEVEL trees** ⟹ `[M]` `first_failure is record` is `True`, so 6 of 7 message facts become one object read twice (anti-#22 by object identity); the ONLY thing a leaf row still sees is the knob name. ⭐ **Two-tier fixtures, both measured**: synthetic `N6-S` (~0 s, geometric trajectories ⟹ analytic inner `rho=0.900000/proj 189` vs outer `0.500000/25`; all seven facts pairwise distinct) is the KEYSTONE; end-to-end `N6-E` = `solve_sn` 2-D `(2.0,1.5)/(4,3)` reflective, `A-2g`, LS-S4, `max_outer=3, keff_tol=flux_tol=1e-12, max_inner=20, inner_tol=1e-10` — `[M]` **0.18 s**, outer TRUNCATED 3/3 binding `dphi` (rho 0.153338, proj 17), three children TRUNCATED 20/20, `first_failure=children[0]` binding `residual` (rho 0.962217, proj 586). The brief's d=3 equivalent is ~200× dearer. `[M]` all six entry×inner_solver combos run in **0.01–0.20 s** on a 4-cell 1-D slab ⟹ the knob-stamping sweep (G4) is < 0.5 s; `solve_sn_adjoint` DOES carry children (F7′ already discharged by N2); `solve_sn_adjoint_fixed_source` wants `detector_response` shaped `(ng,*spatial)`. ⛔ **Three committed gates BREAK with `TypeError`** (`:451, :510, :552` pass the retiring kwargs) — mechanical migration + ONE honest re-baseline at `:459` (today it prints the caller's `tol=1.000e-09` beside `last dphi`, whose tol is `1e-6`; nothing asserts it). ⛔ **`scratch/mutate_convergence_contract.py` is TRACKED and cannot import at HEAD** (line 24 binds the retired `_claims_convergence`) and two of its five mutations are anti-#18 stale ⟹ repairing it is part of the commit. ⛔ ***predicted* (3 `[M]` facts; run it FIRST): flipping the guard to `fully_converged` reds NOTHING today** ⟹ G5 (scope, INVERTED by commit 2) + G6 (`xfail(strict=True)` todo marker). ⛔ anti-#16: the knob field takes a free `str` — `[M]` `GreenOperator`'s knob is `max_iter`, so a `{max_inner,max_outer}` whitelist breaks a real caller; the only legal refusal is `""`, and the membership check alone is blind to a SWAP (`solve_sn` exposes both). Side-finding **S1**: `[M]` Krylov `budget=5` vs `n_iterations=732` on a CONVERGED solve — `exhausted_budget` is in restart-CYCLES while the trajectory is in callbacks; a truncated Krylov inner would advise `set max_inner=<callback count>`. File it.

- **SN curvilinear ANGULAR-CLOSURE seam (`orpheus/sn/sweep/pole_angular_closure.py`) — gates SHIPPED 2026-08-11**, branch `refactor/operator-strategy-layers`, uncommitted (main agent reviews). **Lessons → L47.** Deliverable: `tests/sn/sweep/test_angular_cell_partition.py` (NEW, 9 gates / 56 rows, the partition producer's FIRST value gate on either arm) + `tests/sn/sweep/curvilinear/test_psi_half_positivity.py` (NEW, 4 gates / 19 rows) + a widen/re-derive of `tests/sn/sweep/curvilinear/test_tau_producer_equivalence.py` (5 → 14 rows) + the `vv-status` block in `docs/theory/foundations/structured_geometry.rst`. `[M]` 89 passed on the three modules, pyright **0**, `tests._harness.audit` clean (`angular-cell-partition` 30 tests, no sentinel violation), scoped 298-row slice **298 passed / 0 failed** (a 25-min wide run incl. `slow` gave **357 passed / 27 xfailed / 0 failed**). ⛔⛔ **HEADLINE: the three committed τ properties share ONE symmetry group and the orientation flip is inside it** — membership `[0,1]`, the fold box `[¼,¾]` and the reversal identity `τ_m+τ_{M−1−m}=1` are ALL invariant under `τ→1−τ`, so the flip reddened **0 of those 4 rows**; catcher = the signed, tolerance-free `(τ−½)·μ ≥ 0` (12/12 rows red). ⛔⛔ **The brief's `min ψ̂ ≈ −77` is a ZERO-SEED number**: on the production value path (marched ψ½ state) `[M]` ψ̂ is **positive**, `+0.1337/+0.1286/+0.1287` at n_φ=6/8/16; `−76.9` reproduces only with a random ψ + zero seed. Mechanism pinned instead: `A(M)=max_m ∏(1−τ)/τ` (`[M]` 2.41…**9.44** at M=2…32 — the `vv` #24b figure, previously uncommitted) + the exact `∏(1−τ)/τ = 1`. **13-mutation battery, 298-row scope, control clean**; measured coverage delta for the three partition-specific mutations: **6 pre-existing catchers each → 27/32/34 new**. ⛔ M3b (`w[n]→w[N−1−n]`) is a REFUTED candidate — bit-identical on palindromic GL weights, **0/298**. ⛔ Two labelled non-catchers recorded in-docstring (the value-path positivity row is green under 4 chart mutations; the product identity is invariant under `τ→1−τ`). ⛔ My own first draft called production in a `parametrize` ARGUMENT LIST ⟹ 6 of 13 mutations reported `FAILED=0` via a collection kill. Costs: fast catcher scope **32 s**; the same slice with `slow` + 3 heavy files is **24 m 53 s**.

- **Q5.6 "6.3 flip" (`SNMesh(CYLINDRICAL)` admits ONLY carrying quadratures) — three gate sets DELIVERED 2026-08-08**, `scratch/q5_6_3_gate_design.md` (§2 ask a = the admission module, §3 ask b = a RECONCILIATION, §4 ask c = the slot re-pose, §5 twinning ledger, §6 six user questions, §7 order). Substrate: `scratch/q5_6_3_flip_call_sites.md`. **Lessons → L43.** ⛔⛔ **The tree moved TWICE mid-dispatch**: brief HEAD `143e6e2a` → delivery HEAD `ce6607f5` (+ 12 uncommitted test files mid-migration), and **ask (b) had already LANDED** — `test_mms_ordering_blindness.py::test_production_mms_source_is_identical_on_the_mirror_pair` was re-posed at `ce6607f5` exactly as I was deriving it (parent rule via `dataclasses.replace`, both builders, + a restriction leg), so §3 is a **six-gap audit** instead of a design (`atol=1e-15` where `[M]` the delta is exactly `0.0` and bit-exactness is a float theorem; node-level preconditions ungated, which also leaves a **Mode-12 hole** — `[M]` the source has NO `mu_z` term so `Q[π]==Q` holds for σ_z too; the docstring's `psi_ref` clause is **signature-tautological** and doubly false for the isotropic case which has no `psi_exact`; the ξ-odd control runs on NODE_ALIGNED while the positive runs on STAGGERED; parent/case coupled by coincident defaults ⟹ a red by RAISING; nothing asserts the builder ships the fold). ⭐⭐ **Ask (a)'s keystone is a PROVENANCE PINCER**: `Quadrature.quotient()` stamps `folded_by`, so `[M]` a hand-assembled rule with `folded_by=None` and `array_equal` arrays must CONSTRUCT while a genuine `quotient(NODE_ALIGNED product)` (tag present, `on_edge_node` ×4, N=20) must REFUSE. ⛔ **Slab GL is a DISCRIMINATION row, not a negative** (the structure-less guard at `reduced_operator.py:826` owns it; the predicate would too if wired one line earlier — `[M]` it classifies GL `(True,False)` without raising). ⛔ **The `∀ levels` quantifier is unreachable** through any shipped factory ⟹ split `non_carrying_levels(starts) -> tuple[int,...]` out of the raiser. ⛔⛔ **Ask (c)'s obvious functional is Mode-12 blind**: `[M]` every per-level datum on a folded product is **bit-palindromic** (`s_p` = {0.5084, 0.9404, 0.9404, 0.5084}; α, ΔA/w, η, w all palindromic) — only signed `mu_z` and the index sets escape ⟹ two rows. `[M]` 10 of the 20 cylinder slot rows are near-vacuous TODAY (early return). ⛔⛔ **Collateral: the #280 2.5b direct-seed fold becomes DEAD CODE** (`loss_representation:4195-4212` + transpose twin) — its 6-test battery loses its SUBJECT. `[M]` fragments minted: `non-carrying` (0 exception-message hits), `starts on an ordinate` (0 hits), `shared eta-minimum` (0 hits), disjoint from the existing `level structure`. `[M]` MMS: the fold is **19× better at equal ordinate count** (a one-time measurement — the comparison arm refuses post-flip, so it can never be a gate) and the isotropic ladder is identical to 3 s.f. across every rule. `[M]` slice cost `128 passed in 0.93s` ⟹ no rationing. Baseline clean.

- **G2 (geometric-transformation machinery / `orpheus/geometry/transformation.py`) — verification plan DELIVERED 2026-08-03**, `scratch/g2_verification_plan.md` (**42 gates**: A1–A6, B1/B2a/B2b/B3/B4, C1–C5, D1–D5, E1–E3, F1–F4, O1–O8, X1–X3, H1–H3; all `@pytest.mark.foundation`, target `tests/geometry/test_transformation.py`). **Lessons → L35.** ⛔ **HEADLINE: G1 LANDED MID-PLAN** — the brief said the module did not exist; it does (763 lines, **untracked**, mtime 04:30). Re-scored: **10 of 15 API findings DISCHARGED** (no `Reflection`/`Rotation` TYPES at all — one `RigidMotion` with named constructors and a COMPUTED `determinant`; `permutes(points,*,atol)` / `preserves(...,atol,weight_atol)`; `a @ b` = apply-b-first, documented; `_exact_key` on the realized `(Q,t)`). **5 OPEN**: **A14 the embedding `ι: E(d)→E(d+1)` DOES NOT EXIST** (zero `embed`/`lift` methods — this BLOCKS campaign step G3's "delete the 1-D/3-D arm split" and gate H2), A6 (`-> ndarray|None` collapses 3 guards), A4 (`close_group` raises a bare `ValueError`), A1 (d=1 rotation raises but blames the caller's plane instead of naming `SO(1)={e}`), A15 (no `Permutation` type). **3 gates the landed design EARNS**: X1 `on_points` vs `on_directions` (seating-invariance of the direction action), X2 the circle-point rotation primitive preserves exact zeros, X3 a glide has `element_order() is None` while its linear part has order 2. `[M]` 13 gate families incl. every ⭐ row smoke-run against the real API — all pass. Key measurements: the wrong composition order violates the homomorphism on **102/144** cube pairs; all four offset variants are involutions while the fixed plane moves 0.37/0.74/1.48; seated `O_h` preserves a shifted cube **48/48** vs **1/48** unseated; a 1e-9-off set certifies under a 1e-7 window with an injective π; ERR-073 duplicate isolates guard 2 (48/48, 0/48, 48/48); the `k=2` generator mutation is caught at even `n` only; SymPy general×general conjugation CHOKES (>120 s) — specialise one factor (0.6 s).

- **#325 (symmetry-exact circle nodes / `roots_of_unity`) — verification plan DELIVERED 2026-08-01**, `scratch/issue325_verification_plan.md`, plus a SHIPPED gate module `tests/numerics/test_roots_of_unity.py` (17 gates / 251 params, `[M]` green under `-O` and plain in 0.52 s; **all 17 mutation-proven** — 14 by an 11-mutant body battery, 3 by targeted constant/shape mutations; control clean). **Lessons → L34.** ⛔ **HEADLINE: #325 is BLOCKED.** `[M]` exactness makes azimuthal mirror partners share `η` bit-exactly, so `rules_product.py:139`'s default `np.argsort` has real ties: per-level ordering changes in **36/36** configs, and on a heterogeneous 2-G cylindrical fixed-source at `n_phi=32` the ORDERING ALONE (node values bit-identical) moves the flux **1.008 %**, while `quicksort` vs `stable` differ **1.775 %** — versus a **1.06e-14** node-value drift. It does NOT converge away (flat ~1e-2 at `n_phi ∈ {32,64,128}`). The sort key `η` is 2-to-1 over φ ⟹ the ordering was never determined by the physics; **U1 is a USER physics ruling** (sort by φ / lex `(η,ξ)` / `kind="stable"`), and it must land as its own commit BEFORE the node-value change. Other live findings: `test_eps_below_min_genuine_cosine` is a **dangling xref** (real name `…sits_in_the_round_off_to_genuine_gap`) and `product` is NOT in its fixture list and **would fail today**; `test_product_bit_identical_to_legacy_adapter` is a **proven TAUTOLOGY** (random-node replacement leaves it green) so the `cyl_*_product_*` snapshot has **no independent anchor**; odd-`n_phi` `reflection_index(0)` is garbage (residual 0.94) yet passes perm+involution (ERR-042/045 do raise, ERR-044 does not). REFUTED: the NN search is NOT mis-pairing (margin 5.0e-3 vs 1e-16 up to `n_phi=1024`) — KEEP it, gate exactness. Issue-body "≤1 ULP vs `np.cos`" is FALSE (3.75 ulp); vs 100-digit mpmath the new code is 0.57 ulp and legacy 3.72 ulp.

- **B3.4c (periodic → the partner face) — verification plan DELIVERED 2026-08-01**, `scratch/b34c_verification_plan.md` + a RUNNABLE gate module `scratch/b34c_proposed_gates_dryrun.py` (95/95 green; 7-mutation battery, control clean). **Lessons → L33.** HEADLINE: all seven production steps landed on disk DURING the plan (steps 1/2/3/5/6 were already uncommitted at start, step 4 mid-session) — **what remains is entirely the TEST surface, and it does not exist.** The two live "B3.4c" strict xfails CANNOT detect the change: `[M]` `test_bc_equivalence_snapshot.py::…test_delivers_the_partner_faces_outflow` still reds byte-identically (735/735) AFTER step 4, and its documented flip-edit makes it a verbatim duplicate of its own green flip-proof sibling. `test_b3_domain_narrowing.py[periodic]` flips only if the realized leaf also VALIDATES its domain. Also open: `face_name`/`face_opposite`/`FACE_NAMES` are UNGATED (no `--doctest-modules`); the three block-diagonality gates stay GREEN while their REASON becomes false; a 2-D cross-axis companion is mandatory (slab is degenerate); `periodic` is NOT in `SNMesh.BOUNDARY_OPERATOR_REGISTRY` so the only end-to-end path is `sn.realize_boundary_law` injection (#189). Complementary blast radius: `scratch/b34c_periodic_blast_radius.md`.

- **Boundary machinery B3.2 → B3.4b + task #21 — ALL DELIVERED, branch `refactor/operator-strategy-layers`** (B3.4b committed `943b37c1`). Design of record `.claude/plans/archive/b3_domain_narrowing_crosswalk.md`; per-phase notes `scratch/{b3_2_migration,b3_4a_test_migration,b34b_verification_plan}.md`; mutation harnesses `scratch/b3_2_mutations.py` / `scratch/b3_4a_mutations.py` / `…/tmp/{mut,bcsnap_mut}.py`. **Durable lessons → L29 (narrowing), L30 (inherited-surface migration), L31 (shared-body `≡`), L32 (recorder→reference inversion).** Latest (#21, 2026-08-01): the bc_equivalence generator was INVERTED per user ruling — it no longer imports `SNBoundaryRealizer`; all 7 `.npz` re-anchored to expressions derived from the law equations; **43 passed/1 xfailed**, affected `tests/{geometry,sn/operators}` `1465 passed/0 failed`, pyright 0, Sphinx `-W` clean; derived-vs-retired-recording = **bit-identical ×3, 1–2 ULP ×3, 98 % ×1** (periodic). **STILL OPEN / actionable**: (1) a narrowed law does NOT validate its domain — a full-face input is silently accepted (RG-3b strict xfail); (2) narrowed ∘ un-narrowed cannot compose → 6 mixed-BC strict xfails unblocking as each law narrows; (3) `SNMethodSpace.minimal` is a partial constructor, retirement candidate (only albedo/periodic use it); (4) periodic's reference-image row is a strict xfail naming **B3.4c** — deleting it is that phase's job; (5) LATENT production bug `transport/method.py::_law_from_tag` builds every parameter-free law as `law_cls()`, so `WhiteBoundary` always defaults to `xmax` — unreachable until #189 admits `white` to `SNMesh.BOUNDARY_OPERATOR_REGISTRY`; (6) `test_operator_block_role.py` carries a REAL latent orientation mismatch (`face="xmin"` vs a law declaring `xmax`).

- **Three-DOF separation (operator ∥ splitting ∥ realization) — full gate spec DELIVERED 2026-07-28, PRE-carve** (`scratch/campaign_verification_plan.md`; lessons L24). **HEADLINE: the proposed acceptance criterion is ALREADY GREEN and cannot red** — `build_within_group_system` takes no strategy parameter; `A==M−N` measured EXACTLY 0.0 (2-D Cart, both schedules) / 3.5e-17 (sphere carrying); falsifiers 5.15e-2 / 8.57e-3 prove non-vacuity. **The real red = the WELD (R7)**: `WithinGroupSystem` spans stage-2+stage-3, and `_within_group_si(...,'gauss_seidel')` gives `base is record.implicit_operator` → **False** (twin splitting; `jacobi` → True = the control leg). Other reds: `F.domain is None` + `F.H` → bare Euclidean transpose (R1/R2); `A − M` RAISES `IncompatibleOperatorComposition` (R4, cross-carrier — *unspellable*, worse than red); non-uniform carrier guard (R6); no pencil type (R3). Replacement AC = 3 legs (signature-disjointness / `is`-identity of the driver's splitting / sha256 object fingerprint across 16 strategy rows). Spectral gate anchored on `ρ = c` and `ρ = c/(1−c)` with a MANDATORY anisotropic power-iteration seed (isotropic seed ⟹ `N≡0` ⟹ ρ≡0 = the Mode-9 trap in closed form) + `SourceIteration.contraction_ratios` cross-check. Perf gate = leaf-kernel call-COUNT invariance under mesh refinement (hard-blocks the stage-3 merge). #261 Pattern-M spelling ruling RESPECTED (gates the contract, not the dispatch mechanism).
  - **P0 DELIVERED 2026-07-29** — `tests/sn/architecture/{test_composition_cost,test_monomorphic_leaves}.py` (9 perf gates + G1.1/G1.3/G1.4/G1.5 = 82 passed/20 xfailed; pyright 0). Discharges O-2/O-3/O-6; lessons L25 (call-count perf) + L26 (commutator, not "uniform h"). Carry-forwards: P-2 bound `MAX=72.0` is **PROVISIONAL** (contended host — recapture quiescent); the in-file fissile `_fissile_anisotropic_2g` should fold into `_config.py` at consolidation (`anisotropic_mixture` is non-fissile ⟹ F ≡ 0 ⟹ `0==0` rows).

- **#280 RESIDUE — binding gate spec DELIVERED (`.claude/plans/archive/residue_verification_spec.md`, 2026-07-23, PRE-carve, awaiting surgical impl).** Completes SN adjoint: R1 (scheme-registered transpose kernel PAIR + LD-slab from UBLD algebra-of-record) + R2 (multi-D Cartesian reverse WALK, DD→LD-2D `apply_transpose`). Sibling of L17/L18/L19; 5 flip-safe commits C1–C5 (spec §10). Keystones = OBJECT oracles (SymPy `simplify(VJP−AᵀM⁻¹)==0` + dense-Euclidean-`Mᵀ` off FORWARD apply, NEW 2-D probe required + Cartesian assembled-`Mᵀ`); reciprocity = composite canary ONLY, NEVER spectral (Mode-12). THREE contract findings (§13, verification SHAPES the carve): (1) R1a VJP `(residual,psi_out)→(psi_bar,psi_in)` INCOMPLETE for curvilinear DD (drops M-M `numer_bar`) → keep angular VJP on `PoleAngularClosure`; (2) `has_transpose_kernel` must be REGISTRATION-coupled not a bare bool; (3) LOAD-BEARING — LD `.H` metric MUST carry moment mass θ (`G_bulk_LD=V·w_n⊗diag(1,θ)`, verified LD fwd=`M⁻¹(Aψ⃗−R)`,`M=⊗diag(1,θ)`) else `.H` WRONG adjoint on slope DOF AND reciprocity Mode-12-BLIND (L18/ERR-067 metric-repair applied to a mass-normalized op). Mandatory config: ≥2G + asymmetric SigS + non-uniform h + rectangular nx≠ny + anisotropic + full-composite recip + backward-octant.
- **Coupled Block Operator — Phase-C step-5 (#41) gate spec DELIVERED** ([step-5](coupled_operator_step5_solve_verification.md), branch `refactor/sn-walk-unification`). Resolvent block-triangular DIRECT + scattering ITERATIVE; ρ-honest STOP = `evaluate_residual(A,ψ,q)` not `‖Δψ‖`. M (per-iter SI, back-substitution, row-6 dense-LU oracle + in-M-lag certificate) vs A (full 2×2, `MatrixInverseOperator` + NET-NEW `apply_transpose` LUᵀ). Awaiting user rulings D2a/D3/D5 + 5a-5e carve.
- **Diffusion integration #290 pre-carve plan DELIVERED** ([plan](diffusion_integration_290_verification_plan.md)). `SigS1=0,SigT=transport` Mixture reproduces legacy D bit-identically → refs don't move. Mode-12: k=λ_max(A⁻¹F) similarity+transpose blind → object gate `A_diff.as_matrix()≡FD-stencil` + MMS. 3 user rulings; awaiting carve. **#290 MERGED @ `3a19133` (`A_diff` real) — the DSA consumable below is now built.**
- **DSA for SN (#2) — full gate battery DELIVERED (`.claude/plans/archive/dsa_verification_spec.md`, PRE-carve, runs AFTER #280 walk unification).** 13 gates D1–D13 (lessons L23; sharpens vv Mode 9). THE crux = the **correction→0 PARTITION**: FP-invariance (D3–D5) catches ONLY the σ_r-fold #215 class (1/8 canonical errors, ANISOTROPIC config mandatory — `Mixture` ctor DIRECT, `make_mixture` nulls ℓ≥1); the other 7 (R/P/`A_diff`/correction-sign/boundary) are FP-INVISIBLE (correction→0) → caught ONLY by object gates (D1/D2 assemble≡hand-stencil, D7 R-conservation+anti-#8 delta-source, D8 P≡Rᵀ frame-adjoint) + rate gates (D11 ρ=0.2247c residual-estimator, D12 reflective-divergence, D13 count-table). D6 correction→0 (+first-iterate non-triviality closes the dead-R blind spot), D9 no-masking, D10 σ_r routing Mode-11 guard. Phase-map 3a=D1/D2, 3b=D3–D10, 3c=D11–D13. 7 tensions for plan-of-record (R4 low-order provenance ruling; L16 shared-closure independence; D8 blocks on cross-domain-attacker PG-frame; ERR-070 to file on first σ_r-fold catch). #215 has NO ERR entry (catalog ends ERR-069).
- **A3/#280 Phase-2.5 walk-unification** — S0/2.5a/2.5b/2.5c/2.5d gate files AUTHORED; the surface (reverse-scan `apply_transpose`, swap-law, two-factor `is_adjointable`) is on `main` (residue above is the live successor). Recipe [a3-reverse-scan](a3_reverse_scan_transpose_verification.md); lessons L17/L18/L19. 2.5c Part-A completion breaks (`is_adjointable` False→True broke `test_capability_survival`/`test_inverse_operator_equivalence` rows) reported. Reconcile per-hash vs git before resuming.
- **Prior SN campaigns** (#206/#208/#236/#240/#247/#251/#257/#18/#19/#20) MERGED to `main` — NOT open work. `git merge-base --is-ancestor <hash> HEAD` before trusting any "in-flight" claim.
