# Test-Architect Memory Index

Three sections: (1) the lessons pointer — READ FIRST every dispatch;
(2) active/in-flight state — git-true; (3) durable reference (reusable
verification-design files cited by AGENT.md or holding a meta-recipe).
The failure-mode taxonomy lives in the `vv-principles` skill; the
reference inventory + XS mixtures live in `AGENT.md`. Do NOT re-add
campaign play-by-play here — it is merged archaeology.

## 1. Lessons (durable verification discipline — read at START)

- [Test Architect lessons](lessons.md) — L1 config-blindness (flat/1G/homog/slab/iso-snapshot); L2 reference↔claim-layer + structural independence; L3 MMS simplification-bias override + activate/null declaration; L4 prove every gate's teeth bite (Mode-8/xfail/mutation/call-count); L5 characterization-gate without calcifying; L6 Mode-10 structural teeth; L7 regression-tolerance = the claim (SAFETY×conv_tol, non-fissile→fixed-source, IEEE-vs-exact); L8 cross-method infra; L9 V&V tagging; L10 proactive-refute-the-premise payoff; L11 axis-transpose-of-shipped-reduction (gate the FLIPPING rule, not the shared machinery); L12 fast-path→composed FOLD = principled-equiv-not-0-ULP + unchanged-sibling-stays-0-ULP + "transpose-falls-out-free" gated on the ONE leaf transpose + Euclidean-not-`.H` reciprocity + Mode-11 leaf-wrap + outside-the-fold channel; L13 capability-string→typed-operator carve (`.solve`/`CAP_SOLVE`→`.inverse()`, `CAP_APPLY_TRANSPOSE`→`.H`): equiv-keystone=bit-id INHERITANCE+closed-form anchor+seed Mode-11 wrap; the WHOLE frozenset (BOTH axes) retires SAFELY (the "iff both" law is in the METHOD BODY not the set; half-modernized axis=twin-path HAZARD) — licensed by a FAITHFULNESS scaffold gate (derived `is_invertible`/`is_adjointable` ≡ old frozenset ∀ operator, asymmetry fixtures mandatory, then deleted; recursive pins permanent); runtime-query=derived `@property` NOT `isinstance(Supports*)` (class-uniform→blind to half-adjointable composites+value-dep singular edge; Protocols=static contract only); LITERAL-string `"apply_transpose"` precondition in 2 S† canaries breaks on deletion (grep both); M-ADJ-FORGE downstream=RAISE not wrong-value; adjoint-of-inverse via RECIPROCITY not round-trip (metric≠Euclidean activates curvilinear). L14 the FIRST *iterative* inverse (`GreenOperator`=`OperatorSum.inverse()` wrapping the SI driver): NO legacy-solve to inherit→keystone=dense-LU-anchor+G-Neumann NOT bit-id, `inverse().apply≡solve` is a TAUTOLOGY (solve:=inverse().apply)→not evidence; `ConvergenceFailure` MUST check TRUE residual `‖Aψ−q‖/‖q‖` NOT the driver INCREMENT (Signature-9 ρ-blind, c=0.99 falsifier); `is_invertible=a.is_invertible` FLIPS frozen `is_invertible is False` pins False→True by design (grep-audit WITHOUT adjoint-filter which HIDES compound-line hits; leaf/product STAY, plain-sum-invertible-leading FLIPS; "no general (A+B)⁻¹" retires) + faithfulness green ONLY if conditional CAP_SOLVE is LOCKSTEP + BOTH asymmetry fixtures (inv+ao & ao+inv; (T,F) uniquely IDs the a-rule); 3 ordering edges (L+C→Sweep via MRO / C+L→divergent-Green-loud-fail / (−S)+A→refuse-at-ctor-canonical-msg) + flatten-EXACT-OperatorSum-only; NEW iterative op needs its OWN Mode-11 seed pin (landed per-iterate spy one level below is BLIND + drop value-invisible); name-earning triage (G-Neumann+dense-anchor LOAD-BEARING / G-reciprocity INCLUDE-lighter non-symmetric-A Euclidean-not-`.H` / G-kernel FOLD-into-anchor; L1 Mode-9=het≥2G VACUUM slab); wrap-delegate MIXIN extraction is no-op⇒existing-suites-green-UNCHANGED + pyright abstract-sig teeth (#285 STRUCTURAL). L15 the RETIREMENT terminal step (frozenset→typed-predicate, EXECUTES L13's design): the coexistence scaffold (`is_X≡CAP_X∈caps`) DELETES→its PERMANENT successor is keystone-v2, a structural/value CONTRACT (`is_invertible False`⟹`_STRUCTURAL_ABSENT` not-hasattr OR `_VALUE_RAISE` eager-`NotInvertible`; pin WHICH per type) + eager-`.H`-or-`MissingAdjoint` mirror + bridge-consistency (`invertible(op)==is_invertible` pins the TypeGuard body); migration is a MECHANICAL-RULE + completeness-REGREP (returns-ZERO) NOT a table (surface WIDER than any map—127 reads/33 files, re-grep yourself; bare-string `"apply_transpose"` preconditions missed by CAP-grep); land-order W1-additive/W2-ATOMIC/W3-teeth (frozenset+its exception-class retire together—a W1 `MissingCapability→NotInvertible` flip breaks `pytest.raises(MissingCapability)` mid-flight; eager-`.H` is the ONE W1 behavior change; ratchet re-baselines DOWN); the pruned-body `OperatorProduct.solve` re-route = bit-id-INHERITANCE(`--capture-baseline`)+dense-anchor+Mode-11-sentinel, DIRECT-factors `array_equal` but the ITERATIVE Green-factor row `assert_regression(SAFETY×driver_tol)` NOT array_equal (L7 iterated-drift), non-commuting Diag@3-cycle-Perm activating. L16 NEW *consumption mode* of a shipped per-cell closure algebra (`assemble()`=3rd mode beside sweep-`solve`/matvec-`apply`, emits same coeffs as sparse COO/CSR): ONE-SOURCE PROOF=shared-source sign-flip (`cell_balance_for_streaming`/`assemble_ubld`/`_interior_conductance`) reds BOTH new equiv-gates AND existing sweep/matvec (only-new-reds→twin-path→STOP); DD=HIGHER twin-risk than LD (DD no dense block/fused scalar matvec; LD apply already `einsum(assemble_ubld.A,psi)`). NO gate 0-ULP: sparse-order≠apply-order→G1(`asm@x≡apply(x)`)=nulp/`rtol=1e-11` NOT `array_equal`; `solve_triangular`(LAPACK)≠sweep scalar-recurrence→G2=rtol AND that independence EARNS the L2-crosscheck+discharges the sweep-inverse contract (#284), triangularity leg=EXACT `triu(P·L·Pᵀ,k=1)==0`. `as_matrix`-DELEGATES-to-assembly TAUTOLOGY trap→force the RETAINED `_as_matrix_by_probing`≡`assemble().to_dense()` (keep ONE probed≡assembled fuller-view-oracle/family). Config: DOF-transposition+face-pairing mutations transpose-BLIND on symmetric A (Mode-12)→mandatory asymmetric-SigS+non-uniform-h; G1 x=non-flat fixed-seed random (flat nulls DD streaming §0.6), K≈8 (Δx=0 for fixed bug caught w.p.1), NEVER `keff(asm)≡keff(apply)`. Out-of-scope defect (curvilinear assembly blocked on #282 pole-seed)=POSITIVE assert-defect `np.any(triu≠0)`+loud-flip msg, NOT xfail (opposite of strict=False). RELOCATION behavior-free BY CONSTRUCTION→lean on EXISTING `-W error::DriftWarning` snapshots+Sphinx-`W`+regrep-ZERO, argue AGAINST new hash; tests travel WITH modules (relocation≠retirement). L17 reverse-SCAN transpose-solve (#280, 4th face of the `(L+C)` 2×2) + its apply-loop unification: RECONCILE the retired a3 vocab FIRST (`solve_transpose`/`CAP_SOLVE_TRANSPOSE`/`_AdjointOperator.solve`→LIVE `SweepOperator.apply_transpose`+`is_adjointable`; metric transpose-solve falls out of EXISTING `_AdjointOperator.apply` FREE; `_AdjointOperator.inverse()`+`is_invertible` close `A.H.inverse()≡A.inverse().H`); the existing 0-ULP `array_equal` matvec canaries are SELF-REFERENTIAL (ref=same `loss_action[_transpose]` on fresh op)→necessary-not-sufficient for RELOCATION→genuine proof=FROZEN pre-carve `assert_regression --capture-baseline` (both orientations); new shared 1-D loop frame needs its OWN pin (NO 1-D spy exists, unlike multi-D `test_one_octant_walk`)=Mode-11-WRAP spy + AST tripwire forbidding `is_adjoint`/`is_forward`/`is_transpose`, demand orientation-OBJECT; assembly gives NEW structurally-indep transpose oracle `solve_triangular(permuted.T,b,lower=False)` (catches wrong transposed-scan-coeff a'/b')—Cartesian-ONLY→SPHERE μ-reversal/mirror INVISIBLE, dense-apply G2 `solve(M.T,b)` stays KEYSTONE on the mandatory sphere leg; object-level ONLY (NEVER `keff(sweep_transpose)`, Mode-12); G3 `(L+C−S−B)` reciprocity PRE-carve (S† `15185e5`). Full recipe→§3 [a3-reverse-scan]. L18 CARRIER AUGMENTATION's ψ½ block (#282 route (a): the direct μ=−1 seed as a typed per-level block, spec `a3_solve_transpose_verification.md` §16). ⚠ SHARPENED 2026-07-06 (derivation-of-record `starting_direction_metric_gauge_derivation.md` REFUTED the ghost premise): a DOF's Hilbert STATE metric is set by its OPERATOR ROLE not its angular-INTEGRATION weight — ψ½'s self-block A_ss is a BANDED radial transport op `μ∂_r+σ` (diag_gsd_05), so G_sd=V_cell (SPD), NOT the ghost 0; α₁ᐟ₂=0 is the angular THROUGH-FLUX weight (an operator coeff excluding ψ½ from φ=Σwψ), not the state metric. G_sd=0 is the single FORBIDDEN value (puts seed in null(G), severs seed→bulk A_bs from A.H=G⁻¹AᵀG → WRONG adjoint for nonzero seed, defect 1.3e-2, green only on a present-ZERO seed). `apply_transpose≡Aᵀ` EXACTLY ⟹ reciprocity GAUGE-FREE among SPD G_sd; with SPD G_sd reciprocity CATCHES a seed-row flip (Mode-12 CLOSED) → INVERT the old blindness gates (`_blind_to_`→`_catches_`, assert flip REDS). A Mode-12-CLOSURE reciprocity gate needs BOTH legs: control-holds (unmutated nonzero-seed recip `<1e-12`, baseline=honest V_cell) + mutated-reds (flip `>1e-6`); mutated-ALONE is FOOLED by a broken baseline (revert g_sd→0 gives flip-defect 0.107>tol TOO — broken baseline mimicking "caught"). Dense sweep {0/id/V_cell/V·w}×{zero/rand/flip}: g_sd=0→rand 0.107/flip 0.107 (BLIND); V_cell→rand 1e-16/flip 1.000 (CLOSED); faithfulness ‖G⁺TG−A.H‖=2.8e-14; gauge INERT (corner V[-1]→2V[-1]: recip green + bulk solve bit-id). `_g_inner` seed term reuses PRODUCTION `starting_direction_space.inner_product_weights` (gauge MUST match A.H, else false-red; NOT contamination — seed metric verifies SPD-ness not a VALUE, the TRANSPOSE is the load-bearing catch). Canonical gate=promoted `diag_gsd_02` dense sweep; production-path=inverted `test_282::…_catches_a_seed_row_flip`. The closed-form direct-solver pin + EUCLIDEAN Mᵀ oracle + full-field round-trip still pin seed COEFFICIENTS (orthogonal to the metric). Direct-solver structural pillar=closed-form exponential ODE `φ=q/σ+(φ_R−q/σ)e^{−σ(R−r)}` O(Δr²), NOT the recurrence hand-trace (procedural, ERR-032; existing pins route the PROXY `Q̄=σφ₀/Σw`); GRADED-mesh row MANDATORY (uniform blind to `dr[k]` drift Mode-5); q½ fold `P_ℓ(−1)=(−1)^ℓ` anisotropic 2-term hand-check manufactured FIRST (iso suite blind §0.6). PROVE the block CONSUMED (zero q½→solve MUST move; else INERT) + Mode-11 WRAP sentinel on `carlson_inward_sweep_from_source`. Cold-residual is THE acceptance number (promoted diagnostic, lag-death classifier): sphere 5.18e5→`<1e-11` (no iterate-threading, no sphere slack — fix EARNS tight tol), slab/cyl ~7e-16 STAY; certifies solve↔apply CONSISTENCY not coefficients; +seed-insensitivity 4.57e-2→bitwise +decisive-classifier (`b=A.apply(psi0)`;cold recovers psi0) +coarse `level_symmetric` S4 end-to-end (NaN/neg→finite+positive, physicality NOT precision NOT keff) +c=0 degenerate. CYLINDER "unmoved" is a TESTED assertion (weightless: α=0+α-dome telescopes)→assert `array_equal` FIRST; a move HALTS the carve; only SPHERE re-captures (principled), slab bitwise. R10 seq 2.5d BEFORE 2.5b→sphere G1/G2 written EXACT single-pass (supersedes a3 §0.3 loose-1e-9); 2.5d successor=triangularity-ONLY matvec-probed AUGMENTED `triu(PᵀM_augP,1)==0`, LAPACK-≡-sweep waits for 2.5b. Framing-agnostic (F1 block/F2 trace/F3 zero-weight-ordinate all give zero G-weight)→gate the PUBLIC contract. Sub-phase sibling of L16/L17. L19 the SWAP-LAW wiring TERMINAL (#280 2.5c, `A.H.inverse()≡A.inverse().H`; `_AdjointOperator.inverse()=inner.inverse().H`, `SweepOperator.apply_transpose=inner.solve_transpose`, `is_adjointable` flips True over `InvertibleOperator`; metric solve `G⁺·solve_transpose(G·b)` FREE from existing `_AdjointOperator.apply`; file `test_inverse_adjoint_coherence.py`): Gate 1 (forward-matvec G-reciprocity `⟨Aψ,x⟩_G=⟨ψ,b⟩_G`, `x=A.H.inverse().apply(b)`) is structurally independent of the reverse-scan BY CONSTRUCTION (verification arithmetic=FORWARD apply+metric ONLY, reverse-scan lives solely in the SUT x); `b` MUST be bulk-only source-carried (#284 free-DOF slots + metric null space |Ω·n|=0 ⊕ ghost-seed → random FULL b falsely reds ~1e-1 even unmutated; bulk-only ~1e-16), ψ stays FULL (activates trace coupling+metric), Gate 3 `A*∘(A*)⁻¹` compares BULK only; Gate 2 (swap law) is a BIT-IDENTICAL object identity (`array_equal` not rtol — both spellings run the SAME `A.inverse().H` graph). **M-ADJ-metric "slab stays GREEN" discriminator is FALSE** — composite `G=V·w_n`(bulk)⊕`|Ω·n|·w_n`(trace) non-trivial on EVERY geometry (non-uniform mesh + bulk-vs-trace weight mismatch), `A*=G⁻¹AᵀG≠Aᵀ` everywhere, reds slab too (0.33/0.19/0.13); `.H`≠Euclidean TRUE universally, NOT a slab split (never design one as slab-green; sibling L1). `A.H.inverse()` needs the Design-C `invertible(A.H)` TypeGuard bridge for pyright (`.inverse()` on `SupportsInverse`, `.H` returns base; positive-branch narrow + `pytest.fail` NoReturn; reads predicates only→doesn't perturb Mode-11 counts). Mode-11 wrap `solve_transpose`(>0)+`solve`(==0), routing-mutation reds both. Predicate flip is SURGICAL (`InverseOperator`/`GreenOperator` STAY False, no override) and **MUST propagate to the capability-survival CONTRACT** — Part-A flip BROKE `test_capability_survival.py::…keystone_v2_sn` row `((L+C).inverse(),True,False,…)`→`True` (report as required Part-A completion; single-file gate session can't fix it). Terminal wiring-proof of the reverse-scan chain (L13→L17 vocab, L18 carrier); `vv §Mode-11/12 / L11 / L1`. L20 STRUCTURE-ONLY ABC re-parent + stringly→typed-key `FaceLayout[K]` generalization (bit-identical, `facefield_codim1_design.md` §5 C2): CRUX=MULTI-INDEX SYNTHETIC fixture — the shared flat-buffer layout is exercised in PRODUCTION at ONE key-index (sphere=1 seed level→`(2·pos+sign_pos)·per_sign` DEAD at pos=0 in every pin), so the generalized level-index term is never activated; `for_levels((0,2,5),…)` forces pos∈{0,1,2}, load-bearing mutation `2*pos→pos` REDs multi-level/GREEN single-level (asymmetry=config-blindness evidence, sharpens L1/L11); golden HAND-COMPUTED not SUT-captured, pin `.offset`+cells/corner slice start/stop AND slice CONTENTS via `np.arange` fingerprint (transpose-blind to bare shape). Metric=REGRESSION-GUARD not new gate ("structure-only, NO metric on the ABC")→keep commit-1 object-level V_cell suite (`test_shipped_metric_block_values` atol=0.0, `TestA4`, trace 0-ULP) UNMODIFIED+green, ADD ONE ERR-067 tripwire: an ABC uniform-face-metric mutation (`(1−µ²)·w≡0` at pole=the ghost) REDs byte-exact V_cell; pole gates Mode-12-safe ∵ assert weight ARRAY not scalar, nonzero-seed+control-twin (L18). MRO=SIBLING-NOT-CHILD (`isinstance(sd,BoundaryField) is False`+`isinstance(bnd,StartingDirectionField) is False` cross-slot guard for `full_field.py:274/280`), break mode=collapsing the INTERMEDIATE name (`BoundaryField`) INTO the parent breaks `isinstance(x,BoundaryField)`; +`P in mro`+exported+`isinstance(x,Field)` stays True+`__post_init__` flat-buffer validation fires EXACTLY-ONCE up the super() chain. `to_flat/from_flat` bit-id BY CONSTRUCTION iff offsets unchanged (reads `.values`)→offset gate discharges round-trip. 2 new files (`test_face_layout_typed_key.py`+`test_facefield_hierarchy.py`); `vv §Mode-12 / L11 / L1`.

## 2. Active / in-flight verification work

- **Coupled Block Operator campaign — Phase-C step-5 (#41) "THE BLOCK SOLVE" gate spec DELIVERED (branch `refactor/sn-walk-unification` @ `d881243d`, this dispatch; step 4 + 4e ALL LANDED on-branch — 4e CLOSED @ `9c66fbe6`, wall 6340/0·37xf).**
  [step-5 solve verification](coupled_operator_step5_solve_verification.md) — pose the resolvent block-triangular DIRECT + scattering ITERATIVE + the ρ-honest block residual `r=Aψ−q` via `evaluate_residual` (solver.py:240) as the STOP test, not `‖Δψ‖`. **HEADLINE FINDING:** `grid.is_invertible` in the dormant xfail (`test_a2a_grid_swap_law_inverse_arm`, coherence :659) refers to the LOSS grid `A=system.loss` (the FULL 2×2 `CoupledOperator`), invertible via the **materialize/LU branch (`MatrixInverseOperator`)** — NOT the resolvent M (which is ALREADY invertible, `CoupledInvertibleOperator.is_invertible=True` :444). TWO distinct solves: **M** re-poses as the honest upper-triangular grid `[[LC,Seeding],[None,A_BB]]` (emission LAGGED into N → (B,A)=None), `M.solve`=block back-substitution (`ψ_B=A_BB.solve(q_B); ψ_A=LC.solve(q_A−Seeding·ψ_B)`) — the per-iteration SI step; **A** is FULL 2×2, `A.inverse()=MatrixInverseOperator(A)` — the whole-system EXTRACT-oracle + swap-law-inverse-arm solve. Step 5 flips `CoupledOperator.is_invertible`→xfail goes LIVE, REQUIRING the NET-NEW `MatrixInverseOperator.apply_transpose` (LUᵀ `trans=1`, zero negative tests today) so `grid.inverse()` is adjointable (`_AdjointOperator.is_invertible` clause-2, operator.py:1251). LOAD-BEARING: **row-6 dense-LU oracle** (`M.solve≡np.linalg.solve(dense(M))` ~5.5e-16, psi_half :391 — the substitution re-points onto it, principled-equiv not bit-id, M-M row) / **the in-M-lag CERTIFICATE** (the free-identity `r=N·Δψ` is BLIND to in-M lag BY CONSTRUCTION — #282's seed-lag was IN M — the end-of-solve `evaluate_residual(A,ψ,q)` is the ONLY catcher; NOT subsumed by row-6 which is tautology-blind to a lag on both sides) / **φ=Q/Σ_t + k_eff anchors** (the operator-is-correct claim vs reduction-tree-independence) / **`MatrixInverseOperator.apply_transpose`** (Mode-12-asymmetric — `trans=1`-forget REDs asymmetric/GREENs symmetric). STOP-TEST RE-BASELINE LEDGER (a deliverable): the D5 stop change re-interprets `tol` → frozen k-snapshots (`test_dd_regression:100`) move ~inner_tol-scale (solver.py:835-837 documents EXACTLY this) → RE-BASELINE against the closed-form anchor; the ρ=c convergence-rate RATIOS (`test_si_convergence_rate`, relative n_inner) are ROBUST (re-derive `n_predicted`, don't re-baseline). PENDING USER RULINGS spec'd BOTH ways: **D2a** retire `CoupledInvertibleOperator`/`CoupledSweepOperator` (M IS a numerics CoupledOperator — RECOMMEND retire) vs keep SN facades; **D3** route `M.apply` through the block matvec (re-baseline sphere fwd_bulk ~5.5e-16) vs keep fused (RECOMMEND keep under H1-narrow); **D5** free-identity+certificate (RECOMMEND) vs honest-per-iteration, scope coupled-vs-all, ‖q‖-vs-‖ψ‖ normalization. Governing memos same dir: step4 (M1-M5 machinery/assemble≡probe/A2 reciprocity), 4e (survivors ledger/row-6/L18 cold-residual). Awaiting user rulings + 5a-5e carve.
- **Diffusion integration #290 (pre-carve plan DELIVERED @ `d2a2a0c`).**
  [Verification plan](diffusion_integration_290_verification_plan.md) — the 7-deliverable spec for
  replacing `orpheus/diffusion/` with an in-algebra operator on MaterialMesh+ScalarFlux. CRUX
  (arithmetic-verified): a `SigS1=0, SigT=transport` Mixture reproduces legacy D BIT-IDENTICALLY →
  references DON'T move → all L1 anchors survive. 3 USER RULINGS: vacuum-BC semantics
  (keep zero-flux Dirichlet vs Marshak #182), data-seam bit-id vs re-baseline, Richardson retirement
  (H4 self-referencing → DELETE). Mode-12: k=λ_max(A⁻¹F) is similarity+transpose blind → object gate
  `A_diff.as_matrix()≡FD-stencil` + MMS flux-shape (#93, override the single-group/constant-D trap).
  Awaiting carve.
- **A3 / #280 Phase-2.5 walk-unification (branch `refactor/sn-walk-unification` @ `dec09ae`; S0+2.5a+2.5d+2.5b LANDED on-branch; 2.5c gate file AUTHORED 2026-07-05, this dispatch).**
  Gate spec = `.claude/plans/a3_solve_transpose_verification.md` (§§9–15 reverse-scan/apply-loop;
  §16 = 2.5d route-(a); §13 line 627 = the 2.5c gate list). Distillation [a3-reverse-scan](a3_reverse_scan_transpose_verification.md);
  the swap-law gate-design recipe = lessons L19. Ordering per git evolved: S0✓→2.5a✓→2.5d✓→**2.5b cyl reverse-scan LANDED @ `f1ddeb6`**→**2.5c NOW**→2.5e.
  **2.5c DELIVERED (this dispatch):** `tests/sn/operators/test_inverse_adjoint_coherence.py` — Gate 1 forward-matvec
  G-reciprocity (structurally indep of reverse-scan) + Gate 2 swap-law `array_equal` (object identity) + Gate 3
  round-trip + Mode-11 wrap sentinel (solve_transpose>0 / solve==0) + M-ADJ-swap/M-ADJ-metric mutations + predicate
  flips + `assert_type` static pin. **19/19 green under `-O`, pyright 0-error, every mutation RED-verified** (helpers
  reused from `test_loss_transpose_solve.py`; Part-A production wiring `operator.py`+`sweep_operator.py` landed
  UNCOMMITTED). TWO findings reported to main agent: (1) **the M-ADJ-metric "slab stays GREEN" spec prediction is
  FALSE** — the composite metric is non-trivial on every geometry, reds slab too (0.33) → gate asserts RED on all
  three (L19). (2) **REQUIRED Part-A completion (TWO forward-canary reds, same flip):** the
  `SweepOperator.is_adjointable` False→True flip BROKE (a) `test_capability_survival.py::…keystone_v2_sn` — row
  `((L+C).inverse(), True, False, INVERTIBLE)` needs `adj False→True`; (b) `test_inverse_operator_equivalence.py::
  test_inverse_returns_sweep_operator_surface:119` — `assert inv.is_invertible is True and inv.is_adjointable is
  False` needs the last `is False→is True`. Blast radius BROADER than one contract table (grep ALL
  `is_adjointable is False`/`adj=False` SweepOperator assertions). Rest of operators+sweep GREEN (1332 passed);
  single-file dispatch scope forbids the fix.
- **None else (SN).** Every SN campaign whose closeout this memory once tracked
  (#206/#208/#236/#240/#247/#251/#257/#18/#19/#20) is MERGED to `main`
  (verified: #257 branch deleted, its hashes ancestors of HEAD; the #236
  separability gate `test_space_angle_separability.py` is on main). The
  `feature/sn-spatial-angular-product` branch is 30 commits ahead with
  its own "#236 complete" closeout — finished work awaiting an ff-merge,
  NOT open verification work. Before resuming any branch claim,
  re-verify with `git merge-base --is-ancestor <hash> HEAD`.

## 3. Durable reference (reusable verification-design recipes)

These hold a reusable RECIPE or are cited by name from `AGENT.md` — not
campaign logs. Their core lessons are in `lessons.md`; these files keep
the full worked method.

- [Convergence-RATE verification](si_convergence_rate_verification.md) — cited by AGENT.md §5. Verify an iterations-to-converge / acceleration claim vs the analytic SI spectral radius ρ=c; measurand `history.n_inner`; the still-OPEN eigenvalue-path `n_inner=None` API gap; rate-claims are flux-shape-independent → 1G-OK (declare the claim layer).
- [Snapshot migration when production goes BARE](snapshot_migration_when_production_goes_bare.md) — cited by AGENT.md §7. Shared-driver SoT, schema=persisted∩compared, VACUUM-bit-id correctness gate, snapshot-inheritance-needs-anchor, false-`@catches` retirement, term-activation re-verify after an inject.
- [SN sentinel (canary) harness](sn_sentinel_harness.md) — `@pytest.mark.sentinel` one-cheap-test-per-capability-node design; cosmic-ray mutation-validation recipe (`local` distributor, `git checkout` after every run); the per-NODE-sentinel-leaves-module-interior-uncovered gap-closer.
- [SOTP separability verification](sotp_separability_verification.md) — the SOTP master condition (separable ⟺ Cartesian-product per-axis; coupled physics → OperatorSum fallback); how to gate an `assert_separable` claim; Route-A array_equal vs Route-B nulp bound; slab is the degenerate case.
- [Operator space-guard only bites OperatorSum](operator_space_guard_only_bites_operatorsum.md) — the `OperatorSum`/`Product` domain/codomain guard is INVISIBLE to the SI/Krylov matvec (`L.apply−Σgᵢ.apply`, no OperatorSum; drivers validate only capabilities); it bites only on actually-composed sums (`InvertibleOperator`, `evaluate_residual`'s `LC-S-B`). `FunctionSpace.__eq__` by `(name,shape)`; activation-gate targets the composed sum, blocker = a new space name. (Banked from the P4.5b/c verification plan.)
- [Cross-layer relocation carve verification](cross_layer_relocation_carve_verification.md) — verifying "relocate a method-agnostic unit DOWN a layer + route hardcoded dispatch through a registry". H1 registration-timing regression MASKED by process-global registry state → fresh-process subprocess gate is mandatory (same-process = vacuous, Mode-11-adjacent). H2 verbatim move TRIPS `test_layer_imports.py` via a `TYPE_CHECKING` sn import (INPUT pkgs are NOT TC-exempt) → drop it, retype param `Any`. The "layer inversion" is usually documentation-only at runtime (`sys.modules` diff empty) — refute at design time. (Banked from the `realize_recursively` move spec.)
- [A3/#280 reverse-scan transpose-solve verification](a3_reverse_scan_transpose_verification.md) — the reverse-DAG transpose-solve (`SweepOperator.apply_transpose`, #280's 4th face) + the 2.5a apply-loop unification. Operator-vocab drift reconciliation (retired `CAP_*`→typed predicates); the assembled-Mᵀ oracle (Cartesian-only, LAPACK back-sub) vs the dense-apply SPHERE keystone; self-referential-array_equal→frozen-baseline; the 1-D loop spy + orientation-OBJECT AST tripwire; the reverse-SCAN-vs-reverse-LOOP new modes; scaffold order (G3 pre-carve) + #282-fix conditional. **§7 = the CYLINDER arm (2.5b-cyl, task #28):** ONE mandatory config = `product(n_mu=4,n_phi=8)` cyl (LS NULLS BOTH hard terms — 8-vs-0 degenerate ords AND `mm_a_in_coeff[m0]` 1.0-vs-0.0 seed-fold M-M → §0.6 double-blindness, LS=control); gates G1 round-trip / **G2 dense-Mᵀ keystone** (NO assembled-Mᵀ curvilinear) / **G3 full-field reciprocity** (validated 1.8e-16 sphere, bulk-only fails 42% → covers the #284 `m_boundary` slot G1/G2-bulk miss) / G4 m_seed=None+R12a-refuse / G5 activation-sentinel; mutation matrix (a seed-fold M-M misroute / b degenerate-DROP=ERR-066 LOAD-BEARING / c level-leak / d pole-mirror / e m_seed) with (a)/(b) product-RED/LS-GREEN asymmetry AS the config-blindness evidence; file=`tests/sn/operators/test_loss_transpose_solve.py` (a3-spec `tests/sn/sweep/` path stale). (Banked from the Phase-2.5 spec extension.)
- [A_BA ψ½ Schur-fold un-weld verification](aba_schur_fold_unweld_verification.md) — lessons L22. Gating a WELDED-fold un-weld (a per-cell/level closure hand-rolled at N sites → ONE source; Coupled Block Operator Step 2). 7 gate types: manufactured-anisotropic fold contract (ℓ≥1; iso-null asymmetry = necessity), Mode-11 wrap-counter EXACT `2·n_levels` (seed arms local-import → source-module monkeypatch IS picked up), bit-identity INHERITS + independent `½·emission` closed form, TWO transpose gates (helper `⟨fold,y⟩=⟨m,fold_T y⟩` + operator fwd↔adj consistency blind-to-shared-coeff; hand-rolled `0.5`=coeff[0]), F-fissile-non-vacuity (#4), cyl/slab None-ray CONTROL (#6). `# BIND:` isolates the undecided A_BA surface → direct rows xfail(strict=False). 7 live + 3 xfail @ `test_psi_half_coupling.py::TestA_BA_SchurFold`.
- [CoupledOperator Step-4 verification](coupled_operator_step4_verification.md) — Coupled Block Operator Step 4 **RE-SCOPED 2026-07-10** (N-general semantics-agnostic block machinery; ψ½ = instance #1; SUPERSEDES bespoke-2×2 — `OperatorSum`+present-zero REJECTED, keeps wrong-mult representable; honest object = typed block op over typed block vector, Pattern-1∘4). FOUR landings, bars split: **4d.0** `FullField`→`System[Interior,Boundary]` generic = L20 structure-only carrier (System A ≡ today, per-consumer VALUE bit-id `array_equal`/nulp NOT proxy ERR-063; CRUX = the multi-instantiation synthetic — production exercises the generic at ONE leaf-type, so MANUFACTURE `System[ScalarFlux,ScalarBoundaryFlux]`, hardcoded-`AngularFlux` mutation reds scalar/greens angular = config-blindness evidence; metric=regression-guard+ERR-067 tripwire; MRO sibling-not-child, TimedFullField stays subclass, break=collapse the FullField name). **4d.1** CoupledField+CoupledOperator (numerics, synthetic toy blocks) = the LOAD-BEARING **assemble≡probe** (`assemble().as_matrix()≡_dense(.apply)` principled-equiv `rtol=1e-11` NOT array_equal — L16 COO→CSR≠apply order; TAUTOLOGY trap=probe `.apply` NOT `.as_matrix` if it delegates; offset-swap tooth on ASYMMETRIC blocks Mode-12) + per-block type-safety NET-NEW teeth (`A_AB@x_A` raise `match=` + pyright) + block-`.H` Mode-12 (A2a composite-G extends `test_inverse_adjoint_coherence.py` `coupled` fixture, M-ADJ-metric reds ALL-3-geoms slab-green FALSE L19; A2b seed-isolated V_cell L18 nonzero-seed+control). **4d.2** `build_coupled_system`→(op,space) aligned; presence=block-existence (carrying→2×2/non-carrying→1×1) SUBSUMES step-6, 7 guards ZERO neg-tests→NET-NEW `match=`. **4d.3** block apply WRAPs fused walk (bit-id, Mode-11 route-sentinel) + E4 two-anchor Q/Σ_t+fissile k_inf. Carry-fwd (ray still FullField's 3rd block thru 4d.3): TestRegressionFloor/#284 oracle 5.5e-16, TestCoupledLift, TestSystemRoleLattice, TestA_AB (already leaf-typed). RE-POINT at 4d.2 (ray→System B): `_template`/`_blocks`/`_dense`/A_BA/B_b probes; floor triangularity re-points at 4e NOT 4d.0. Sibling of A_BA/A_AB/inverse_adjoint (L16/L18/L19/L20).
- [CoupledOperator B.2b re-type verification](coupled_operator_b2b_retype_verification.md) — Coupled Block Operator sub-step B.2b (executes step-4 P3): re-type A_BA (codomain) + B_b (domain=codomain) onto `RadialCharacteristicComposite` behind thin transient FullField→FullField gain adapters (production BIT-IDENTICAL). THE RULING: B.2b is a PURE RE-LABELING → `array_equal` EVERY value row, NO principled-equiv (a `rtol`/`nulp` in a B.2b gate = RED FLAG; drift enters at B.2d/4e). Three sub-commits. **b1** mint split SourceSink pair + re-bind composite static params flux→FIELD-BASES + ROLE-PRESERVING bridge: G-b1.3 CRUX = (role class)⊕(values) split, each blind to the other (hardcoded Flux-bridge PASSES value round-trip for ALL roles→ROLE assertion is the ONLY catcher; RED-SourceSink/GREEN-Flux asymmetry = §0.6 evidence); G-b1.4 static-param re-bind = pyright `reportArgumentType` gate (runtime guard already admits bases). **b2** System B member space = family-blind `FullFieldSpace.from_blocks(name=)` + `_rebuild` PRESENCE-DISPATCH (pass `radial_characteristic` kwarg iff carrier exposes slot; today unconditional→`AttributeError`/`TypeError` on 2-block): G-b2.1 metric-trio ≡ direct split-space on REAL composite (`inner_product` scalar Mode-12-blind→PAIR `apply_metric` array_equal; exercise BOTH dispatch branches L11; FullField byte-id tooth); coextensiveness `to_flat().size==prod(shape)` on REAL member. **b3** A_BA `apply`→RadialCharacteristicComposite(source members)/`apply_transpose`→FullField(rc=None); B_b domain=codomain→ray composite: P3 value-unmoved rows (`to_unified()`≡`_ba_oldloop_reference` bit-id INHERITANCE + type companions; old present-zero-disjointness now STRUCTURAL/unspellable Pattern-4); **G-b3.3 = THE adapter-delegation sentinel-teeth check** (isolated spy on `RadialCharacteristicEmission.apply` class-method, `adapter.apply(psi)`==1 → PROVES delegation-not-inline → keeps L4-S sentinel non-vacuous, Mode-11); G-b3.4 B_b.H recip through NEW composite space (Mode-12 CLOSED, control+2 teeth L18). Re-points: `TestB_b_RayBoundary`/`TestCoupledLift` L1-L3 read `.interior`/`.boundary` via bridge; isinstance gates→wraps-A_BA predicate; L4-S spy UNCHANGED (fires thru adapter); `test_tooth_a_ba_transpose_drop_reds`+`_full_loss_case`→adapter-wrapped; `TestRegressionFloor::test_bulk_to_ray_…`→`_dense(adapter.apply,tpl)` (`_dense(A_BA.apply)` breaks on hetero codomain). UNAFFECTED: `TestA_BA_SchurFold` (fold FACTOR not operator). Governed by coupled_operator_step4 (P3); sibling of aba_schur_fold/a_ab_seed_injection/A_BB-forward-extract.
- [A_AB seed-injection verification](a_ab_seed_injection_verification.md) — Coupled Block Operator Step 3: gating a CELL-LOCAL RECTANGULAR coupling op (domain=ray `cells(p,-1)`, codomain=bulk `AngularSourceSink`, σ-INDEPENDENT) extracted from the in-sweep M-M seed injection = the `A_bs=7.505` block of `(L+C)`. CRUX = the equivalence gates (fwd bit-id vs `(L+C).apply(FF(0,0,s)).bulk`; transpose bit-id vs `(L+C).apply_transpose(FF(v,0,0)).ray.cells(p,-1)`) SHARE the closure methods with production ⇒ BLIND to inner-method bugs (mutate `cell_contribution` → BOTH flip → GREEN); the ONE patchable CORRECTNESS cross-check is gate-3 Euclidean fwd↔transpose adjoint-consistency (flip lands on one side); the VALUE anchor RIDES the existing structurally-indep `test_radial_characteristic_metric::test_r1` (`T_sb=A_bsᵀ` vs numpy `Afwd.T`) + the regression floor. bulk=0 isolates A_AB by M-M LINEARITY (σ cancels → `array_equal` 0-ULP, ctor needs NO σ_t). Mode-11 sentinels: precompute==1 w/ `psi_view` ALL-ZERO + seed threaded, angular_adjoint==1 w/ `numer_bar==−v/V`. Gate-5 asymmetry = "reads ONLY cells(p,-1)" (full-seed≡cells(-1)-only-seed). Gate-3 EUCLIDEAN not metric `.H` (L19; Mode-12-safe). Sphere carries ONE level → multi-level UNTESTABLE (refutation, blind spot). DEFER-forward variant: gates 1/3/5a `xfail(strict=False)`; gate-3/5a FALLBACK uses `(L+C).apply` surrogate so the transpose stays fully verified. Sibling of A_BA.
- [A_BB forward shared-kernel EXTRACT verification](radial_characteristic_forward_extract_verification.md) — Coupled Block Operator Step 4b: complete `A_BB.apply`/`apply_transpose` via relocating `_seed_residual_march`+extracting `radial_characteristic_forward_residual`→`psi_half_angle_seed.py` (walk `_seed_rows_forward/_transpose`→thin wrappers, `A_BB.apply` wraps SAME fn, zero twin), flip `is_invertible`/`is_adjointable` True + `inverse()` involution. 4 refutations: (1) round-trip PRINCIPLED-EQUIV ~3 ULP NOT 0-ULP (MEASURED 6.66e-16; `dr·(2/dr)≠2` reassoc; ONLY outflow-corner bit-exact)→cells `rtol=1e-11`/corner `array_equal`; (2) `solve∘apply=id` ONLY on CONSISTENT subspace (+1 corner=free datum apply-measures/solve-overwrites→build ψ=solve(q0) or test `apply∘solve` on physical q); (3) transpose seam: walk method=shared_fn **+ seed_cells_bar A_AB term**, `A_BB.apply_transpose`=shared ONLY (feed empty seed_cells_bar); (4) EUCLIDEAN not V_cell metric (L19 composite's job). Gate 1b Mode-11 anti-twin routing sentinel (leftover-copy=false-green). Predicate-flip contract EMPTY of assertions (A_BB not composed, not in capability-survival)→additive-safe + doc-drift(`radial_characteristic.py:53-100/:175/:260-281`)+net-new positive pins. Sibling of coupled_operator_step4 + a_ab_seed_injection.

## 4. Durable design idioms (feedback)

- [Regression tolerance design](feedback_regression_tolerance_design.md) — `assert_regression`: iterative→`SAFETY(10)×conv_tol` off the run-config SoT, direct→`nulp(reduction_depth)`; `DriftWarning` tripwire; `-O`-safe.
- [Eigen on non-fissile mixture is malformed](feedback_eigen_on_nonfissile_mixture.md) — k=0/abs→nan dead gate; reformulate as fixed-source; corroborate vs flat inf-medium `(diagΣ_t−Σ_s0ᵀ)⁻¹Q`.
- [Diagnostic→test promotion](feedback_diagnostic_promotion.md) — verify-diag-runs-first; reproduce pins via public API; 3 foundation classes; warning suppression; delete-after-pass. (Policy SoT: `tests/derivations/_promotion_policy.md`.)
- [V&V tagging idioms](feedback_vv_tagging.md) — module `pytestmark` vs per-test `verifies()`; foundation carries NO `verifies()`; xfail `strict=False`+`reason=`.
- [Cross-method protocol design](feedback_cross_method_protocol.md) — reuse registry schema (thin wrapper); `max(tol_a,tol_b)` agreement; L1-not-L4 convention; verify truth values vs literature memos first.
