# Test-Architect Memory Index

Read `lessons.md` at the START of every invocation. Durable verification
principles live in the `feedback_*` notes + the carve-lesson notes. The
committed suite records WHAT is verified and these notes keep WHY — and
several plan notes are themselves cited as the verification-rationale of
record by committed tests + `docs/theory/operator_algebra.rst`, so they are
load-bearing **provenance**, NOT redundant scaffolding. Do not delete
committed brain; hygiene the uncommitted working notes.

## Durable verification principles (read when designing a plan)

- [Test Architect lessons](lessons.md) — L1 homogeneous-blind-to-curvilinear; L2 walk the analytical-references checklist; L3 no mesh-independent transport eigenvalue reference (Issue #8).
- [Regression tolerance design](feedback_regression_tolerance_design.md) — `assert_regression`: iterative→`SAFETY(10)×conv_tol` read off run-config (SoT), direct→`nulp(reduction_depth)`; `DriftWarning` tripwire; `-O`-safe (no bare assert).
- [Eigen on non-fissile mixture is malformed](feedback_eigen_on_nonfissile_mixture.md) — k=prod/abs=0/0→nan dead gate; reformulate as fixed-source (same Pℓ/streaming path); corroborate vs flat inf-medium `(diagΣ_t−Σ_s0ᵀ)⁻¹Q`.
- [Diagnostic→test promotion](feedback_diagnostic_promotion.md) — verify-diag-runs-first; reproduce pins via public API; 3 foundation classes (invariant/negative-regression/math-origin); warning suppression; delete-after-pass.
- [V&V tagging idioms](feedback_vv_tagging.md) — module `pytestmark` vs per-test `verifies()`; foundation NO `verifies()`; xfail `strict=False`+`reason=` pointing at unlocking API/label.
- [Cross-method protocol design](feedback_cross_method_protocol.md) — reuse registry schema (thin `CrossMethodCase`); `max(tol_a,tol_b)` agreement; L1-not-L4 convention; verify truth values vs literature memos first.

## SN cell_update-seam carve verification (IN FLIGHT, #206, branch refactor/sn-cellupdate-seam-slab)

- [Issue #206 cell_update-seam verification](issue_206_cellupdate_seam_verification.md) — 3-phase 1-D operator-algebra carve (A route closures through `cell_update`, B extract 1-D-scan frame, C move 1-D matvec off operator = 1-D S6.3b). ⭐ THE `--capture-baseline` random-ψ bit-id gate ALREADY EXISTS (`test_bc_extraction_matvec.py::TestVacuumMatvecBitIdentity`, flag in ROOT conftest — re-use not rebuild). PhaseA/B = strict bit-id via `-W error::DriftWarning` on the 1-D `test_dd_regression`+sha256 (`test_affine_carve_bit_identity::si_slab_2g_het`); A-NEW = heterogeneous NON-flat random-ψ single-sweep+matvec capture (flat ψ NULLS curvilinear `dA_w·c_out` — vv §H2). PhaseC four-leg: leg1=`test_phase_c_crosscheck` (trajectory_resolvent Variant-α + kinf_homogeneous, structurally-indep); leg2=Q/Σt + dense-probe (`diag_p42_adjoint_oracle`); leg3=`test_loss_action_convention`/`test_streaming_operator_decomposition`/`test_sweep_vs_apply_consistency` (the TWIN); leg4=refinement. Adjoint leg = `test_g_adjoint_reciprocity` (sphere/cyl, `pytest.fail` -O-SAFE, paired L11 neg control). L20 migration = DOCSTRING-ONLY (matvec already privatised behind apply/loss_action; loss_action call-sites move intra-production). L17 crosswalk: (L+C)-not-L Resolution-A `−C`-glue-at-operator; WDD inline `2ψ−in` ≡ `outgoing_face_from_average` (THE Phase-A bridge); boundary outflow-defect KEPT; Carlson mirror seed crosses (ERR-058, flat-ψ-invisible). Mode-8: `test_flat_flux_per_ordinate_balance_no_pole_spike` + `test_streaming_operator.py` bare-asserts INERT under -O → run those WITHOUT -O. Route-around reds: `test_sphere_{1g,2g}_apply_bit_identical` (stale post-ERR-058, `-k not`), #212 keff_slab hang (`--deselect`).
- [Issue #206 PHASE C matvec relocation](issue_206_phase_c_verification.md) — ⭐ Phase-C-specific gate set (re-verified live @ HEAD 03d5c02; supersedes the PHASE-C section of the seam note, whose anchors drifted). THE CRUX = density-vs-scan denom grouping: matvec `(denom·ψ−numer)/V` (`cell_balance.py:247`) and scan `2·QV·inverse_denom` (`diamond.py:460-461`) build `denom` SAME grouping → DENSITY option = strict bit-id (RECOMMENDED), SCAN option = nULP `≤nx` (the `kind=direct reduction_depth=nx` A-NEW matvec leg ALREADY absorbs it). Per-leg verdict: **legs 1+4 GREEN-but-BLIND to the matvec** (SWEEP/`solve_sn` only — never call `(L+C).apply`); matvec pinned by leg-3 TWIN (`test_loss_action_convention` -O-safe) + Krylov-vs-SI (`test_sweep_vs_apply_consistency`). ⚠ leg-2 transpose dense-probe oracle `diag_p42_adjoint_oracle.py` DELETED — adjoint now SOLE-covered by `test_g_adjoint_reciprocity` (the ONLY -O-firing curvilinear `angular_adjoint` gate, +L11 neg control). ⭐⭐ §3 the ONLY Phase-C-unique hazard: `_compute_decomposition` (`operator.py:774`, 3 live consumers `:269/:1158/:1276`) is the BYTE-TWIN of `_compute_LpC` — relocating one and not the other RE-CREATES the twin path; gate = decomposition invariant `TestT4cAlgebraDecompositionInvariant*` + recommend single-source tripwire `M_spatial+M_angular_redist==(L+C)` array_equal. Degenerate-cyl branch = NO scan analogue (matvec `A_down=0` direct, sweep slow `cell_update.update`) → sharpest catcher = A-NEW matvec[CYL] under `level_symmetric`. Mode-8: `bc_extraction_matvec:477` pole-spike + decomposition `:180` bare-assert INERT under -O → run WITHOUT -O (recommend migrate `:477`→`np.testing` at touch). FINDING-1: `-W error::DriftWarning` escalates ONLY in sweep/core + solve (qualified path). Live baseline: sweep/core 445p/1s/4xf strict-clean; fast four-leg 122p/3skip/3desel; full four-leg green (exit 0). Route-arounds: `-k "not (vacuum_bulk_bit_identical_1d and SPH)"`, `-k "not (sphere_{1,2}g_apply_bit_identical)"`, `--deselect …test_heterogeneous_absolute_keff` (#212). Extends [[issue-206-cellupdate-seam-verification]].

## LD spatial scheme + dispatch-seam verification (PRE-IMPL, #158)

- [Issue #158 LD spatial + seam verification](issue_158_ld_spatial_verification.md) — pre-impl plan for `LinearDiscontinuous` (`is_affine_scannable=False`) + `OneDimPerCellWalk` (`supports = is_1d AND NOT is_affine_scannable` — fills the GAP: a 1-D non-scannable mesh is admitted by NOTHING today → `default_for` would raise). 5 gates instantiate L14 four-leg: G1 per-cell round-trip (mirror `test_diamond.py::TestResidual`, foundation, feed FULL solved state back — LD couples 2 moments); G2 seam-control = DD-via-`OneDimPerCellWalk` ≡ DD-via-`CumprodScan` at **`nulp=nx`** (principled-equiv NOT array_equal: sequential loop vs parallel-prefix cumprod, drift=reduction-depth·ULP, #206 Phase-C precedent) + mutual-exclusion (anti-pattern #11); G3 slab MMS O(h²) with **ansatz-bias OVERRIDE** (`[a0+a1·sin(πx/L)+a2·cos(3πx/L)+μ·(b0+b1·sin(2πx/L))]/W`, **a0>0** non-vanish-at-bndry, mixed k=1+k=3 scales, μ-non-trivial, 2G het Σ_t — the universal `sin(πx/L)` is Mode-7 simplification bias) + value-band (rate≠correctness vv§5); G4 = the EXISTING #233 pole gate ([[w2_pole_cell_characterization_gate]], LD-ready by `>0.8` lower-bound), sphere lift DEFERRED (#235 angular floor co-limit, staged xfail-non-strict, do NOT hinge on it); G5 DD bit-id = `pytest tests/sn/sweep/core tests/sn/solve -W "error::…DriftWarning"` (NOT regression/ — conftest forces `always`, inerts `-W error`; live 505p/1s/4xf). ⭐ DRIVER GAP: `solve_sn_fixed_source` builds `SNMesh(...)` with NO cell_update → always DD; thread `cell_update` kwarg (skeleton constructs SNMesh+SNSolver directly until then). L17 crosswalk = 7 rows (`/W` at PRODUCER; direction-resolution at FRAME not cell-op; `(L+C)ψ` glue at OPERATOR). Mode-8 verified: DD round-trip fires under -O (np.testing). Extends [[issue_206_cellupdate_seam_verification]] + [[issue_206_phase_c_verification]].

## Curvilinear anisotropic-scattering verification (IN FLIGHT, #229 + #9 + W1 + W2)

- [W2 pole-cell characterization gate (#233)](w2_pole_cell_characterization_gate.md) — permanent L∞/per-cell-rate CHARACTERIZATION gate for the **spatial** central-cell closure `dd-curvilinear-scalar` (DISTINCT from W1 τ-clamp + #229 angular-thread). ⭐ Design pattern: GUARANTEE tests (`verifies`) = global vol-wt L2 O(h²) under BOTH midpoint AND Hébert-3.430 shell-volume-average (structurally-indep cross-check proving L2 order is real not midpoint artifact); CHARACTERIZATION tests (NO `verifies`) = pole L∞ order bounded BELOW only (`>0.8`) + L∞-dominant + interior O(h²) → NO upper bound so a future LD/nodal fix stays GREEN (vv anti-pattern #5/#17). Headline = L∞(pole)=O(h) COEXISTS with L2(global)=O(h²) via √V~h^1.5 dilution. Cyl shares IDENTICAL defect MASKED by midpoint (pole-vs-volavg O(h), pole-vs-midpt accidentally O(h²)). Mode-8 verified live (bare-assert FIRES under -O in collected test fns). 4 tests GREEN under -O, zero solver code.
- [W1 sphere τ-clamp removal verification](w1_sphere_clamp_removal_verification.md) — gate set for `tau_mm=tau_raw` (sphere only; cyl keeps clamp). ⭐ REFUTES 2 program-plan premises: iso NOT bit-id at IEEE-754 (closure routes through τ-bearing reduction → exact-arith only; gate = closure-unit few-ULP + converged FP-tail<1e-9, NOT bit-id); aniso O(h²) NOT clean at S16 (floor is #229-INTERPOLATION untouched by W1; needs S32; W1 only cleans the coarse RATE 1.978→1.995 → S16 discriminator staged xfail-strict flips on W1). G5: ONLY `sphere_2g_3reg` regens (Δk2.35e-4); the 2 FLAT sphere cases (homog-reflective + p1_aniso) FP-tail-only, pass existing gate. `hash()`-salted-useless-for-snapshot-diff.
- [Curvilinear aniso #229+#9 spec](curvilinear_aniso_229_9_verification.md) — TWO-unrelated-aniso-paths split (path-I geometric redistribution P0-ansatz vs path-II P1 Legendre `R·Λ·M`). #229: SPHERE has pre-floor O(h²) window (S16 nx{10,20,40} orders 1.979/1.978, assert `min(orders[:2])>1.9` COARSE seg NOT `[-2:]`) but CYLINDER does NOT at any quad (floor dominates → ABANDON cyl spatial claim; floor IS verified via `err(2×quad)<err/2.0` scaling). All 5 xfails removed; 6 eq-labels migrate to green; wide-band ERR-026 re-floor catcher (cyl 1e-3<err<5e-2). #9: L0 operator-admits P1-source-WITHOUT-MMS-source (`S₁.apply−S₀.apply` per-ord ≡ hand-ref, VALIDATED 3.9e-15/6.2e-15, 1g OK=flux-shape claim); L1 P1-LOWERS-keff (leakage-monotone neg control, het Δ=−1.40e-2); L2 subsumed. KEEP σ_s1 reserved. Phase-0 = 4-D→3-D index fix.

## SN operator-algebra carve lessons (LANDED #208/Wave-O/Wave-T/Phase 4-5; tests live)

- [Issue #208 Wave-O carve lessons](issue_208_wave_o_carve_lessons.md) — snapshot-on-random-ψ + Q/Σt two-anchor; `--capture-baseline` ROOT-conftest gotcha; defect-vs-raw zero-input precision boundary; affine-torsor gate; sha256-golden bit-id; residual-before-adjoint; metric-blind reciprocity.
- [Wavefront-flux carve lessons](wavefront_flux_carve_lessons.md) — storage-A/B; inherited-octant-snapshot (interior edges ephemeral→outputs+boundary pinned); peak-pin Leg-1(size-ratio sharp) vs Leg-2(tracemalloc loose); nulp-einsum vs array_equal-typed-wrapper; SHED/CAPTURE highest-risk.
- [Phase 5a/5c angular-windowing lessons](phase5a_5c_angular_windowing_lessons.md) — THE TRAP (all 2-D snapshots P0-isotropic→a φ_ℓ≥1-dropping carve passes the whole suite); moment-tensor fuller-view oracle catches ℓ/m drift (Mode-5) scalar is blind to; nulp(n_diag) relaxation.
- [Phase 4.6 non-vacuum MMS ansatz](phase4_46_nonvacuum_mms_ansatz.md) — prior MMS all VANISH at boundary→q.boundary≠0 untested; `(A+μB)/W` with A=a0+a1·sin, **a0>0 load-bearing**; converged-VALUE-not-just-O(h²)-rate; sphere companion mandatory; G-4 reflective-mesh-not-kwarg dud.
- [SOTP separability verification](sotp_separability_verification.md) — master condition: SOTP needs Cartesian-product per-axis; coupled physics (per-material XS, M-M curvilinear recurrence) NOT SOTP-able→OperatorSum fallback; Route A array_equal vs Route B nulp; slab degenerate-separable hides curvilinear bugs.
- [SI convergence-rate verification](si_convergence_rate_verification.md) — rate≠value test-design; measurand `history.n_inner` (eigenvalue path =None, OPEN gap); target = analytic ρ_J=c; gate `<0.75×baseline`+value guards; 1G-OK for rate (flux-shape-indep by design).
- [Snapshot migration when production goes bare](snapshot_migration_when_production_goes_bare.md) — reusable recipe: shared-driver SoT; schema=persisted∩compared; vacuum-bit-id correctness gate; inheritance-from-bare needs closed-form anchor; retire false `@catches`; re-verify term activation.

## N-D layout carve verification (IN FLIGHT, branch worktree-sn-nd-layout)

- [C3 dim-generic sweep-DAG verification](c3_dim_generic_sweep_dag_verification.md) — `from_cartesian_2d→from_cartesian(d)` + 3-axis diamond + `product((-1,+1),d)` octants. d=2 moat = hand-loop oracle `TestApplyMatchesLegacyInlined` (non-square, -O-safe); GAP = x↔y swap blind in SQUARE octant-snapshot (closed by non-square DD-regression + new pin); synthetic-3-D exact counts `Σ(n−1)+1` levels / `2^d` octants / `i+j+k=ℓ`; standing reds all `xfail` #206/#195/#212(deselect), #214 CLOSED.
- [SweepStrategy carve verification](sweep_strategy_carve_verification.md) — S0 plan for C3.4+C3.5 polymorphic sweep/matvec dispatch. `reduced⟺is_1d` coincidence pin (verified T/T/T/F); `is_cartesian` orthogonal (key on BOTH is_cartesian+ndim); A2D-1 WRAP-not-relocate; Mode-8 LIVE hazard in `test_unified_sweep_dispatch.py` (S1 migrate+fix); 2-D adjoint deferral gate; synthetic-d3 `frontier_dim=d−1` admission via `_walk_roundtrip_residual`; cyl needs LS quad.
- [Scan-march verification (S5/#222)](scan_march_verification.md) — S5 scan-march unification (schedule×backend; row-march+x-scan reuses `ordinate_scan` per line). G2 scan-march≡FullFieldWavefront@nulp(128) d1+d2 (NON-bit-id across schedules), G3 conditioning, G4 Mode-9 off-degenerate-box + ERR-056 shed-order, G5 snapshot-regenerate Fork-B2-only, G7 synthetic-d3 + (d−1)-slab mem. ⭐ DEMONSTRATED LIVE PRODUCTION BUG: `ordinate_scan` denormal-cumprod NaN leak (guard tests `==0.0`, misses denormal `cumprod[-1]=1e-312` → `b/cumprod`=inf → NaN; `a=0.1,nx=312`). 3 decision forks flagged (ScanMarch-new-vs-CumprodScan-becomes-d1; default-flip-vs-opt-in; coeff cache placement).
- [S6 re-layering verification (#222 capstone)](s6_relayering_verification.md) — operator/representation re-layering. SweepStrategy→SpatialRepresentation; residual→loss_action returns `(L+C)ψ`, operator does `−C` glue (verified `operator.py:1483-1487`). ⭐ THE PAYOFF = the "one representation instance" `is`-test (concrete drop-in spec; FAILS today=two `default_for` doors `apply@1462`/`solve→transport_sweep@2530`, PASSES after S6.5; `pytest.fail` -O-safe; stage as `xfail`→xpass). A2D-1 REGENERATE-in-same-commit + pin OUTPUT via `window≡full` matvec oracle (NEVER stale source hash). loss_action `(L+C)ψ`-not-`L·ψ` pin (pure-z octant + zero-source cross-checks, ≥2G het). Curvilinear angular-transpose hazard rides `closure.angular_adjoint@727` — COVERED by `test_g_adjoint_reciprocity` sphere/cyl BUT bare-assert@212 inert under -O (Mode-8 fix prerequisite). `as_scipy_linop` Krylov round-trip transitively covered (`test_keff_2d::test_si_krylov_*` + `test_operator.py`). Per-stage table S6.2-S6.5.
- [S6.4 unified-walk + DAG-ownership verification (#222)](s6_4_unified_walk_verification.md) — the `_OctantWalk2D` carve (sweep≡matvec ONE octant frame, fork at CELL-KERNEL OBJECT not boolean + EMIT policy). S6.3b LANDED@`7bbeb5f` (walk off operator into `loss_representation.py`); S6.4 collapses 3 lockstep matvec frames (`MovingFrontierWindow`/`ScanMarch`/`FullFieldWavefront`.loss_action) + 2 sweep frames (`sweep_octant_group`/`_sweep_2d_scanmarch`) + 4× O.4b boundary block. ⭐ PAYOFF = one-walk SPY test (`L.apply`+`A.solve` both hit `_OctantWalk2D._interior_walk`; FAILS now=ImportError, xfail→xpass@(b)). ⭐ A2D-1 **RETIRE** recommendation (output `window≡full` MATVEC oracle supersedes the source-hash; not fuller-view-oracle exception). DAG-ownership: mesh-no-`sweep_graphs` `hasattr` gate + `geometry.py:343/350` None-slot GONE + cache byte-id. §6 anti-boolean source guard (Smell #3). NON-SQUARE 12×7/5×9 LOAD-BEARING (x↔y swap blind in square). Staging (a)matvec-share[A2D-1 fires]→(b)sweep-into-walk[SPY flips]→(c)DAG-move→(d)fold-oracle. Anchors collect `-O`-clean (39). Extends [[s6-relayering-verification]].
- [C3.6 honest d-dispatch verification (#220)](c3_6_honest_d_dispatch_verification.md) — the N-D campaign TAIL: (a) `sweep_schedule` d-generic `_octant_sweep(entry,ndim)`+`_outgoing_faces enumerate(signs)` closes the d=3 SILENT z-face-shed hole (ERR-056 EXISTENCE-variant: a +z octant never sheds `zmax` → reflective z-face gets no G-S group → wrong FP; DEMONSTRATED via pure helpers); (b) `SNMesh.streaming(axis)` d-generic = BIT-ID BY CONSTRUCTION (`mu_x IS axis_cosines(0)`, a property view — G-b3 EXACT `array_equal` pin guards the view-not-copy claim); (c) `ScanMarch.supports` narrow `is_1d or (is_cartesian and ndim==2)` → d=3 falls through to `FullFieldWavefront` (any-d spine). ⭐⭐ WORKING TREE IS MID-CARVE: (a) prod HALF-written, callers `:99,119` don't pass `ndim` (TypeError), 7 tests RED from shim removal. Synthetic-d3 seam = PURE helpers (entry duck-typed `.label`/`.indices`; `supports` mesh duck-typed `is_1d`/`is_cartesian`/`ndim` — NO SNMesh). Mode-8: schedule tests bare-assert INERT under -O (migration is the fix moment). F2 mixed-BC (x-refl/y-vac/z-refl) closes x↔y↔z swap. NO golden regeneration (task constraint). Extends [[scan-march-verification]] + [[c3-dim-generic-sweep-dag-verification]].
- [C4 SNMesh.bc-dict verification (#220)](c4_snmesh_bc_dict_verification.md) — the MESH-side BC inventory dim-generic (sibling to C3.6's DAG-side). NEW `face_name(FaceLabel)->str` crosswalk (`min→xmin`/`max→xmax`/**`outer→xmax`**/axis2→`zmin`) single-sourced near `FaceLabel`; `SNMesh.bc` → string-face-keyed DICT (keys == `boundary_face_layout.faces` == `{face_name(l) for l in face_labels}`); `_resolve_bcs` collapses the 1D-curv/1D-slab/2D isinstance split → ONE loop over `face_labels`; `_resolve_one(FaceLabel)` KILLS the `:523` `axis="y" if face in (ymin,ymax) else "x"` LATENT d=3 BUG (z-face would build x-perm — silent-wrong reflect-PARTNER, vv Mode-9, distinct mechanism from C3.6 shed); `boundary_face_layout` 3-branch→ONE `from_named_shapes` loop. RETIRE `bc_xmin/xmax/ymin/ymax/left/right` + 1-D y-placeholder block + 2D left/right aliases. ⭐ BYTE-ID PROVEN this session (crosswalk preserves `xmin,xmax,ymin,ymax` order ⟹ offsets intact; `face_shape` reconstructs `(N,ng,ny)`/`(N,ng,nx)`). ⭐ Blast-radius NARROWED: only 2 LIVE prod consumers (`boundary_operator._face_laws:130`, `sweep_schedule._reflective_faces:218`) — all other `.bc_*` reads are INPUT `Mesh1D`/`Mesh2D` (untouched); `operator.py:1321` is a STALE docstring. ⭐ :523-fix OBSERVABLE = the realized y-face PERM (`_angular_factor(bc["ymin"].inner).perm == quad.reflection_index("y") != ...("x")`) — law's `axis` consumed at `realizer:185` into a `PermutationOperator`, not retained. Mode-9 closer = ASYMMETRIC mixed-BC (both axes, nx≠ny). 2-commit landing: (1) crosswalk+layout, (2) bc-dict+`_resolve_one`+migrate+retire. NO golden regen (byte-id). Extends [[c3-6-honest-d-dispatch-verification]] + [[c3-dim-generic-sweep-dag-verification]].

## Test infrastructure & harness (LANDED)

- [SN sentinel (canary) harness](sn_sentinel_harness.md) — `@pytest.mark.sentinel` on the capability DAG; **vv Mode-8 `-O`-strips-bare-assert hazard** (gate runs WITHOUT -O); cosmic-ray mutation recipe + `git checkout` gotcha; thin-wrapper pattern; known gaps.
- [SN taxonomy reorg mapping](sn_taxonomy_reorg_mapping.md) — tests/sn capability-taxonomy reorg; file-destination map; cap-marker-via-dir-conftest; the xdist 3.8.0 + Py3.14.3 loadscope DEADLOCK (gate sequential per-dir, whole-tree -n auto unachievable).
- [Peierls rank-N / rank-2 closure](project_peierls_rank_n.md) — Marshak/DP_N rank-N + Phase F rank-2 per-face BC; regime-A bit-exact recovery (rtol 1e-14); §9.1 dual-route discipline (GL vs mpmath.quad of same integrand) for any new analytical formula; F.1/F.2 test layout.

## Landed-work verification plans (committed brain — KEEP; ★ = cited as provenance)

These pre-implementation plans/specs drove now-landed carves. The committed
tests are the live record of WHAT passed; these keep the gate design + WHY.
Several are cited by file-path from committed tests/docs — deleting them would
dangle that provenance, so they stay.

- [D-I.3 verification plan](D-I.3_verification_plan.md) — StreamingOperator bare-`ndarray` retirement (Depth B D-I.3): both 1-D B1''-packed + 2-D `ravel('F')` arms retire → `TimedFullField`-only.
- ★ [D-G BoundaryFlux pure-Field verification](dg_boundary_flux_pure_field_verification.md) — D-G BoundaryFlux pure-Field carve + SweepScratch split; pre-D-G snapshot + 3 specs + ULP-bound equivalence. Cited by `tests/transport/fields/test_boundary_flux.py` (+2 tests).
- [Issue #196 Phase G Step 1 gates](issue_196_phase_g_step1_verification_gates.md) — DiamondDifference→`SNCellOperator` + MorelMontry→`AngularRedistribution` type promotion; 7 gates + Phase-F twin-path defense.
- [Phase 1 verification plan (moment-space)](phase1_verification_plan.md) — ERR-039 fix via `SphericalHarmonicBasis/Space`/`MomentProjection` + `.T`-vs-`.H`; pin legacy + new-convention tests + L1 MMS gates.
- [Phase 3 verification plan (moment-space)](phase3_verification_plan.md) — layered re-architecture: P3.1 import-linter `FORBIDDEN_EDGES` + per-step gates P3.2–P3.6 + AngularFlux split.
- ★ [Wave T.3 scattering SOTP spec](wave_t_t3_scattering_verification_spec.md) — `ScatteringOperator.apply`→`SumOfTensorProductsOperator` pre-impl plan. Cited by `tests/sn/_fixtures/wave_t_t3/`.
- ★ [Wave T.4 streaming SOTP spec](wave_t_t4_streaming_verification_spec.md) — `StreamingOperator.apply`→`L_spatial+L_angular_redist` tensor-network pre-impl plan. Cited by `tests/sn/operators/test_streaming_operator.py` + `docs/theory/operator_algebra.rst`.
