- [Peierls rank-N / rank-2 closure conventions](project_peierls_rank_n.md) — rank-N angular (Marshak/DP_N) and rank-2 per-face (Phase F) Peierls BC; bit-exact regime-A recovery, dual-route §9.1 discipline, Phase F.1+F.2 test file layout.
- [V&V tagging idioms](feedback_vv_tagging.md) — Module-level `pytestmark`, explicit per-test `verifies()`, xfail for API-dependent tests, strict=False convention.
- [Diagnostic-to-test promotion patterns](feedback_diagnostic_promotion.md) — Verify diag runs first, reproduce pins via public API, three foundation classes (invariant / negative-regression / math-origin), warning suppression, delete-after-pass ordering.
- [Cross-method test protocol design](feedback_cross_method_protocol.md) — Reuse registry schema; CrossMethodCase + SolverAdapter Protocol; max(tol_a,tol_b) for pairwise agreement; L1 (not L4) for cross-method gates; verify truth values against literature memos before writing.
- [Phase 1 (moment-space) verification plan](phase1_verification_plan.md) — Legacy pin/adapt/delete dispositions for `test_projection_operators.py`; 10 new tests under `test_spherical_harmonic_space.py` (5 plan-identity + 4 API-surface + 1 forward-compat); L1 MMS-aniso bit-identity contract; P1.7 snapshot drift criteria per `vv-principles` §Bit-identity; P3.1 import-linter spec; gates checklist per P1.x.
- [Phase 3 (layering) verification plan](phase3_verification_plan.md) — P3.1 import-linter `FORBIDDEN_EDGES` dict + 76-LoC pytest skeleton with TYPE_CHECKING + WHITELIST tolerances; per-step gates (P3.2–P3.6) with bit-identity vs principled-equivalence judgment; AngularFlux CC.4 resolution (BoundaryFlux co-promote = Option A); risk register (P3.4 highest); concurrence with REVISED sequencing P3.1 → P3.5 → P3.0 → P3.2 → P3.3 → P3.4 → P3.6.
- [D-G BoundaryFlux pure-Field verification](dg_boundary_flux_pure_field_verification.md) — pre-D-G regression snapshot pytest gate (re-runnable per sub-commit); 3 new test specs (transport.fields/test_boundary_flux: Field algebra + FaceLayout slice views + flat-buffer round-trip; sn/test_sweep_scratch_split: reflective-BC two-iter persistence + interior-cache split invariant; sn/test_boundary_flux_immutability_invariant: FrozenInstanceError gate + legacy-attribute retirement); 7 ranked failure modes (Rank 1 = stale reflective-BC face state); PRINCIPLED equivalence with rtol=1e-13 / iter_count×ULP bound.
