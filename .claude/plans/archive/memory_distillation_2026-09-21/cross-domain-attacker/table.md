# Audit trail — cross-domain-attacker memory distillation, 2026-09-21

Every DUPLICATE verdict below was made by opening the source and naming its
heading or item, never from memory of it. Sources read whole or at the cited
lines this dispatch: `.claude/agents/cross-domain-attacker/AGENT.md`;
`.claude/skills/cross-domain-frames/SKILL.md` + `reference.md` (Part A rows,
Part C Smells 1-16); `.claude/skills/nexus-exploring/SKILL.md`;
`.claude/rules/process-discipline.md:150-163`;
`.claude/rules/plan-authoring.md:1-36`; `docs/development/rules/*.md` for
`brief:` fields.

## Part 1 — `lessons.md`, one row per entry or contiguous range

| lines | entry (first words) | verdict | where its correction lives now |
| --- | --- | --- | --- |
| 1-16 | "Behavioral/process lessons only… The cross-cutting meta-pattern" | KEPT (compressed) | digest header + THE SPINE paragraph |
| 18-27 | L-001 "→ The DIRECTIVE is now in AGENT.md" | DUPLICATE of `AGENT.md` §Required Output Shape ¶"A refuted frame is first-class output" (and `process-discipline` §"A refuted candidate is first-class output", whose `check:` names this agent's UNEXPLORED block) | those two sources; digest cites them in M3 |
| 38-41 | L-001 bullet "Category theory / operad / PROP — almost always LOW-SIGNAL" | DUPLICATE of skill `reference.md` Part A.2 row **Category theory** ("LOW-CONFIDENCE… Mark low-signal until a concrete win appears") | skill A.2; digest Part 4 cites the row instead of restating it |
| 28-37, 42-49 | L-001 bullets Wiener-Hopf / homology / tensor-networks / Christoffel | KEPT (4 ledger lines) | digest Part 4 — the frame NAMES and the trigger each needs, which `AGENT.md`'s parenthetical reasons do not carry |
| 50-55 | L-001 "How to apply: when a high-prior exotic frame does not fire" | DUPLICATE of the same `AGENT.md` paragraph (it carries the reason list verbatim) | `AGENT.md` |
| 57-64 | L-001 "⭐ SHARPENING (2026-08-27) — a refutation must record the QUESTION" | DISTILLED into M3 | digest M3; `agent_md.md` P1; `uplift.md` U2 |
| 68-95 | L-002 "A first test that cannot fail is rejected output" | DUPLICATE of `AGENT.md` §Required Output Shape ¶"A first test that cannot fail…" — it carries the native-frame-property construction, `array_equal` 0 ULP vs `allclose`, the NEGATIVE typing test, and the "what would PASS this test" check | `AGENT.md`; the two worked constructions → archive §L-002; the non-test claim shapes → digest M2 |
| 98-111 | L-003 "Smell #16 … is the dominant transport tell" | DUPLICATE of skill `reference.md` Part C **Smell #16** (all four shapes + "name which one fired", lines 167-207) and `AGENT.md` §Promoted Library Kernel | skill Part C; `AGENT.md` kernel |
| 114-162 | L-004 "Property-vs-TYPE is decidable" | KEPT (compressed) | digest D1; worked verdicts → archive §L-004; pointers `spatial_order_type_vs_property_criterion.md`, `rep_role_grid_double_category_frames.md` |
| 165-178 | L-005 "Read the WORKTREE, distrust Nexus on a feature branch" | DUPLICATE of `nexus-exploring` SKILL.md §"Worktrees and ambient context" + §"The position bridge" honesty bullet (graph is a build-time snapshot; `node_at` warns; rebuild before trusting positions) and of `MEMORY.md` §2's standing re-ground rule | the skill; `MEMORY.proposed.md` §2 (kept, strengthened to "status is read from the tree") |
| 181-206 | L-006 "Frame-leak naming" | DISTILLED into the promotion ledger | digest Part 3 row 1 (firing rule + the 2nd-consumer-with-no-1st-in-scope test + the hold reason) |
| 209-212 | L-007 "The spine itself is the AGENT.md kernel" | DUPLICATE of `AGENT.md` §Promoted Library Kernel "Cross-method backbone" | `AGENT.md` |
| 213-229 | L-007 deployment clauses (posings of one eigenproblem; generality flows toward the opaque resolvent) | KEPT (compressed) | digest D2(a),(b) |
| 231-246 | L-007 corollary "the backbone tells you WHERE a foreign frame fires" | KEPT (compressed) | digest D2(c); `agent_md.md` P4 (queued promotion); detail in `dsa_saddle_point_mixed_fem_frames.md` |
| 250-273 | L-008 "A 'fully probes' claim is about operator LINEARITY" | KEPT (compressed) | digest D9 |
| 276-358 | L-009 frame OWNER + Galerkin-vs-PG by symmetry, 3 outcomes | KEPT (compressed) | digest D3; derivations → archive §L-009 + `harmonic_frame_ownership_funk_hecke.md`, `dsa_rp_angular_frame.md`, `quadrature_symmetry_quotient_frames.md` |
| 359-367 | L-009 "SKILL-PROMOTION STATUS" | DISTILLED into the promotion ledger | digest Part 3 row 2 |
| 371-414 | L-010 conserved-quantity COLLAPSE (average vs marginalize) | KEPT (compressed) | digest D4 + Part 3 row 7 (the docstring-caveat-wants-a-type corollary); pointer `xs_coarsening_collapse_marginalize_vs_average.md` |
| 415-419 | L-010 "SKILL-PROMOTION STATUS" | DISTILLED into the promotion ledger | digest Part 3 row 3 |
| 423-465 | L-011 coupled block system = free re-association; defer counts KINDS | KEPT (compressed) | digest D5; pointer `coupled_system_field_bc_frames.md` |
| 469-567 | L-012 naming task = frame detection (5 moves) | KEPT (compressed) | digest D6; the name-invariant test → M2; the stem/prose grep → `uplift.md` U1 (plan-authoring NAME-FREE); the "name states a contract the content violates" smell → Part 3 row 5; worked cases → archive §L-012 |
| 571-622 | L-013 "check whether a PREDICATE already computes X" | DISTILLED into M1 + M4 | digest M1.6 (both shapes) and M4; skill proposal → `uplift.md` U4 |
| 626-676 | L-014 "An UNSATISFIABLE predicate is a wrong-ARGUMENT diagnosis" | DISTILLED into M1 | digest M1.7; the SPENT-group three fates → `quadrature_symmetry_quotient_frames.md` (MEMORY §3 hook) |
| 680-725 | L-015 node set already Gauss; vanishing flux function | KEPT (compressed) | digest D7; pointer `cylinder_angular_march_jacobi_ladder.md` |
| 726-729 | L-015 "SKILL-PROMOTION STATUS" | DISTILLED into the promotion ledger | digest Part 3 row 4 |
| 733-765 | L-016 unit end-to-end gain ⇒ reversible | KEPT (compressed) | digest D8 |
| 769-815 | L-017 optimality theorem on the TARGET space | DISTILLED into M1 + M4 | digest M1.5, M4; pointer `sphere_mu_line_reduction_frames.md` |
| 819-891 | L-018 `PROPERTY ⟺ HYPOTHESIS` gates; the unspellable operand | DISTILLED into M2 | digest M2 (both bullets); `uplift.md` U1 §6c rider |
| 895-933 | L-019 God object = fixed point of a weak identity relation | DISTILLED into M1 | digest M1.8 + Part 3 row 6 |
| 937-998 | L-020 analogy ADJECTIVE-accurate and LAYER-wrong | KEPT (compressed) | digest D10; `uplift.md` U1 §1 PRECEDENT rider; pointer `kernel_as_frame_layer_inversion.md` |
| 1002-1063 | L-021 general-case derivation first; refuted FOR a question; the layer-table verdict shape | DISTILLED into M1 + M3 | digest M1.4 (a and c) and M3 (b); pointer `alpha_dome_chart_vs_measure_cross_method.md` |
| 1067-1136 | L-022 the THREE counts; zero-caller machinery | DISTILLED into M1 + M4 | digest M1.1 and M4 (both riders) |
| 1140-1175 | L-023 (a) sort the fused object, (b) count the inversions | DISTILLED into M1 | digest M1.2, M1.3 |
| 1176-1189 | L-023 "⛔ REFUTED 2026-09-08 … the solution-set clause" | STALE — landing named: superseded by L-024's family rule, which is the digest's M1.9; the acceptance GATE it left standing is inside M1.9's degenerate-cell/admissibility clauses | digest M1.9; the refutation's own record → archive §L-023 |
| 1191-1212 | L-023 (c) two riders (identity direction; mutation sites) | KEPT (compressed) | digest M1.2 riders + Part 3 row 6 |
| 1215-1282 | L-024 terminal object + parameter domain; grep name AND verb | KEPT (compressed) | digest M1.9, M1.10; pointer `problem_solution_split_frames.md` |

## Part 2 — `MEMORY.md`

| lines | entry (first words) | verdict | where its correction lives now |
| --- | --- | --- | --- |
| 1-6 | "Pointers only. Per-lesson detail lives ONLY in `lessons.md`" | KEPT (gains the `lessons_archive.md` pointer and names the `AGENT.md` kernel as the second preloaded source) | `MEMORY.proposed.md` preamble |
| 8-23 | §1 hook, incl. the four ⭐ inline summaries of L-021…L-024 | CUT (lossless) — the summaries violate this file's own "never re-summarize a lesson here", and all four contents are digest entries (M1.1-M1.4, M1.9, M1.10, M3) | digest Part 1; `MEMORY.proposed.md` §1 is a 2-pointer hook |
| 25-29 | §2 "Standing rule: re-ground every cited `file:line`… git is the authority" | KEPT (strengthened: no campaign status is recorded here at all) | `MEMORY.proposed.md` §2 |
| 29-30 | §2 "The quadrature attack (§3) is the one whose campaign was still in flight at last curation" | ARCHAEOLOGY — a status claim with no verifiable referent: it names no issue, no plan and no branch, and the neighbourhood has since moved (three plans now exist for that work: `symmetry_quotient_plan.md`, `symmetry_machine_reads_like_the_math.md`, `angular_spaces_derived_from_symmetry.md`). Not carried forward in any form | `MEMORY.proposed.md` §2's rule to read status from `.claude/plans/` and git |
| 37 | §3 Problem/Solution hook "`Pencil.at(σ)` is chartered and ships mutating" | STALE — landing named and verified in the tree: `orpheus/numerics/pencil.py:69 def at(self, sigma: float) -> LinearOperator[V, V]`, i.e. chartered-and-mutating is two steps behind | `MEMORY.proposed.md` §3: "`Pencil.at` has since landed (`orpheus/numerics/pencil.py`)" |
| 64 | §3 #336 hook "the Stage-0 gate is landed and unwired" | STALE — a wiring claim needs an AST consumer pass (`instrument-doctrine`), which this dispatch cannot run; the line-grep shape of it no longer reproduces the recorded picture (`admits_domain` in `orpheus/` returns the definition and docstring cross-references only, not the `select_quadrature` call the memory records). Removed rather than re-asserted; the timeless verdict is kept | `MEMORY.proposed.md` §3 keeps the Gauss-optimality + CDF-internality verdict; wiring status → the tree |
| 32-98 | §3, the other 44 design pointers | KEPT — they are Nexus graph nodes with 857 inbound reference lines from `docs/` and `.claude/plans/`; hooks tightened (5 of them) to "whether to open it" length, no pointer retired | `MEMORY.proposed.md` §3 |
| 99-101 | footer "Files here not listed above = archive" | KEPT verbatim | `MEMORY.proposed.md` footer |

## Part 3 — Topic files judged archaeology (no retirement here)

Per the brief these are listed only; the orchestrator runs their blast-radius
audit separately. I judge **none** of the 58 topic files archaeology. Two carry
superseded verdicts, and both already say so IN the file and are cross-linked
from the surviving entry, which is the state the standard wants (a refuted
premise edited in place, not deleted): `psi_half_seed_angular_trace_frames.md`
(its "sphere-only" verdict, superseded by `cylinder_angular_march_jacobi_ladder.md`)
and `homogenization_measure_derivation_frames.md` (its Galerkin reading,
superseded by `projection_discipline_hierarchy_frames.md`). One is fully absorbed
by the skill and is named as such in the index footer:
`elegance_smell_rank_non_monotone.md` → Part C Smell #15 — a candidate for
retirement ONLY after the retirement-audit blast radius (`docs/`, `.claude/plans/`,
graph) is clean, which this dispatch cannot run.

## Part 4 — Counts (X2: the predicate and the denominator)

Line counts, `rg -c '^' <file>` (every line matches `^`, so this is `wc -l`):

| file | before | after |
| --- | --- | --- |
| `lessons.md` → `lessons.proposed.md` | 1 282 | 389 |
| `MEMORY.md` → `MEMORY.proposed.md` | 100 | 95 |
| `lessons_archive.md` | did not exist | 1 282 + a 17-line header (the moved file, unedited) |

Hot surface per dispatch: 1 382 lines → 484 (−65 %), and the archive is cold.
`MEMORY.md` barely moves because 44 of its 95 lines are the §3 design-pointer
index — one line per cold graph-node file, which is the index doing its job; its
§1 hook is what shrank (16 lines → 8).

Rows per verdict, over 35 `lessons.md` rows (denominator = every line of the
file, 1-1282, covered without gaps or overlaps):

| verdict | rows |
| --- | --- |
| KEPT (compressed) | 14 |
| DUPLICATE | 7 (4 × `AGENT.md`, 2 × `cross-domain-frames` skill, 1 × `nexus-exploring` skill) |
| DISTILLED into a meta-lesson | 13 (→ M1 ×8, M2 ×1, M3 ×2, M4 ×4, Part 3 ledger ×4; a row may feed two) |
| STALE (landing named) | 1 |
| CUT (lossless) | 0 |
| ARCHAEOLOGY → archive | 0 as a whole row — the archive takes the whole old file verbatim, so every row's war story is in it by construction |

Over 8 `MEMORY.md` rows (denominator = every line, 1-101):

| verdict | rows |
| --- | --- |
| KEPT | 4 |
| CUT (lossless) | 1 |
| STALE (landing named) | 2 |
| ARCHAEOLOGY | 1 |

Uplift candidates: 4 (`uplift.md` U1-U4, of which U1 asks `plan-authoring` for a
`brief:` line it does not have). AGENT.md promotions: 2 new (P1, P2 — P2
conditional on U3 not landing) + 2 re-endorsed from the previous pass's queue.

## Orchestrator's notes at apply time (2026-09-21, the main agent with the session's context)

The owner had no Bash. Executed for it: the archive as the header block plus the old digest verbatim (the pure move; `archive.endswith(old)` asserted in the apply script); `gh issue view 336` → OPEN; an AST pass over `orpheus/` for `admits_domain` → its definition only (1 site, 0 callers), so the removed "wired to `select_quadrature`" status was stale, and the Smell 17 shape-(b) sighting is "unconsumed" rather than "advisory only" (recorded in the uplift queue). Applied: P1 (the question a frame was refuted for) in `AGENT.md`; U2 as a `process-discipline` check and brief-line clause; U3 as a new `process-discipline` section and brief-line clause (four agents learned it separately); U1 as a `plan-authoring` `brief:` line plus the three riders PRECEDENT-LAYER, NAME-CARRIES-A-VERB, GATE-IS-SPELLABLE. Not applied: P2 (U3 landed with a brief line, so one home); P3/P4 and Smell 17 stay HELD in the queue for the user; `elegance_smell_rank_non_monotone.md` is not retired (a graph node; the blast-radius audit is a separate pass).

Counts as applied (`wc -l`): lessons.md 1282 → 394; lessons_archive.md 0 → 1300 (new); MEMORY.md 100 → 95.
