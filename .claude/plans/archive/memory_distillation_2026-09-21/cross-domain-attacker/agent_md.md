# AGENT.md promotion candidates — cross-domain-attacker, 2026-09-21

Read `.claude/agents/cross-domain-attacker/AGENT.md` whole first. Its standing
directives already carry: the refuted-frame bar and the discriminating-first-test
bar (§"Required Output Shape", two paragraphs), Smell #16's four shapes and the
transport-resolvent backbone (§"Promoted Library Kernel"). Those are why L-001,
L-002, L-003 and L-007's spine retire in this pass rather than being promoted
again.

Two items from the previous pass are still HELD in
`.claude/plans/memory_distillation_uplift_queue.md:113-116` and are re-endorsed
below (P3, P4) rather than re-proposed as new text.

---

## P1 — one sentence, appended to the existing refuted-frame paragraph (§Required Output Shape)

> **Record the QUESTION the frame was refuted FOR.** The same frame can be
> decisive on a different question, so write "refuted FOR <question>; the FACT it
> establishes is <fact>" — and when a memory or a plan hands you a ⛔ banner,
> check which question earned it before carrying it forward.

Identity-level: every attack emits an UNEXPLORED block, so this fires on every
dispatch. It is also the half of the bar that ACTIVELY destroys answers when
missing — a permanent "no lever" banner is what a later summariser carries
forward (the symplectic / momentum-map case: refuted for a design question,
load-bearing for a classification one). The instance stays in the digest as M3
with a `→ now in AGENT.md` pointer. Cross-agent form: `uplift.md` U2.

## P2 — a Procedure step, ONLY if the rule in `uplift.md` U3 does not land

Proposed as a new §"Step 1b — Measure the brief's premise" between feature
extraction and trigger lookup:

> Before the trigger lookup, take the count the brief's framing presumes and
> publish it: the runtime BRANCHES a proposed dispatcher would collapse; the FIBER
> of a proposed "problem → object" section; who CONSUMES the object a
> justification names; the attributes of the object where a proposed split is
> currently FUSED; the inversions a chartered discriminating rule produces over the
> tree's straddlers; whether a general-case derivation or an optimality theorem on
> the target space settles the whole table at once. The measured answer is usually
> SMALLER than the proposal and somewhere else — report that collapse plainly.

⚠ **Land P2 or U3, never both.** U3 proposes the same lesson as a
`process-discipline` clause, on the evidence that `test-architect` learned it
independently (queue A1, "measure the premise before you gate it"). If U3 lands
WITH a `brief:` line that reaches me, P2 must not land — a second copy is the
failure this campaign exists to remove. If U3 is declined or lands without a
brief line, P2 is the only copy that reaches an `omitClaudeMd` agent and should
land. Meanwhile the content stays in the digest as M1.

## P3 — re-endorsed: queue A1, a new task-classification row

Queue text: *"a naming/vocabulary adjudication is frame-detection work, not taste
work"*. Still correct and now at four sightings (the reaction-term species split,
the `SNMesh` residual tournament, the terminal-object naming, the `Pencil` name
plus verb). Proposed row for the §"Incoming task type" table:

> | Naming / vocabulary adjudication | Frame-detection work, not taste work: hunt the refinement theorem, then the spent word (your task) |

Digest home meanwhile: D6.

## P4 — re-endorsed: queue A2, the backbone says WHERE a frame fires

Queue text: *"a frame keyed to an operator's ALGEBRAIC SHAPE fires only on the
members whose shape matches"*. It belongs in the kernel's backbone paragraph,
which already predicts the diffusion exception — the same principle answers "at
which member do I aim this frame?" (saddle-point / inf-sup / mixed-FEM fires on
the diffusion member; the sweeps are characteristic-triangular with no saddle to
stabilize). Digest home meanwhile: D2(c).

---

## Considered and DECLINED (a thin AGENT.md beats a bloated one)

- **M2 (the `⟺` gate procedure; the unspellable operand).** Fires only on briefs
  containing an equivalence or a chartered law — conditional, not identity-level.
  Stays a lesson; its cross-agent form is `uplift.md` U1 §6c.
- **M4 (the true deliverable is smaller and elsewhere).** A reporting standard
  rather than a detection identity; proposed as U3's `REPORT-THE-COLLAPSE` rider,
  and P2 already carries its last sentence if P2 lands.
- **D1–D10.** Each fires on a recognisable class of brief (a type question, a
  naming question, a collapse, a march direction). Conditional by construction;
  these are exactly what recalled memory is for.
