# Step 3 of the consumers campaign — the Solution carries its posing (its kind is the posing's type; the state, the gauge that picked it, the certificate and the record travel with it)

_Plan-mode session 2026-09-14. Governed by `.claude/rules/plan-authoring.md`. Markers: `[M]` measured (command/file:line), `[M-census]` the explorer's AST census, `[R]` ruled by the user, `[T]` theorem, `[H]` hypothesis (with the measurement that settles it). Plan of record: `.claude/plans/cs4c_binding_design.md` §27.8 (⏸ COMPACTION POINT #11; §27.8.5 is step 3's charter). On approval this memo is folded into the repo as `.claude/plans/consumers_step3_design.md` (precedent: `consumers_step2_design.md`), §27.9 of the plan of record carries the landing log, and the session memos are copied to `scratch/_consumers/step3/`: the explorer census `~/.claude/plans/inherited-swimming-rivest-agent-a1da3f479eeb7013d.md` → `explorer_solution_census.md`; the frame pass `…-agent-afdc583991f142296.md` → `attacker_solution_articulation.md`; the design memo and the landing memo (Plan agents' final messages, in the session log) → `design_step3.md`, `landing_step3.md`._

## 1. Context — why this step, and why a planning session

The consumers campaign splits the SN machinery into a **Problem** (`SNMesh`, renamed `SNProblem` LAST — #412) that determines the consumed objects from its generating data, and a **Solution** side that feeds a Problem to a solver and works on the consumers. Step 1 CLOSED `290e4747`; step 2 (the pencil: `OperatorPencil` + the kind-typed posings, the hub's `system`/`pencil`/`eigen_posing`/`source_posing(q)`, σ as a Problem datum, the Strategy value `Splitting`) LANDED 2026-09-14, tip `c930c68f`, exit gate 11 792 / 21 / 55 over 13 trees rc=0 (§27.8.2 — the baseline for THIS step). `[M]` no `.py` changed since (`git diff --stat c930c68f..80e117e5 -- '*.py'` empty); every step-2 hash is an ancestor of HEAD `80e117e5`.

**Goal (domain terms).** A Solution is the pair (Problem, posing) plus the Strategy that produced it and the records. Its KIND is the posing's TYPE — an eigen Solution carries a ray representative, λ and the normalization GAUGE that picked the representative; a source Solution carries the coset representative and the kernel gauge — never `keff is not None`. The residual and the exit balance read the posing (ONE functional); the adjoint posing is `eigen_posing.H()` on BOTH arms. Charter items: **#461** (the gauge on the Solution) · **#462** (the balance functional at the SN tier) · **#467** (the seedless adjoint on the coupled carrier); adjacent **#460** (`admits(q)`, filed — F7), **#465** (`ProblemSpec`'s Optional pair — in scope minimally, §3.4).

**Why a planning session:** the §27.6 O-2 process ruling governs step 3; the reshape crosses every SN entry, both adjoint entries, the derivations' reference specs and the homogeneous family, and it is an operator-algebra carve crossing typed ↔ adjoint ↔ multi-family boundaries — the **test-architect proactive MUST trigger: the FIRST tool call after approval is that dispatch**, before any carve.

## 2. Ground facts `[M]` at HEAD `80e117e5` (this session, read-only)

### 2.1 The Solution today (`orpheus/sn/solution.py`)
- `SolutionBase` (`:346`), frozen, NOT instantiable (`:428`): `angular_flux: TimedFullField`, `scalar_flux: ScalarFlux`, `mesh: SNMesh`, `keff: float | None`, `history: IterationHistory | None`, `radial_characteristic: RadialCharacteristicField | None`. `[M]` exactly TWO subclasses tree-wide (`Solution` `:668` — `reaction_rate_density`/`homogenize`/`condense`; `AdjointSolution` `:1083` — `importance`). ⚠ §27.8.5's *"every `SolutionBase` subclass across the six method families"* was a wrong premise (§2.6 of the census: no other family subclasses it).
- **The kind is a PROPERTY today by a recorded ruling** (`:36-46`: *"the two kinds share every realization AND every operation … a type here would be ceremony"*): `is_eigenvalue()` `:543` = `keff is not None`; `is_fixed_source()` `:547`. The role is a TYPE (#276 A5). That ruling's verb-set half still holds (§3.1: the ROLE keeps two classes); its "same realization" half is what step 3 overturns — the kinds now carry different OBJECTS (ray + λ + scale gauge vs coset + kernel gauge) and their adjoints differ in ARITY.
- `IterationHistory` (`:115`): a VIEW over `record: IterationRecord` + `keff_history`, `balance_defect: float | None` (FIVE documented meanings of `None`, `:152-179`), `gauge_correction: float | None` (three, `:193-204`); its own docstring says the two magnitudes are iterate properties, *"not a convergence quantity"*. `compare` (`:604`) branches on `keff is not None` (`:642`, `:655-656`).

### 2.2 The mints and the exits (`orpheus/sn/solver.py`)
- **3 mint bodies serve 5 entries** `[M-census]`: `_solve_fixed_source_si:3814` and `_solve_fixed_source_krylov:4016` build `Solution(...)` inline (whole `angular_out`, DG moment tail KEPT — pinned by `tests/transport/spatial/test_ld_slope_frame.py:73` `(N, ng, nx, 2)`); `_package_solution:2480` mints POLYMORPHICALLY (`cls(` `:2537`; `cls=Solution` from `solve_sn:2455`, `cls=AdjointSolution` from `_package_adjoint_solution:2939`) through `_cell_average_angular` (slot 0 of the moment axis, `:2458-2474`, `_average_moment_scalar` `:3195-3207`). The eigen exit packages the POWER ITERATION's converged φ as `scalar_flux` (`:2455`), not `∫ψ dΩ` of the returned ψ (`solver.rst:2852` records the asymmetry).
- `solve_sn_fixed_source:3209` solves `pencil.at(0)` (docstring `:3226` *"the fission source is zero"*) and states NO posing; `solve_sn_multiplying_source:3503` calls `sn_mesh.source_posing(q)` as a BARE STATEMENT (`:3563`, value dropped), measures the admissibility `k` (`:3546`) and DROPS it (`keff=None` via the SI arm), is NOT exported from `orpheus/sn/__init__.py`, and `[M]` **bypasses the hoisted `warn_if_unconverged`/`warn_if_gauge_freedom`** (those live in `solve_sn_fixed_source:3473-3484`; `tests/numerics/test_family_convergence_contract.py:685` pins the warn-site count at 7). `[M]` on ONE fissile hub the two entries differ (`tests/sn/solve/test_subcritical_multiplying_source.py:66-75` asserts φ_mult > φ_pure).
- The exit functionals: `_exit_balance_defect:567` — the RELATIVE per-group ratio `‖R_g(Aψ − q)‖/‖R_g(q)‖` through `_balance_projection` (`:531`, volume × weights per group), `None` when fully converged (the certificate has it, `:726-790`) / inexpressible (`_residual_is_expressible:466`, #310) / zero source; 4 sites; a DIAGNOSTIC never a gate (N5). `_exit_gauge_trace:658` projects `ker A` out of the returned TRACE (`LossKernelGauge` is *"an endomorphism of the boundary-trace space … the kernel is a trace object"*, `loss_kernel_gauge.py` class docstring), returns `(gauged, ‖Πψ‖/‖ψ‖)`; 5 sites; MUTATES the answer, so coverage is gated by the entry ledger (§2.5). `evaluate_residual:328` **EXISTS** — the typed full-system residual `Aψ − q` on a `WithinGroupSystem`, 10 calls / 4 files + 6 theory pages `[M-census]` (the step-2 memo's "0 hits" was false when written). `SNSolver.compute_keff:1590` = `R_νΣf/(R_Σa + L − E_2n)`; `EigenPosing.rayleigh(ψ, w=1)` = `⟨1,Fψ⟩/⟨1,Aψ⟩` — a REFERENCE-class pair (gap = the convergence residual). `[M]` `SourcePosing.residual = q − Aψ` (`posing.py:132`) while `evaluate_residual`, `_typed_balance` and `SourcePosing.balance`'s own docstring use `Aψ − q` — a sign twin.
- `solve_sn_adjoint:2606` poses `EigenPosing(OperatorPencil(implicit.H − gain.H, F_posed.H), K_MAP)` PER ARM from `_adjoint_posing_parts:2553` (#467); `[M]` the seedless adjoint iterates on a bare `FullField` while the hub's 1×1 coupled `apply` demands a `CoupledField` (`_bare_loss_arm` `:797-799`). The adjoint fixed-source entry takes its own `q*` (unary). `PowerIterationOutcome` (`numerics/eigenvalue.py:304`): `keff, keff_history, flux_distribution, record, converged`; `KEigenvalue(posing, implicit, explicit)` (`iteration.py:1200`) consumes `EigenPosing`.
- **The eigen gauge ships as FOUR functionals under one name, recorded nowhere** `[M]`: `power_iteration` rescales to production rate 1 ONLY when `isinstance(solver, ProductionRateSolver)` (`eigenvalue.py:437-447`) — SN's `compute_production_rate` (`solver.py:1557-1588`) INCLUDES the (n,2n) emission; `KEigenvalue`'s (`iteration.py:1455`, the adjoint's) is fission-only; diffusion's `⟨νΣ_f, φ⟩` (`diffusion/solver.py:346`); homogeneous rescales to 100 (`homogeneous/solver.py:436`); the dense engines sign-normalise only (`eigenvalue.py:506`). No result type records which applied.

### 2.3 The Problem side step 3 consumes (`orpheus/sn/mesh/augmented_mesh.py`)
`system` `:1121` (built ONCE), `pencil` `:1139` (`(system.loss, system.production)` on the coupled space), `eigen_posing` `:1146`, `source_posing(source)` `:1152` (lifts a full-field source into the ONE-system `CoupledField`; `SourcePosing(pencil.at(1.0), lifted)` ALWAYS — the step-2 fork-2 ruling), `fission` `:1172`, `mat_xs`, `with_cross_sections` `:857`, `_identity_key` `:596` = `(_contractibility_key, closure class, scattering_order, sigma_t_cell.tobytes())`. `OperatorPencil.at(0.0)` **returns `self.lhs` itself** (`pencil.py:69-74`). `[M]` `CoupledField` has NO `.space` (`numerics/coupled_system.py`: `systems`, `n_systems`, `principal_bulk_leaf`, `to_flat`, `from_flat`) ⟹ `SourcePosing.__post_init__`'s ends check (`posing.py:124-126`, `getattr(source, "space", None)`) is INERT on every SN source. `[M]` `A.H.H is A` by identity (`operator.py:1674-1678`).

### 2.4 The other families
`HomogeneousResult` (`homogeneous/solver.py:75`, mutable): `k_inf, flux, …, mixture`; the gauge applied at `:436` and recorded nowhere; `HomogeneousProblem.pencil`/`.eigen_posing` exist (`:315/:328`). `[M-census]` every other family's answer is eigen-ONLY with a REQUIRED `k`; no shared base; `[M]` `grep pencil|eigen_posing` in `diffusion/cp/moc/*/solver.py` = 0 (no other family poses a pencil yet).

### 2.5 The entry ledger gate (`tests/sn/solve/test_every_entry_gauges_its_trace.py`)
`_ENTRY_LEDGER` hand-lists 5 rows; discovery = `name.startswith("solve_sn") and inspect.isfunction(obj)` over `orpheus.sn.solver`, set-equal ⟹ any new/renamed/retired `solve_sn*` reddens it; 7 of its 10 rows read `history.gauge_correction`, 1 reads `keff`. ⚠ A non-`solve_sn*` entry would be invisible to it.

### 2.6 Ground-count reconciliation (obligation 1) — ⚠ SURPRISE, logged
§27.8.5's six counts were one LIST read as one census; re-run with the population stated: `Solution(` 4/2 and `_build_fixed_source_rhs(` 3/1 reproduce over `orpheus/` only (as LINE counts incl. a docstring and the `class` line); `.is_eigenvalue(` 6/2, `_adjoint_posing_parts(` 8/3, `solve_sn_multiplying_source(` 4/2 reproduce over `orpheus + tests` (LINE counts incl. `def`s); `keff is not None` 3/1 reproduces under NEITHER (the predicate was `keff is (not )?None` over `orpheus/`, missing the truthiness form and 13 test branch sites). The 2026-08-31 surprise ("a list of counts implies a shared denominator") — no new clause; the row goes to plan-authoring's surprise log at close-out. The design is sized on the census (§6), AST, per tree, per receiver TYPE.

### 2.7 Measurements taken against the frame memo's claims
- The eigen gauge's three spellings — CONFIRMED and sharpened to FOUR functionals (§2.2) ⟹ #461 holds on every family; the gauge must record the functional as the OBJECT that ran.
- The memo's torsor leg 2 ("the gauge acts on the boundary only, interior kernel modes survive — REDs today") — ⛔ REFUTED: the kernel IS a trace object; gauging the trace is gauging the state. The Γ-invariance test draws its kernel vector from the TRACE space (`Π`'s own range).
- F8 (which identity is "the same problem") — MEASURED, routine: `same_phase_space` is the CONTRACTIBILITY tier and admits a σ-variant BY DESIGN (`tests/sn/sweep/core/test_cache.py:355`, `tests/sn/mesh/test_sigma_datum.py:85`); its three `solution.py` consumers (`compare:634`, `homogenize:917`, `condense:1045`) are LAYOUT gates. ⟹ the Solution's "same Problem" is `==` (identity tier, hub-derived, no new code); `same_phase_space` stays on the collapses.
- The memo's negative discriminator ("`replace(sol, outcome=<other kind>)` fails because the posing and the iterate's space disagree") — ⛔ false on one hub: both kinds' posings share the coupled space. Corrected in §3.2.
- The memo's ScaleGauge idempotence at 0 ULP — corrected to `allclose` (a rescale multiplies by `1/(1 ± ε)`); its "`P.H().H() == P` may RED" — a pass by identity (`A.H.H is A`).

## 3. Design (after the design round and the checkpoint rulings of §5; what SURVIVED)

### 3.1 The chain as OBJECTS at the Solution tier (owner per R-cc2: Problem / Strategy / Solution)
| stage | object | owner | answers when queried | step 3 lands | later |
|---|---|---|---|---|---|
| base point | `mesh: SNMesh` (field name KEPT until #412; `[M-census]` 24/10 test reads) | Problem | `system`, `pencil`, `eigen_posing`, `source_posing(q)`, `loss_kernel_gauge`, `==` | unchanged | `problem` at the rename |
| the question | `EigenPosing` / `SourcePosing` (step 2) | Problem — except a solver-chosen point in Λ (Strategy) | residual, balance, rayleigh, `.H` (nullary / unary) | recorded INSIDE the outcome. **F11 `[R]`: the fixed-source driver poses `SourcePosing(hub.pencil.at(0), q)` ITSELF** (fission suppression on a fissile hub is the entry's modelling choice — a Strategy-side point in Λ, step-2 memo §3.1 row 2; on a non-fissile hub `at(1) == at(0)` in value and the hub's `source_posing(q)` IS the physical question); the multiplying entry records `hub.source_posing(q)` (the bare statement becomes the value); "fission suppressed?" reads `outcome.posing.operator is problem.pencil.lhs` (identity: `at(0.0)` returns `lhs`). No second hub member; no σ field; the step-2 ruling and the AC-a chain untouched. `SourcePosing.residual` flipped to the loss-sign `Aψ − q` (Pattern 7; one law in `tests/numerics/test_posing.py`) | `admits(q)` #460 |
| Strategy values | `Splitting` (the value every entry holds at exit: `[M]` `solver.py:2326` `inner.splitting`; `:3658`, `:3922`, `:2587`); budgets + tolerances | Strategy | the operators AS USED | **`strategy: Splitting`** recorded by reference. NO `SolveStrategy` type: `[M]` the record ALREADY carries the budget (`IterationRecord.budget: IterationBudget(limit, name, iterations_per_unit)`, `convergence.py:1008-1014`) and every tolerance per level (`StoppingCriterion(name, trajectory, tolerance)`); the driver KIND is the record's `label` (stringly — P5's `inner_solver: str` retirement, not this step's) | P5/P6 |
| the state | **`outcome.state: CoupledField`** — the RETURNED iterate WHOLE (1-system seedless, 2-system carrying) `[R]` F3 — ⛔ the FINALIZED state every entry returns, NEVER `InnerSolve.iterate`: `[M]` (test-architect F-3) on the 4 windowed 2-D arms the inner iterate is a `HarmonicMomentFlux` and the posing's verbs raise `IndexError` on it; on the returned state all 16 finalize cases evaluate | Solution | `angular_flux` = `state.systems[0]` (the arm's own convention: WHOLE with the DG tail — the fixed-source spelling; eigen/adjoint GAIN the tail on LD, `_cell_average_angular` retires), `radial_characteristic` = `systems[1]` (arity 2, else `None` BY ARITY — the biconditional `solution.py:483-494` and the ray-space guard `:495-505` delete by construction), `boundary_flux` = `systems[0].boundary`, **`scalar_flux` DERIVED** = `∫ψ dΩ` of `state.systems[0].interior` through `_average_moment_scalar` (slot 0) `[R]` F3b — the marginal-axes guard `:457-478` deletes; ⚠ on the EIGEN path this is a PRINCIPLED RE-READ (today's field is the power iteration's φ; #448 measured the two 3.2e-10 apart): the 12 `.npz` + 16 `.npy` eigen pins are re-read at `flux_tol`, predicted green, the delta RECORDED (vv-principles' three criteria) | — |
| the outcome (kind-typed) | **`EigenOutcome(posing, state, lam, trajectory, gauge: ScaleGauge)`** / **`SourceOutcome(posing, state, gauge: KernelGauge)`** — `orpheus/numerics/outcome.py`, `Generic[V]`, frozen; F2 FUSED: `(EigenPosing, KernelGauge)`, `(SourcePosing, λ)` and a state separate from its question are UNCONSTRUCTIBLE | Solution | `residual()`, `balance(w)`, `rayleigh(w)` (eigen), `keff` (eigen; `ValueError` unless `spectral_map.name == "k"`), `dominance_ratio()` (eigen, from `trajectory`), `adjoint_posing` = `posing.H()` (eigen only — F1) | ✅ | α: the same type under `ALPHA_MAP` |
| the gauge `[R]` F5 | **`ScaleGauge(functional, target)`** — `apply(ψ) = ψ·target/functional(ψ)` (sign-fixing subsumes the dense engines' ℤ/2 leg), `displacement(ψ) = target/functional(ψ)`; **`KernelGauge`** — a numerics PROTOCOL (`gauge(trace)`, `dimension`) satisfied structurally by `LossKernelGauge` (`[H]` `protocol_conformers` after U1); the Solution records the section AND the displacement it applied — `orpheus/numerics/gauge.py` | Solution (the SECTION that picked the representative) | which representative, how far it was moved | SN values: forward eigen `ScaleGauge(solver.compute_production_rate, 1.0)` (fission + (n,2n) — the functional is the OBJECT that ran); adjoint `ScaleGauge(ke.compute_production_rate, 1.0)` (fission-only, daggered — a DIFFERENT functional, recorded as such); source `KernelGauge = hub.loss_kernel_gauge` (a zero-block projector when no freedom, `loss_kernel_gauge.py:1156-1170`) | homogeneous `ScaleGauge(problem.production_rate.evaluate, 100.0)` (U1) |
| the certificate `[R]` F6 | **`ExitCertificate(balance, gauge, rayleigh_gap, admissibility)`**, each an **`Evidence`** = `Measured(value) | Certified(bound, by) | NotApplicable(reason) | NotYet(issue, reason)` — the TYPE in `numerics/outcome.py`, the SN EVALUATOR beside the mints in `solver.py` (it needs `_balance_projection`/`_residual_is_expressible`; `solver.py:186` imports `solution` at runtime — a cycle otherwise) | Solution | what was measured about the RETURNED state, and why not | every member has a shipped witness (§6c): `balance` — `Measured` (truncated), `Certified(_CERTIFICATE_SAFETY·tol, "within-group certificate")` (fully converged), `NotApplicable("zero source")`, `NotYet(310)`, `NotYet(354)` (carrying eigen exit), `NotYet(353)` (adjoint eigen); `gauge` — `Measured`, `NotApplicable("no kernel freedom")`, `NotYet(…, verdict.because)` (undetermined closure); `admissibility` — `Certified(k, "the hub's k-solve at keff_tol")` (multiplying — the DROPPED `k` recorded with its configuration), `NotApplicable` (transport, eigen); `rayleigh_gap` — `Measured(|λ − posing.rayleigh(state)|)`, RECORDED never gated (an `assert` is inert under `-O`; a `raise` on a magnitude is N5). Retires the 5-way and 3-way `None`s (`[M]` 38 reads / 10 files, NOT the 95/20 `history` figure); `warn_if_unconverged` (5 callers) takes `Evidence` | CW enclosure `Enclosed(lo, hi)` (F7) |
| the path record | `record: IterationRecord` (direct field) | Strategy | where it stalled, what budget | **`IterationHistory` RETIRES** (routine F13): `record` → `record`; `keff_history` → `outcome.trajectory`; `balance_defect`/`gauge_correction` → `certificate`; its derived readings (`n_outer`, `n_inner`, `total_inner_iterations`, `flux_residuals`, `latest_*`) move to `IterationRecord` per its own docstring (`solution.py:222-228`). Landed as a one-cycle VIEW at U2 and retired at U6 — BOTH inside step 3 | — |
| `keff` `[R]` F4 | `EigenOutcome.lam` + the typed alias `EigenOutcome.keff`; `trajectory`; `dominance_ratio()` | Solution | λ | `Solution.keff`/`keff_history`/`keff_history_list`/`dominance_ratio`/`is_eigenvalue`/`is_fixed_source` RETIRE from the base; a source Solution has NO `.keff` (a pyright error). Readers → `sol.outcome.keff` / `.trajectory` — migrated PER FILE from the census's Solution-typed receiver list (`[M-census]` 174/43 + 13 derivations + 1 example + 4 pin loaders); NEVER tree-wide (`.keff` is a six-type homonym, ~135 other reads) | α: `lam` is α |
| `q` | the posing's second datum; identity by CONTENT on the POSING: `SourcePosing.__eq__`/`__hash__` = `operator` by `==` ∧ `source` by `to_flat().tobytes()` (routine F12) | Problem (the question) | "same question?" | ⛔ `with_source(q)` as a HUB morphism REFUTED: a hub carrying `q` is an Optional-source Problem (the `ProblemSpec` defect one tier down) and the multiplying entry runs its k-solve on the SAME hub. `_build_fixed_source_rhs`'s 17 test + 4 derivations direct callers UNCHANGED (they build the typed `q`) | — |
| adjoint, both arms (#467) | `AdjointSolution.outcome.posing = hub.eigen_posing.H()` (eigen) / `SourcePosing(hub.pencil.H.lhs, q*)` (fixed-source) | Problem (the daggered question) | the daggered pencil | the POSING recorded is the hub's; the Strategy pair stays `(implicit.H, gain.H)` per arm (R-cc2); `_adjoint_posing_parts` shrinks to the Strategy half (the Jacobi `Splitting` daggered, plus the seedless 1×1 lift onto the one-system coupled carrier) and stops returning `F`. ⛔ RESOLVED 2026-09-17 (test-architect F-2, `probes/p2_adjoint_1x1_lift.py`): the lift is constructible with NO production edit — `CoupledOperator([[M.H]], domain=hub.system.space, codomain=hub.system.space)` (`domain=`/`codomain=` are REQUIRED keyword-only — a §6b member the sketch omitted); the `[H]` "bit-identical" is REFUTED: `[M]` het slab GL-8 `keff_tol=1e-10`: today `k_adj = 1.216174144202823`, lifted pair `1.2161741442028244` (rel 1.095e-15), `hub.eigen_posing.H()` `1.216174144202825` (rel 1.643e-15), `n_outer = 5` on all three — a ULP-class re-baseline; the 15 certification rows (1e-9) clear it by six orders | #353 |
| balance functional (#462) | `Solution.balance()` — the SN certificate's `balance` = `‖R_g(outcome.residual())‖ / ‖R_g(posing.source)‖` (the per-group RATIO `_exit_balance_defect` computes) | Solution | the returned state's imbalance | `_exit_balance_defect` becomes the certificate EVALUATOR (4 sites → the mint); it reads `posing.residual` and KEEPS the SN projection — ⛔ CORRECTED 2026-09-17 (test-architect F-1): "NO reported number changes" holds for **4 of 5 entries** (they pose `at(0) is system.loss`, `[M]` bit-exact); the MULTIPLYING entry's number MOVES, `[M]` truncated subcritical solve (`L = 4`, `k = 0.907`, `max_inner=5`): today `0.8294593510371534` (measured against `A` — the residual of the WRONG equation, the lagged fission term counted as a defect) → `0.8758249879057027` against `A − F` (+5.59 %) — a REPAIR to predict and gate (battery arm A6′; anchor `TestRecordTheMultiplyingEntrysBalanceReadsTheLOSS`), not a regression; `posing.balance` stays the numerics scalar; the eigen exit's rebuilt rhs (`:2409-2423`) IS `eigen_posing.residual(state, λ)` (`Aψ − Fψ/k`) — one spelling | adjoint-weighted (F7) |
| `evaluate_residual` (Pattern 2) | `solver.py:328` — the typed full-system residual; callers pass `(system, ψ, q)` | SN tier | — | the NAME survives (10 callers); its body becomes the SN-typed realization of `SourcePosing(system.loss, q).residual(ψ)`; its arity guards delete once every state is a `CoupledField`. `[H]` the typed field algebra must admit `(source-role) − (A-output)`: measure `hub.source_posing(q).residual(ψ)` on typed fields FIRST; if it raises, the typed mint moves into the field algebra's `__sub__`, never a second body | — |
| `compute_keff` vs `rayleigh` | λ := the SN estimator's value (`keff_history[-1]`, bit-identical to today — 28 eigen pins at `keff_tol`); `certificate.rayleigh_gap` measures the gap to `posing.rayleigh` | Solution | the reference-class agreement | both spellings SURVIVE step 3 (the method-tier estimator the k-outer reads; `KEigenvalue.compute_keff` = `posing.rayleigh`); single-sourcing is a later ULP re-baseline | — |

### 3.2 The types, concretely
- **`orpheus/numerics/outcome.py`** (proposed; imports `posing`, `gauge`, `operator` only): `EigenOutcome(posing: EigenPosing[V], state: V, lam: float, trajectory: tuple[float, ...], gauge: ScaleGauge)` — `__post_init__`: `isinstance(posing, EigenPosing)` (the ONE kind parse, a boundary refusal), `trajectory[-1] == lam` when non-empty; laws: (i) `|rayleigh() − lam|` bounded by the recorded residual on the shipped slab, (ii) `posing.H().H() == posing`, (iii) NEGATIVE `EigenOutcome(posing=hub.source_posing(q), …)` is a pyright error AND raises. `SourceOutcome(posing: SourcePosing[V], state: V, gauge: KernelGauge)` — laws: (i) `residual()` closes at the solution, (ii) gauge residual-neutrality `‖residual(gauge.apply(state)) − residual(state)‖ == 0` to ULP (`[T]` `Πψ ∈ ker A`; `[M]` `solver.py:699-703`), (iii) NEGATIVE `SourceOutcome(posing=hub.eigen_posing, …)` refused. `Evidence` + `ExitCertificate` as §3.1 (NO `value_or_none` — it would re-import the leak).
- **`orpheus/numerics/gauge.py`**: `ScaleGauge` laws — `functional(apply(ψ)) ≈ target` (allclose), idempotence (allclose), Γ-invariance `apply(cψ) ≈ apply(ψ)` for `c ∈ (0.1, 10)`, the NEGATIVE leg (two gauges of one ray are distinguishable values — REDs today: nothing records which of the four functionals applied); `KernelGauge` Protocol laws — idempotence `Π(Πψ) ≈ Πψ`, Γ-invariance with `v` drawn from `Π`'s OWN RANGE on the TRACE space, residual-neutrality. ⚠ `FORBIDDEN_EDGES["numerics"] = L2 | L3` (`transport` is L2): `ScaleGauge.functional` is a `Callable[[Any], float]`, never an `IntegratedReactionRate` import.
- **`SolutionBase[O]`** (`Generic[O]`; pyright decides the spelling — `[H]` §8.7): fields `mesh: SNMesh`, `outcome: O`, `strategy: Splitting`, `certificate: ExitCertificate`, `record: IterationRecord`; of today's six fields only `mesh` survives as a field; `angular_flux`/`scalar_flux`/`radial_characteristic`/`boundary_flux` are derived accessors (names unchanged — 0 reader edits), `keff`/`history` retire (§3.1). `__post_init__` after the reshape: (1) role closure STAYS; (2) the interior-space check → ONE law *"`state` lives on `outcome.posing`'s domain"* — needs a derived **`CoupledField.space -> CoupledSpace`** (closes the inert ends check of §2.3 too); (3)–(5) the marginal, biconditional and ray-space guards DELETE by construction. Role leaves `Solution(SolutionBase[O])` / `AdjointSolution(SolutionBase[O])` unchanged (A5) — **2 leaves, not 4**.
- **The negative discriminator, honest:** (i) `EigenOutcome(posing=<SourcePosing>)` — pyright error + runtime `TypeError`; (ii) `replace(sol, outcome=o)` with `o.posing`'s domain ≠ `sol.mesh.system.space` → `ValueError` (a different Problem, F8 — a CROSS-HUB refusal; on one hub both kinds share the space, so a same-hub cross-kind `replace` is a LEGAL different solve); (iii) `compare` is role-closed AND kind-closed (`type(other.outcome) is type(self.outcome)`); `SolutionDiff.keff_abs: float | None` → `outcome.agrees(other.outcome, rtol)` folded into `within_tolerance`.
- **Verbs:** `residual()`/`balance()` → the outcome/certificate; `converged()` → `record.fully_converged` (the `False`-on-no-history branch retires: a Solution always has a record); `homogenize`/`condense`/`reaction_rate_density`/`importance` unchanged (the ROLE axis); `is_eigenvalue`/`is_fixed_source` → `isinstance(sol.outcome, EigenOutcome)` at the 2 test files, then RETIRE.

### 3.3 The mint bodies and the five entries after the reshape
**`_package_solution` becomes the ONE mint** (`cls, mesh, *, outcome, strategy, certificate, record`); `_package_adjoint_solution` folds in (its only difference, the `integrate_angular` scalar, is now the derived accessor); the two inline `Solution(` mints retire; `_cell_average_angular` retires (the cell-average VIEW, if wanted, is a derived accessor over `_average_moment_scalar`). `[H]` eigen/adjoint LD readers of `angular_flux`'s shape — none known (the 16 `.npy` cases run DD); AST census at the opener over the 17-file coarse population (`grep -rlE "\.angular_flux" tests | xargs grep -lE LinearDiscontinuous`): reads that index/shape the trailing axis on an EIGEN Solution.

| entry | outcome handed | gauge | certificate | strategy / record |
|---|---|---|---|---|
| `solve_sn` | `EigenOutcome(hub.eigen_posing, state (gauged trace; seedless wrapped `CoupledField((ψ,))`), float(keff_history[-1]), tuple(keff_history), …)` | `ScaleGauge(solver.compute_production_rate, 1.0)` | balance `Measured`/`Certified`/`NotYet(354)`; gauge; `rayleigh_gap`; admissibility `NotApplicable` | `solver._inner.splitting`; `outcome.record` |
| `solve_sn_fixed_source` (SI / Krylov arms) | `SourceOutcome(SourcePosing(hub.pencil.at(0), lift(q)), state, …)` — F11 entry-side | `hub.loss_kernel_gauge` | balance (all four cases); admissibility `NotApplicable` | the arm's `splitting`; `record` |
| `solve_sn_multiplying_source` | `SourceOutcome(hub.source_posing(q), …)` | same | admissibility `Certified(k, keff_tol)`; **gains the hoisted `warn_if_unconverged`/`warn_if_gauge_freedom`** (stacklevel at the PUBLIC entry; the 7→8 pin moves in the same commit); EXPORTED from `orpheus/sn/__init__.py` | SI arm's |
| `solve_sn_adjoint` | `EigenOutcome(hub.eigen_posing.H(), ψ*, k_adj, trajectory, …)` (#467, `[H]` carrier lift) | `ScaleGauge(ke.compute_production_rate, 1.0)` | balance `NotYet(353)`; gauge (inert, recorded as `NotApplicable`) | the Jacobi `Splitting` daggered |
| `solve_sn_adjoint_fixed_source` | `SourceOutcome(SourcePosing(hub.pencil.H.lhs, q*), ψ*, …)` | same | balance `Measured`…; admissibility `NotApplicable` | same |

`solve_sn_fixed_source:3473-3486` reads its own `solution.record`/`certificate`; the ledger gate's 7 rows read `certificate.gauge` (U6); `examples/discrete_ordinates/demo_discrete_ordinates.py:93-95` + `plotting.py:63` repaired (`[M]` the demo already reads a non-existent `elapsed_seconds` — ungated).

### 3.4 Multi-family (routine F9) and `derivations/` (#465)
- **SN + homogeneous.** `HomogeneousResult` gains `outcome = EigenOutcome(problem.eigen_posing, state=phi_column, lam=k_inf, trajectory=(k_inf,), gauge=ScaleGauge(problem.production_rate.evaluate, 100.0))`; `k_inf`/`flux` derived over it (`.k_inf` reads survive); the `:436` rescale becomes `gauge.apply(phi_column)` — the ScaleGauge's first witness (the only shipped deliberately-named target). ⛔ RESOLVED 2026-09-17 (F-4): the outcome's `state` MUST be the posed `(ng, 1)` COLUMN — `[M]` `posing.rayleigh` refuses `(ng,)` loudly, but `IntegratedReactionRate.evaluate` silently ACCEPTS `(ng,)` and returns the WRONG number (`200.0` at 2g, `411.27` at 4g, where the column reads `100.0`; equal at 1g) — so the gauge law's fixture is ≥ 2G, and the functional's silent shape leniency is a hazard to file (or close in U1 if one line). NOT in step 3: diffusion / CP / MoC (no pencils; trigger per family = "its hub grows `pencil`", diffusion first); MC out (F10; trigger = a fission-matrix tally). No shared base class (Pattern 6).
- **#465 minimally:** DELETE `ProblemSpec.external_source` (`[M-census]` 0 of 19 writers populate it; 13 explicit `None` args removed); `is_eigenvalue: bool` stays (1 reader) — its retype to a two-record kind is #418's (the derivations vocabulary plan), filed.

### 3.5 Forward-proofing
α: `EigenOutcome(EigenPosing(OperatorPencil(loss, time_mass), ALPHA_MAP), …, lam=α)` — no shape change; `keff` raises under `ALPHA_MAP`; the gauge is any degree-1 functional. Noise: a PATH of source posings ⟹ one `SourceOutcome` per `s`; a family-of-Solutions is a DRIVER value later, never a third kind; when "which s" must be a queryable datum, a `PencilSourcePosing` SUBTYPE (same kind, provenance added) — not a zero-`M` pencil on every posing. Depletion: "same Problem" is `==`; nothing on a Solution goes stale. The rename #412 LAST: the field stays `mesh` now. JAX #394: outcomes/gauges/certificate frozen; `ScaleGauge.functional` a pure callable. P5/P6: `strategy: Splitting` by reference — P5 changes its constructor, not what a value is.

### 3.6 Refuted alternatives (one structural line each)
S1 (kind-typed `posing` beside free `gauge`/`answer`): the cross-product is spellable. S2 (two kind classes × two roles): the verb set does not vary by kind — forced only if F1 is overruled. A third `MultiplyingSourceOutcome`: its extra datum is EVIDENCE (the certificate's), not a kind member. `with_source(q)` as a hub morphism: an Optional-source Problem; breaks the same-hub k-solve. A `SolveStrategy` record: the record already carries budgets/tolerances; the driver's TYPE is P5's. A `history` alias kept: re-imports the `None` discipline. A construction-time `assert |λ − rayleigh| ≤ r`: inert under `-O`; a gate on a magnitude. Cell-average as the ONE angular convention: strips the fixed-source arm's pinned moment tail. `scalar_flux` stored (the landing memo's objection): one quantity, two representations, a hand-written guard — overruled `[R]` F3b as a principled re-read. F11 hub-side `transport_posing(q)`: makes fission suppression read as a Problem datum. F11 σ on `SourcePosing`: every source posing would carry a pencil (a zero `M` for CP/diffusion — shape (A)). MC in: no posing. All six families: no pencils outside SN/homogeneous.

## 4. Verification (the test-architect is dispatched FIRST after approval and re-briefed with §3; its memo lands at `scratch/_consumers/step3/test_architect_step3.md`)
- **Gate vocabulary** (`python -O -m pytest -p no:randomly -m "not slow" -q --timeout=240`, SERIAL): **scope** = `tests/sn/primitives/test_solution.py` + `tests/sn/solve` + `tests/sn/regression` + `tests/sn/architecture` + `tests/sn/operators/test_step2_posed_fission_anchors.py` + `tests/numerics/{test_posing,test_pencil,test_outcome,test_family_convergence_contract}.py` + `tests/homogeneous` + `tests/transport/spatial/test_ld_slope_frame.py`; **wide** = `tests/sn` (~17 min); **ROOT** = `tests/ --ignore=<12 trees>` (~20 s; the layer-import, docstring-xref and error-catalogue rows); per commit = scope + wide + ROOT + ONE `sphinx -E -W` (`pgrep -f "MacOS/Python -m sphinx"` first) + `dead_references` (baseline 0 / 69); the 13-tree gate (detached + LOG, `scratch/_consumers/c3/gate13/driver.sh` pattern) on EACH merge unit's tip.
- **Exit prediction `[H]`:** baseline 11 792 / 21 / 55 @ `c930c68f`; predicted deltas (reconcile BEFORE explaining): U1 numerics +10..14, homogeneous +2, ROOT +2 (one fresh-interpreter entry × 2 rows); U2 sn +7..9 (net of re-posed rows), the `[solve_sn_multiplying_source]` warning row +1; U4 sn +2; U5 sn +1; U6 sn −11 + 6..8; U7 ±0 ⟹ ≈ **11 810–11 825 passed / 21 / 55**, rc=0 on U2's tip and U6's. The escalated regression run reproduces step 2's nine cases and ULPs at every unit except U4 (the `scalar_flux` re-read, recorded) and U5 (adjoint k ≤ 1e-15 rel `[H]`).
- **Bit-identity claims:** U1 by construction (the rescale arithmetic untouched); U2 by construction on `keff`/`scalar_flux`/`angular_flux` (same objects) — the 4 pin loaders need 0 edits at U2 (they read `.keff`/`.scalar_flux.values`/`.angular_flux.interior.values`), 1 at U6; U4 — fixed-source bit-identical (whole IS its convention), eigen `scalar_flux` a principled re-read within `flux_tol` (measured at the opener: `array_equal(sol.scalar_flux.values, integrate_angular(sol.angular_flux))` over the 16 finalize cases), eigen `angular_flux` gains the LD tail; U5 — NOT by construction (adjoint k through the 1×1 lift; no artefact pins an adjoint; the 15 certification rows are 1e-9 gates); U6/U7 by construction.
- **Defining-law tests per new type** (`feedback_test_intrinsic_properties`): §3.2's laws for `EigenOutcome`, `SourceOutcome`, `ScaleGauge`, `KernelGauge`, `Evidence`/`ExitCertificate` (each member witnessed by a shipped input — §6c), the `CoupledField.space` derivation, `SolutionBase`'s state-on-domain law with its cross-hub negative leg.
- **Battery** (mechanics as step 2: in-process monkeypatch plugins, one process per arm, `--timeout=240`, `cp -a` aside + `diff -q`; scope per arm = the files that can redden; read the red SET against the TARGET row — the AIM trap): A1 `ScaleGauge.apply` unscaled → TARGET the two-convention NEGATIVE leg + the SN `displacement` row (idempotence stays green — a stated null); A2 `KernelGauge` onto a non-kernel subspace → the trace-space Γ-invariance leg + the mirror-even-functionals row; A3 `EigenOutcome.__post_init__` accepts a `SourcePosing` → the type row ONLY; **A4 positive control** `outcome.lam × 1.01` → the 28 eigen pins + `test_the_bulk_and_keff_are_untouched` + the k-anchors; A5 the state-on-domain law disabled → the cross-HUB refusal rows only (same-hub declared null); A6 the certificate reads `posing.source` on both legs (ratio ≡ 1) → the certificate rows; A7 the multiplying entry records `NotApplicable` after admitting → the "k recorded" row ONLY (NOT the refusal leg — step 2's P-2 lesson); A8 `lam := keff_history[-2]` → the `rayleigh_gap` bound row (collateral: the 16 finalize pins at 1e-11); A9 re-mint `is_eigenvalue` as `keff is not None` → UNSPELLABLE (no `.keff` on the source kind) — a demonstration; A10 the multiplying entry's warning dropped → the new warning row + the 8-site pin; A11 a non-`solve_sn*` public entry → the ledger reads **0 red** (the discovery blind spot, recorded with the naming rule); A12 the seedless adjoint poses `hub.eigen_posing` UNdaggered → the k-equality rows stay GREEN (Mode-12 null, `k† = k`) — TARGET the adjoint-vector rows (`TestP14SphereAdjointVector` ×3, `TestP15BiOrthogonality` ×3, `test_4g_spectrum_matches_closed_form`); A13 the 1×1 lift drops the boundary block → the 15 certification rows + both anchors; A14 `certificate.gauge` reads `NotMeasured` on a singular box → the ledger's 7 rows + the typed-absence row; **A15 k-path positive control** cell-local ×1.01 on `hub.fission.isotropic_energy` → all 28 eigen pins.

## 5. Checkpoint rulings (AskUserQuestion, 2026-09-14) and the routine calls
- **`[R]` F3/F3b — the state:** whole `CoupledField` inside the outcome; ALL flux fields derived (`scalar_flux` included — a principled re-read on the eigen path, within `flux_tol`, recorded).
- **`[R]` F4 — `keff`:** retired from the base; `outcome.lam` + the typed `outcome.keff` alias on the eigen kind; a source Solution has no `.keff`.
- **`[R]` F5/F6 — gauge + certificate:** section objects (`ScaleGauge`/`KernelGauge`, `apply` idempotent, the functional as the object that ran) + typed evidence sums retiring the `None`s.
- **`[R]` F11 — the pure-transport question:** ENTRY-side — `solve_sn_fixed_source` poses `SourcePosing(hub.pencil.at(0), q)` itself (a Strategy-side point in Λ); the hub keeps ONE `source_posing(q)`; no σ field on the posing. (The user's question — *"it should not be difficult to zero an operator; is there a fundamental problem?"* — answered: none; `at(0) is lhs`; the fork was about what the posing RECORDS and who names the question.)
- **Routine calls (stated, overrulable):** F1 no `adjoint()` verb (S3 legal); F2 fused outcome; F7 capabilities FILED (CW enclosure, `admits(q)` #460, adjoint-weighted Rayleigh); F8 identity = `==`; F9 SN + homogeneous; F10 MC out; F12 `q` content-keyed on the posing; F13 `IterationHistory` retires (view at U2, gone at U6); `SourcePosing.residual` → `Aψ − q`; #465 minimal (delete the dead field); one new module pair (`numerics/outcome.py`, `numerics/gauge.py`); `Generic[O]` first, pyright decides.

## 6. Execution census `[M-census]` (explorer, AST unless stated; HEAD `80e117e5`) and the landing inventory

### 6.1 Populations (per-file lists in the census memo §3.4–3.6, scripts in its Appendix A)
- The carrier: 2 subclasses, 6 fields. Returners: 42 callables (orpheus 8 · tests 32 · derivations 2 + 4 fixtures · `tests/cross_method` 0 — it reads NO production Solution; its adapters wrap the continuous reference solvers, `k_eff`).
- Field reads on Solution-typed receivers: production **9 sites / 2 files**, NONE on `keff`; tests `keff` 174 / 43 · `scalar_flux` 128 / 47 · `history` 95 / 20 (67 attribute reads: `gauge_correction` 17, `converged` 17, `n_outer` 7, `record` 6, `n_inner` 6, `fully_converged` 6, `balance_defect` 4, `keff_history` 2, `total_inner_iterations` 1, `flux_residuals` 1 + 29 bare) · `angular_flux` 44 / 26 · `mesh` 24 / 10 · `radial_characteristic` 15 / 8 · `boundary_flux` 9 / 4 · `converged` 7 / 4 · `compare` 7 / 1 · `homogenize` 16 / 2 · `condense` 10 / 1 · `is_eigenvalue` 4 / 1 · `is_fixed_source` 5 / 2 · `keff_history` alias 4 (`test_cache.py:295/:305`, 2 in `examples/`); derivations `keff` 13, `history` 6, `angular_flux` 5, `scalar_flux` 4 (`derivations/diagnostics/` is UNGATED — migrate by census).
- Kind-derivation sites: 4 in-type + **13 test branch sites / 9 files** (11 are pyright narrowings on eigen Solutions; 2 RED under "no `.keff`": `test_solution.py:409`, `test_sn_adjoint_entries.py:259`); `getattr(…, "keff", None)` = 0. `.keff` is a SIX-type homonym (~135 other reads).
- Mints: 3 bodies / 5 entries (incl. the polymorphic `cls(` `:2537`); tests 35 `Solution(` + 6 `AdjointSolution(` + 1 `SolutionBase(` in 2 files; `replace(<solution>)` = 0 of 219. `IterationHistory(` **14** = 5 production (`solver.py:2429/2738/2901/3810/4010`) + 9 in `test_solution.py`.
- Entries and helpers: `solve_sn` 139 / 57 · `solve_sn_fixed_source` 140 / 63 · `solve_sn_adjoint` 11 / 9 · `solve_sn_adjoint_fixed_source` 7 / 3 · `solve_sn_multiplying_source` 3 / 1 (unexported) · `_build_fixed_source_rhs` 23 / 11 · `_adjoint_posing_parts` 7 / 3 (`solver.py:2674/:2840`; `test_step2_posed_fission_anchors.py:165/:361/:371`; `test_sn_adjoint_certification.py:408/:444`) · `_exit_balance_defect` 4 / 1 · `_exit_gauge_trace` 5 / 1 · `evaluate_residual` 10 / 4 + 6 pages · `HomogeneousResult(` 1 (`homogeneous/solver.py:469`).
- Pins: 12 `.npz` (`solve_sn` `keff` + `scalar_flux`; loaders `test_dd_regression.py:141-162`, `_generate_snapshots.py:690-697`, `test_phase_c_crosscheck.py:603`), 4 `.npz` fixed-source `scalar_flux`, **16 `.npy` cases** (the eigen finalize, `test_eigenvalue_finalize_reconstruction.py:826-855`), 3 `.npy` fixed-source `angular_flux` (`test_affine_carve_bit_identity.py:195-223`); artefact key strings `"keff"` ×2, `"angular_flux"`/`"scalar_flux"` as quantity labels — artefacts do not move; `test_typed_residual_evaluation.py:553` `hasattr(sol.angular_flux, "radial_characteristic")` (asserts ABSENCE on the composite — still true).
- `derivations/`: `ProblemSpec` 19 ctors / 9 files; `external_source` 0 of 19; `is_eigenvalue` read at 1 site; ⚠ homonym `\bis_eigenvalue\b` in `orpheus/` = 29 lines / 11 files, 28 the field — grep the METHOD form.
- Docs: 99 role xrefs / 10 pages + 24 in docstrings; `dead_references` 0 / 69.
- Warn-site pins: `test_family_convergence_contract.py:685` (`len(sites) == 7`), the signature pins `test_convergence_contract.py:916` / `…family…:676` (`balance_defect=` stays a keyword; the VALUE becomes `Evidence`).

### 6.2 Retirement list (3-search per name)
`is_eigenvalue`/`is_fixed_source` (9 call sites / 2 files + 3 docs lines + 2 docstrings; successor `type(sol.outcome)`) · the `keff_history` alias (4 live sites; the docstring's "~10" is stale) + `keff_history_list` (2 rows + 1 xref) · `dominance_ratio`/`latest_keff` on the history (7 + 3) · `IterationHistory` (14 constructions, 96 reads, 20 docs xrefs, 2 docstring xrefs — U6) · `_adjoint_posing_parts` (7 callers; docs 8 lines / 3 pages: `adjoint.rst:1838/1959/1994/2008`, `solver.rst:1392/1396/1694`, `history.rst:369`) · `_exit_balance_defect` (4 callers; docs 0 `:func:` xrefs; its N5 paragraphs in `solver.rst:3215` re-pointed) — `_balance_projection`/`_residual_is_expressible`/`_typed_balance` SURVIVE as the certificate's SN body (`[H]` their other same-file callers = `_certify_within_group_exit` — AST at the opener; if shared, evaluator and gate are one body, the right home) · the `__post_init__` guards (3 delete; the message fragment `R12a predicate` → `[H]` 0 pins) · `_cell_average_angular` + the two inline mints · `SNSolver.compute_keff` SURVIVES · the multiplying entry's dropped `k` → recorded.

### 6.3 Import placement (§6d)
`FORBIDDEN_EDGES["numerics"] = L2 | L3` (`tests/test_layer_imports.py:57`); `sn → numerics` 60 runtime is the established direction; `outcome.py` → `posing` → `pencil` → `operator` and `gauge.py` → `operator` add NO package edge (positive control: `numerics → geometry` = 3 runtime files); `KernelGauge` a Protocol satisfied structurally by `LossKernelGauge` (`[H]` `protocol_conformers` after U1); the certificate EVALUATOR lives in `solver.py` (a `solution.py` body would need `solver.py`'s helpers while `solver.py:186` imports `solution` at runtime — a cycle); add `orpheus.numerics.outcome` and `orpheus.numerics.gauge` to `test_entry_point_imports_in_a_fresh_interpreter` (`:236-252`; +2 ROOT rows each); inject-and-run in a COLD interpreter before the first gate; the AST edge census resolves relative imports (the 2026-08-31 landmine).

### 6.4 Landable units (each leaves the tree compiling and green; NO unit changes a k VALUE)
| unit | lands | §6b set | deletes | docs falsified (fixed in-unit) | bit-identity · pins |
|---|---|---|---|---|---|
| **U1** the outcome, gauge and certificate types are born; homogeneous adopts | `numerics/outcome.py` (`EigenOutcome`, `SourceOutcome`, `Evidence`, `ExitCertificate`), `numerics/gauge.py` (`ScaleGauge`, `KernelGauge` Protocol); `CoupledField.space`; `HomogeneousResult.outcome` (+ `k_inf`/`flux` derived); `tests/numerics/test_outcome.py`, `test_gauge.py`; 2 homogeneous rows | `HomogeneousResult(` 1; `solve_homogeneous_infinite(` 12 files (additive → 0 edits); the fresh-interpreter list | — | `operator_algebra.rst` four-tier table (the outcome row); the homogeneous page's "normalised to 100" | BY CONSTRUCTION · `tests/homogeneous/test_byte_stability.py:96-97` (`k_inf` hex + flux bytes 8/8) |
| **U2 (a+b+c) — ONE MERGE UNIT** the SN Solution carries outcome, certificate, record | (a) `SolutionBase[O]` fields `outcome`/`strategy`/`certificate`/`record`; `keff`/`is_*` retired; the 3 mint bodies + 2 tails take the new kwargs; `solve_sn` → `hub.eigen_posing` + `ScaleGauge(compute_production_rate, 1.0)`; `solve_sn_fixed_source` → `SourcePosing(hub.pencil.at(0), lift(q))`; the multiplying entry → `hub.source_posing(q)` + `Certified(k)` + the hoisted warnings + the export; `solve_sn_adjoint` → today's per-arm posing object (U5 swaps it); the `.keff` migration per file (`.outcome.keff`); (b) the certificate evaluator (`_exit_balance_defect`'s body) at the 4 sites; `warn_if_unconverged` takes `Evidence`; (c) `history` as a one-cycle derived VIEW; `compare` kind-closed; `SourcePosing.residual` sign flip + its law; `SourcePosing.__eq__` content key | the 42 test constructors; the polymorphic mint; the 5 `IterationHistory(` prod sites; 4 in-type + 13 test kind sites; the 174/43 + 13 + 1 `.keff` reads (per file); the `_k(sol)` helper (`test_sn_adjoint_certification.py:142-145`); the 7→8 warn pin; the AC-a chain UNTOUCHED (F11 entry-side); the step-2 anchors' `solution.keff` `:449` | `is_eigenvalue`/`is_fixed_source`; the `keff is not None` in `compare`; the "kind is a property" ruling `solution.py:36-46` RE-TENSED (never deleted) | `indexing_and_layout.rst:1000-1095`; `operator_algebra.rst:6458-6461` (past tense); `sn/adjoint.rst:2198-2215`; `sn/solver.rst:2840/:2730`; `sn/index.rst:976-1000`; `solver.py:2509` + `_package_solution`'s "3 of 4" paragraph | BY CONSTRUCTION on `keff`/`scalar_flux`/`angular_flux` (same objects) · ⛔ CORRECTED (F-12): **2 loader edits at U2**, not 0 — `test_dd_regression.py:150` and `_generate_snapshots.py:697` read `result.keff` off `run_case(cfg: dict)`, which carries no return annotation (the 2026-09-13 receiver-typing surprise, one hop further: an un-annotated helper makes the read invisible to a type-keyed census) — plus the untyped `_k(sol)` helper; the artefacts themselves do not move; the escalated regression run reproduces step 2's nine cases/ULPs; the multiplying entry's `balance` moves (F-1, a repair) |
| **U4** the state stored WHOLE; all flux fields derived (`[R]` F3/F3b) | `outcome.state: CoupledField`; `angular_flux`/`boundary_flux`/`radial_characteristic`/`scalar_flux` derived; the 3 guards deleted; `_system_b_member` at the mints retires into the accessor; ONE mint (`_package_solution`; `_cell_average_angular` + the 2 inline mints retire) | `radial_characteristic` 15/8, `angular_flux` 44/26 + 5, `boundary_flux` 9/4, `scalar_flux` 128/47 — GREEN (accessors keep the names); `[H]` eigen-LD `angular_flux` shape readers (AST at the opener) | the biconditional + ray-space + marginal guards; `_cell_average_angular`; the 2 inline mints | `indexing_and_layout.rst` carrier bullets; `solver.rst:2852` ("two flux members") → past tense; `_exit_gauge_trace`'s "coverage is GATED" paragraph | fixed-source BY CONSTRUCTION (3 `.npy` angular + 4 `.npz`); eigen `scalar_flux` a PRINCIPLED RE-READ within `flux_tol` (12 `.npz` + 16 `.npy`, measured first — §8.1); eigen `angular_flux` gains the LD tail (no pin) |
| **U5** the adjoint on the coupled carrier (#467) | both adjoint entries pose `hub.eigen_posing.H()`; the seedless arm iterates on the one-system `CoupledField` (the 1×1 lift of `implicit.H`/`gain.H`); `_adjoint_posing_parts` shrinks to the Strategy half | its 7 callers (2 prod, 5 tests); `TestLawTheHubsFissionIsWhatBothFacesRead` ×2 re-posed; `_sphere_*` helpers | the per-arm `EigenPosing(OperatorPencil(...H − ...H, F_posed.H))` at `:2678-2683` | `adjoint.rst:1838-2008` (incl. "arm-asymmetric by design" `:2008`), `solver.rst:1392/1396/1694`, `history.rst:369` | NOT by construction — the adjoint k through the 1×1 lift, measured at the opener (§8.2); the 15 certification rows + 2 anchors (1e-9 gates) |
| **U6** `history` migrates; `IterationHistory` retires | the view deleted; 67 attribute reads + 29 bare re-spelled by CONCEPT (`record.*` / `certificate.*` / `outcome.trajectory`); the ledger's 7 rows; `IterationRecord` gains the moved readings | 20 test files + 2 derivations diagnostics; `TestIterationHistory` ×11 → certificate/record rows; the 9 test constructions; the anchors' `adjoint.history` locals (`:281-283`, `:573-575`); `test_windowed_si_diagnostic_trajectory.py:224/249/268`; the pin loader `test_eigenvalue_finalize_reconstruction.py:333` (`sol.history.converged` → `sol.converged()`) | `IterationHistory` (`solution.py:115-345`) | 20 xrefs (`solver.rst` ×7, `history.rst:2103`, `cartesian_multid.rst` ×2, `indexing_and_layout.rst` ×6) + 2 docstring xrefs; the N5/N6b paragraphs `solver.rst:3084-3163/:3215/:3390-3419` | BY CONSTRUCTION (readers only) |
| **U7** #465 minimal | `ProblemSpec.external_source` deleted | 19 ctors / 9 files (13 explicit `None` args) | the dead field | `reference_solutions.rst:178`; `operator_algebra.rst:6473` (past tense) | BY CONSTRUCTION (`tests/derivations` 1636 rows) |
| **U8** corpus close | `cs4c_binding_design.md` §27.9 + the compaction point; the memos to `scratch/_consumers/step3/`; memory; the SN "Development history" row; the follow-up issues (F7's three + #418 pointer + the ledger's naming blind spot + the LD-tail view if wanted); the ERR entry if the archivist rules one (the multiplying entry's silent truncation) | — | — | ONE Sphinx build + `dead_references` | — |

**Order:** U1 → U2 (one branch, three commits, the 13-tree gate on the tip, one ff-merge) → U4 → U5 (independent files; sequential merges) → U6 → U7 → U8. **Why U2 is one merge unit (§6b):** the three mint bodies each spell `keff=` and `history=IterationHistory(record, keff_history, balance_defect, gauge_correction)` in ONE call — the kind member, the record and the certificate are three readers of one constructor set; landing the outcome without the certificate leaves the mints speaking two signatures. **The entry ledger:** U2 adds/renames/retires no `solve_sn*` (the export is a re-binding of the same function) — 5 rows stay; its `gauge_correction` rows ride the view at U2 and move at U6.

### 6.5 Docs (the archivist's brief at execution; docs only; NO agent Sphinx build)
U2: `indexing_and_layout.rst:1000-1095` (the carrier section: kind by TYPE, the outcome/record/certificate members, dated); `operator_algebra.rst:6458-6461` → past tense; `adjoint.rst:2198-2215` ("role is a type; problem kind is a property" → both are types, the reasoning kept as history); `solver.rst:2840/:2730`; `index.rst:988-1000`; `solution.py:36-46` re-tensed. U4: `solver.rst:2852`; the carrier bullets. U5: `adjoint.rst:1838-2008`; `solver.rst`; `history.rst:369`. U6: the 20 `IterationHistory` xrefs; the N5/N6b paragraphs. U7: `reference_solutions.rst:178`; `operator_algebra.rst:6473`. The SN "Development history" row (`history.rst:37-`, newest first). ERR candidate: the multiplying entry's silent truncation (a hoisted warning whose hoist site the fifth entry bypassed); the unrecorded gauge and the dropped `k` are defects of ABSENCE — the archivist rules. Stage `.claude/skills/vv-principles/error_index.md` + `docs/theory/verification/matrix.rst` with any carve that assigns an ERR (the Sphinx build regenerates both; ROOT re-run after).

## 7. Surprises logged this session (plan-authoring §2/§3)
1. `SolutionBase` has exactly 2 subclasses, both SN — §27.8.5's "across the six method families" premise was wrong.
2. The ground-count list mixed populations (§2.6).
3. `solve_sn_multiplying_source` discards the admissibility `k` it measured; its Solution reads as a plain fixed-source one; it is unexported; and it BYPASSES the hoisted convergence/gauge warnings (silent truncation, gated as correct by the 7-site pin).
4. No entry hands a posing to a Solution today; the ONE `source_posing` call is a bare statement.
5. `evaluate_residual` exists (10 calls / 4 files + 6 pages) — the step-2 memo asserted its absence twice.
6. 75 `.npy` pins outside the earlier `*.npz` predicate; 16 pin the eigen finalize.
7. `tests/cross_method/` reads no production Solution.
8. `ProblemSpec.external_source` is dead at 0 of 19 writers.
9. FOUR "production rate" functionals under one name; SN's includes the (n,2n) emission — `eigenvalue.py:429-432`'s "`∫νΣ_f φ = 1`" is true only on Σ₂-free mixtures.
10. `CoupledField` has no `.space` ⟹ the step-2 source posing's ends check is inert on every SN source.
11. The frame memo's torsor leg 2, negative discriminator, 0-ULP idempotence and dagger-involution claims — each corrected by a read (§2.7).
12. The certificate's reader population is 38 / 10, not `history`'s 95 / 20.
13. The design and landing memos DISAGREED on `scalar_flux` (derived vs stored); ruled derived with the re-read recorded.
14. `examples/discrete_ordinates/demo_discrete_ordinates.py:95` reads a non-existent `elapsed_seconds` — already broken, ungated.
15. ⚠ (2026-09-17, U2e) A census MAXIMUM was quoted from a GATE that stops at its first finding: the DD regression gate raises on `k_eff` before it reads `scalar_flux`, so the escalated instrument's per-case list carried k for six `.npz` and the "φ re-read ≤ 2.94e-11" written into the U2 commit message (un-amendable) was the maximum over the cases the gate REACHED φ on; `[M]` the full old-vs-new census reads 4.18e-11 (`cyl_1g_homogeneous_folded_4x8_dd_n20`, one of the six). Inside every band, so nothing was at risk — but it is plan-authoring §2's FILTER clause with the gate's stop-at-first-finding as the filter, and the 2026-09-14 row's order defect (written before reconciled). ⟹ a maximum over a set is read from a full comparison, never from a gate's first-hit report; a surprise-log row at U8.
16. The plan's "12 `.npz`" eigen artefacts are 10 (§9.3 item 8).

## 8. Risks and hypotheses — each with the measurement that settles it at the opener
1. ✅ MEASURED 2026-09-17 (`probes/p1_scalar_flux_derived.py`): eigen `scalar_flux` derived ≠ stored on **0 of 16** finalize cases by `array_equal`; worst relative gap **7.4094e-11** against the hard band `1e-8` (135× headroom) — every pin holds at its own tolerance. ⭐ **DECISION (F-11):** the escalated regression instrument (`-W error::DriftWarning`, the bit-identity spine) would go from 2 to ~28 permanently-drifting cases under the ruled re-read; U4 therefore RE-BASELINES the 12 `.npz` + 16 `.npy` eigen artefacts with their generators in the SAME commit, the per-case ratio (≤ 7.4e-11) recorded in the commit message, the plan and the theory page — vv-principles' three criteria (a structural reason: one quantity, one representation; the measured delta; the instrument returns to its step-2 reading).
2. ✅ MEASURED 2026-09-17 (F-2): the adjoint k through the 1×1 lift is NOT bit-identical (rel ≤ 1.7e-15 on two routes, §3.1) — U5 is a ULP-class re-baseline with no artefact pinning an adjoint; the certification rows clear it by six orders.
3. `[H]` eigen-LD `angular_flux` shape readers: AST over the 17-file coarse population; if > ~5 files, U4 keeps a cell-average VIEW accessor beside the whole state.
4. `[H]` the `.keff` migration reds exactly 2 sites under "no attribute" and pyright's `reportUnnecessaryComparison` level on the 11 narrowings — `npx pyright` on the 9 files at U2's draft.
5. `[H]` `_balance_projection`/`_residual_is_expressible` same-file callers — AST; if `_certify_within_group_exit` shares them, evaluator and gate are one body.
6. `[H]` `LossKernelGauge` conforms to `KernelGauge` structurally — `protocol_conformers(min_methods=2)` after U1.
7. `[H]` `Generic[O]` under pyright (13 annotated `-> Solution` returns become `Solution[…]`) — `npx pyright orpheus/sn/solution.py` on a draft; fallback a union-typed `outcome` field with `isinstance` narrowing.
8. `[H]` the typed field algebra admits `(source-role) − (A-output)` for `posing.residual` on SN fields — measure `hub.source_posing(q).residual(ψ)` first.
9. `[H]` homogeneous `posing.rayleigh(phi)` needs the pose's `(ng, 1)` shape — measure.
10. The 8-site warn pin + the caller-frame gate (`test_the_warning_blames_the_CALLER_not_orpheus`): the multiplying entry's warning must sit in the PUBLIC entry (stacklevel).
11. Ungated consumers (`derivations/diagnostics/`, `examples/`) migrate by census at U2/U6, never by gate.
12. Process constraints in force (§27.8 / L37 / L38): the main agent writes production; test-architect BEFORE any carve; archivist holds `docs/` only, no agent Sphinx build; ONE Sphinx build at a time; ROOT in every commit's gate set; the 13-tree gate on each merge unit's tip before the ONE ff-merge; never `git add -A`; stage by path; `ast.parse` after any textual repair; no source edit under a running gate.

## 9. Landing log (per unit — gates, batteries, surprises; the ⏹ banner at the top is written at the step's close)

### 9.1 Pre-carve — ✅ `d946ba9d` (the design memo) · `42b90c4b` (the anchors + the plan corrections F-1/F-2/F-3/F-4/F-11/F-12)
`tests/sn/architecture/test_step3_solution_anchors.py`: 31 rows — 5 THEOREM (the scanners against a planted member), 17 RECORD, 8 RULED strict xfails — `[M]` 23 passed / 8 xfailed on unmodified production; 50 / 8 with the two step-2 anchor files; pyright 0. The test-architect's memo `scratch/_consumers/step3/test_architect_step3.md` (12 findings §0; the per-unit new rows §2; 16 battery arms §4; the opener measurements §6 — 8.1 `array_equal` 0 of 16, worst rel 7.4094e-11; 8.2 the adjoint 1×1 lift rel 1.095e-15 / 1.643e-15 on two routes; 8.8 `SourcePosing(at(0), q).residual ≡ −evaluate_residual` at 0.0; 8.9 the `(ng, 1)` column; `rayleigh_gap` on the returned state 4.4e-12 … 2.6e-11 = 0.26 × keff_tol on 16 of 16).

### 9.2 U1 — the outcome, the gauge and the certificate are types; homogeneous adopts — ✅ LANDED (hash at the commit; branch `refactor/consumers-step3`)
**Landed (production):** `orpheus/numerics/gauge.py` (`ScaleGauge(functional, target)` — `apply`/`displacement`, the sign leg subsumed, a zero reading refused; `KernelGauge` a `runtime_checkable` Protocol — `gauge(trace)`, `dimension` — satisfied structurally by `LossKernelGauge`); `orpheus/numerics/outcome.py` (`EigenOutcome(posing, state, lam, trajectory, gauge)` / `SourceOutcome(posing, state, gauge)`, `Generic[Carrier]` — the tree's UNBOUNDED TypeVar, because numpy's stubs cannot prove `ndarray ⊨ Vector` and the 0-D state is a column; the ONE kind parse in each `__post_init__`; `trajectory[-1] == lam`; `keff` refused under a non-k map; `Evidence = Measured | Certified | NotApplicable | NotYet`; `ExitCertificate(balance, gauge, rayleigh_gap, admissibility)`); `orpheus/numerics/coupled_system.py` (`CoupledField.space` DERIVED via `CoupledSpace.from_systems(member.space …)`; `SystemField` declares `space`); `orpheus/homogeneous/solver.py` (`HomogeneousResult.outcome` first field; `k_inf`/`flux` properties — `flux` a view of the posed `(ng, 1)` column; the rescale = `ScaleGauge(production_rate.evaluate, 100.0).apply(phi_column)`); `tests/test_layer_imports.py` (+2 fresh-interpreter entries).
**Tests:** `tests/numerics/test_gauge.py` (15 rows), `tests/numerics/test_outcome.py` (15), `tests/homogeneous/test_homogeneous_outcome.py` (3).
**Gate `[M]`** (`scratch/_consumers/step3/gates/u1/`, serial, `-O -p no:randomly -m "not slow" --timeout=240`, on `42b90c4b` + the U1 working tree): numerics 3441 / 2 skipped (baseline 3411 + 30 new = 3441 ✓) · transport 950 / 1 (= baseline) · homogeneous 92 (89 + 3 ✓) · sn 3741 / 1 / 44 xfailed (baseline 3718 / 36 + the 31 anchors = 23 passed + 8 xfailed ✓ exactly) · root 437 / 5 xfailed (433 + 2 entries × 2 rows ✓) — 5 of 5 rc=0; pyright 0 on the touched files; cold imports of both new modules and `orpheus.homogeneous.solver` OK; the byte-stability gate (k_inf hex + flux bytes, 8/8) holds bit-for-bit.
**Surprises:** (a) pyright's numpy-vs-`Vector` limitation — the outcomes are generic in `Carrier` (unbounded, the `power_iteration` precedent), the posing's carrier parameter erased on the outcome; (b) `IntegratedReactionRate.evaluate` silently accepts an `(ng,)` vector (F-4) — a hazard to file; the outcome's state is the column and the gauge law's fixture is ≥ 2G.
**Decision taken at the U2 design (recorded here, before the carve):** #467 (the adjoint on the coupled carrier, U5) FUSES into U2's merge unit — the state-on-domain law compares the recorded posing's domain with the state's space, and the seedless adjoint's per-arm posing lives on the full-field space while its state is lifted to the coupled one; recording `hub.eigen_posing.H()` while the driver iterates bare would be a posing twin for one unit. U2 = (a) types + mints + state whole, (b) certificate, (c) history view + `compare` + the `q` identity + the sign flip, (d) the adjoint on the coupled carrier via the 1×1 lift (`CoupledOperator([[M.H]], domain=…, codomain=…)`), (e) the eigen artefact re-baseline — one branch, one 13-tree gate on the tip, one ff-merge. The moment-slot reducer `_average_moment_scalar` moves onto the hub (`SNMesh.cell_average_moment`) so the derived `scalar_flux` needs no solver import (`solver.py` imports `solution` at runtime).

### 9.3 U2 — the SN Solution carries its outcome, strategy, certificate and record — IN PROGRESS on `refactor/consumers-step3` (2026-09-17; the carve's own decisions, recorded before the gate)
**Landed (production, uncommitted at the time of writing):** `sn/solution.py` — `SolutionBase[O]` (`O` a CONSTRAINED TypeVar over the two outcomes), fields `mesh, outcome, strategy, certificate, record`; `angular_flux`/`radial_characteristic` (by ARITY)/`boundary_flux`/`scalar_flux` (a `cached_property` reading — identity holds across reads) DERIVED; `keff`/`is_*`/`keff_history*`/`dominance_ratio` RETIRED; `history` a one-cycle VIEW (U6 retires); the state-on-domain law is the ONE guard; `compare` role- and kind-closed; `SolutionDiff.keff_abs: Evidence`. `sn/solver.py` — `_package_solution` the ONE mint (5 entries, 2 roles); RETIRED `_exit_balance_defect`, `_cell_average_angular`, `_package_adjoint_solution`, `_average_moment_scalar` (→ `SNMesh.cell_average_moment`), the two inline mints, the eigen exit's rebuilt fission rhs; NEW `_balance_evidence`/`_gauge_evidence`/`_rayleigh_gap_evidence`/`_exit_certificate`, `_returned_state`, `_as_coupled`, `_StateProductionRate`; the fixed-source entry poses `SourcePosing(pencil.at(0.0), q)` ENTRY-side (F11); the multiplying entry records `Certified(k, keff_tol)`, gains the hoisted warnings, is exported; **U2d fused**: `_adjoint_posing_parts` returns the Strategy value and lifts the seedless pair to the 1×1 `CoupledOperator` (`domain=`/`codomain=` keyword-only), `solve_sn_adjoint` drives `KEigenvalue(hub.eigen_posing.H(), implicit.H, gain.H)`, the adjoint fixed-source entry solves on the coupled carrier (#467). `numerics/convergence.py` — `warn_if_unconverged(..., balance_defect: Evidence)`. `sn/operators/loss_kernel_gauge.py` — `warn_if_gauge_freedom(sn_mesh, correction: Evidence)`. `sn/coupled_system.py` — `_system_b_member` returns `None` on a one-system coupled state (F-7's second guard). `sn/mesh/augmented_mesh.py` — `cell_average_moment`.
**Decisions taken at the carve (each a surprise against the plan, logged in place):**
1. ⚠ The recorded scale gauge must be a functional of the STATE the outcome carries, not of the scalar iterate: `[M]` the anchors' ruled gauge row raised `TypeError(float() … 'CoupledField')` on `ScaleGauge(solver.compute_production_rate, 1.0)` — SN's `compute_production_rate` takes φ. ⟹ `_StateProductionRate(solver)` composes the cell-average scalar reduction with the SN functional (fission + (n,2n)); the adjoint's `ke.compute_production_rate` already takes the coupled state.
2. ⚠ The returned eigen state sat OFF the section by the iteration residual (`[M]` `functional(state) = 1.0000000137` at the default tolerances — the #448 polishing step) ⟹ the section is APPLIED at both eigen mints (`state = gauge.apply(state)`; the adjoint's is a ×1.0 no-op), so `functional(state) == target` is a LAW of the answer (the anchors' `rtol=1e-9` row), and the derived φ sits on it too. A ~1e-8-relative move of the returned eigen ψ, inside `flux_tol`; the artefact re-baseline (U2e) records it with the 7.4e-11 φ re-read.
3. ⚠ U2d fused into U2 (the plan's §9.2 note): the seedless adjoint's per-arm posing lives on the full-field space while the recorded state is the coupled one, so the state-on-domain law would have refused it; with both arms on the coupled carrier the adjoint's `rayleigh_gap` is EXACTLY `0.0` (`KEigenvalue.compute_keff` IS the posing's quotient) and the seedless/carrying asymmetry is gone.
4. `[M]` F-1 confirmed at the carve: the multiplying entry's balance reads `Measured(…)` against `A − F` now (the smoke: `3.02e-06` on the default-tolerance slab, un-converged and AUDIBLE — `hit max_inner=1961` — where it was silent).
5. `[M]` #354 dissolves: the carrying eigen arm's balance is `Certified`/`Measured` through the posing's coupled `production` (the sphere smoke: `Certified(1e-06)`, `rayleigh_gap = 6.07e-08`).
6. The `.keff` migration: the rewriter (`probes/rewrite_solution_reads.py`, the census's receiver resolver made to WRITE) applied 208 sites / 53 files in one idempotent pass (re-run = 0 planned; every file re-parsed); 13 hand sites + the 6 the resolver could not type (`test_dd_regression.py:150`, `_generate_snapshots.py:697`, `test_d3_admission.py:115`, `test_eigenvalue_finalize_reconstruction.py:1315`, `test_sn_adjoint_certification.py:142-145/:408/:444`, `test_sn_adjoint_entries.py:111/:259`) by exact-text edits. ⚠ A first apply mis-sliced ONE site: `ast` column offsets are UTF-8 BYTE offsets and the line carried a `ψ` before the node — caught by the rewriter's own text-equality assert (L64's `ast.parse`-after-repair discipline, one file earlier), fixed by converting the column on the node's own line.
7. `tests/sn/primitives/test_solution.py` rewritten onto the carrier (45 rows: construction on a real hub with a real posing, the state-on-domain law with its cross-hub negative leg and the same-hub cross-kind LEGAL row, the kind by type, the record-reading diagnostics, the one-cycle view, the kind-closed compare, the role axis); the warn-site pin 7 → 8; the anchors: the 8 ruled rows flipped (markers deleted), 7 reddened record classes deleted, 20 rows green.
8. ⚠ `[M]` The escalated drift census on the PRE-step artefacts (`gates/u2/drift_before.log`; `-W "error::tests.sn.regression._regression_assert.DriftWarning"` — ⛔ `-W error::DriftWarning` is REFUSED, `builtins` has no such name — over `tests/sn/regression` + the finalize module + `test_affine_carve_bit_identity.py`): **26 failed / 91 passed** = the **10** eigen `.npz` + all **16** finalize `.npy`; the fixed-source artefacts (4 `.npz`, 6 octant `.npz`, 3 affine `.npy`) read 0 drift — bit-identical, as predicted. ⚠ The plan's "12 `.npz`" was wrong: `[M]` by the artefacts' own `case_kind` key the three `p1_aniso` snapshots are `fixed_source`; the eigen set is 10 (§6.1, §8.1 corrected here, not re-counted there). Per case the φ re-read reads ≤ **2.94e-11** rel (`slab_vac_L0`, band 1e-8) on the `.npy` and ≤ **2.12e-11** rel (`2d_2g_LS4_dd_8x4_het_si`, band 1e-9) on the `.npz`; six `.npz` cases report `k_eff` at 1–4 ULP FIRST — these are step 2's inherited C1 pencil-carve ULPs (its memo §6.4: 3/2/4/1/2/1/1/11/2, never re-baselined), and the DD gate raises on the first drifting quantity, so a case reporting k also carries the φ re-read beneath it. U2e re-baselines all 26 with their generators on the U2 tree and records the post-reading.
9. `[M]` Count reconciliation of the wide gate (reconciled BEFORE it was written): sn 3741 passed / 44 xfailed (U1) → **3738 / 36** = −11 anchor rows (`[M]` `--collect-only` on a temp copy of HEAD's `test_step3_solution_anchors.py`: **31 → 20**) + 8 xfail flips (44 → 36 ✓); `test_solution.py` 45 → 45 rows (rewritten, not resized). The two ERR-086 catchers post-date the wide run (the residue run: 54 passed) ⟹ predicted sn at the U2 commit **3740 / 36**; ROOT re-run after the ONE Sphinx build (the ERR-086 reconciliation rows read the regenerated index).
10. Post-gate residues (the archivist's six code-side findings + the ERR-086 catchers, `probes/u2_residues.py`): two dead `:func:` xrefs in `solution.py` (the retired `_exit_balance_defect`) re-pointed; the `Solution` docstring's "one type covers both kinds via the optional keff" re-tensed; the doctest's retired verbs; the adjoint `Returns` block; the "Until U2d" comment (U2d landed IN U2); the residual number's configuration (1e-8-class at defaults, 1e-10-class at the finalize gates'); `SupercriticalSourceProblem` exported beside its entry; the two unused `test_solution.py` fixtures deleted.
11. ✅ U2 COMMITTED `bcd9c83c` (82 files; ROOT 437 / 5; `sphinx -E -W` clean ×2; `dead_references` 0 / 66 after the anchors' stale `:meth:` was fixed; pyright 0). **U2e — the artefact re-baseline** (its own commit on the branch, the hash at §9.4): the 10 eigen `.npz` regenerated by `_generate_snapshots --case …` and the 16 finalize `.npy` by `--capture-baseline` on the U2 tree (`generator_commit` = `bcd9c83c`); `[M]` the instrument AFTER (`gates/u2e/drift_after.log`): **117 passed / 0 failed** — the bit-identity spine reads ZERO for the first time since step 2's C1 (its six inherited k ULPs are absorbed with the re-read; a later k-path drift on any of the 26 is loud again). `[M]` old-vs-new by numpy: the 16 `keff.npy` byte-identical (k bit-identical 16 of 16), the 10 `.npz` k moved by the same 0–4 ULP the census reported; φ old-vs-new max **4.18e-11** rel (`cyl_1g_homogeneous_folded_4x8_dd_n20`, band 1e-9 — a case the DD gate had reported by its k FIRST, so the census's "≤ 2.94e-11" was the instrument's first-quantity reading, not the set's maximum; the U2 commit message quotes that partial figure), the 16 `.npy` max 2.94e-11 (`slab_vac_L0`, band 1e-8). The theory page's two "FROZEN … unchanged" sentences (`solver.rst` ~:2933, ~:3736) re-tensed in the same commit.
