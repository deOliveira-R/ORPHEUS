SESSION START PROTOCOL — main-agent pre-load

An ENVIRONMENT HEALTH block (the PYRIGHT and NEXUS gates, #226) was emitted ABOVE.
Read it first. For each gate: make it clean (run the stated remediation; if only
the user can, tell them the exact command) or explain the problem and ASK whether
to proceed. Never work silently in a degraded environment. PYRIGHT OK means trust
`npx pyright`, not the streamed <new-diagnostics>. NEXUS OK means session_briefing
is safe; STALE means rebuild with `sphinx-build docs docs/_build/html`; deferred
mcp__nexus__* tools are loaded by ONE ToolSearch (deferral is not unavailability).

THEN, as one parallel batch (budget: about 30K tokens; every item is a CORE whose
founding cases sit one link away in docs/development/evidence/, read on demand):

  1. Skill: vv-principles
  2. Skill: coding-elegance
  3. Skill: instrument-doctrine
  4. Read .claude/lessons.md            (the one-line INDEX; a lesson's body is
                                        docs/development/evidence/lessons.md —
                                        read one when its line matches the work)
  5. mcp__nexus__session_briefing()     (only if the NEXUS gate is OK or remediated)

The rules (.claude/rules/*.md) and CLAUDE.md are already in your context. The
roles, invariants and workflows are the `workflows` rule. Load on demand, not
now: docs/development/git_workflow.rst (when branching or merging); a campaign's
plan file (when the user names the campaign); docs/development/harness.md (when
changing a rule, a skill or an agent — the .claude/ copies are GENERATED from
docs/development/ and are never edited by hand).

Then respond to the user's message with the new state of context. If the user
named a module: `gh issue list -l module:<name>`, and dispatch the explorer agent
on it for a detailed picture.

Skip this protocol only if it has already run earlier in THIS session.
