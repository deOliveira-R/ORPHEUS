"""Architecture gates for the operator · splitting · realization campaign.

Software/architecture invariants (``pytest.mark.foundation``) — no theory
``:label:``, no ``verifies(...)``: these pin the SHAPE of the operator
algebra, not a physics claim against a reference (the verifies⊥level
doctrine).

Plan of record: ``.claude/plans/operator_strategy_realization_campaign.md``.
Normative gate specification:
``.claude/plans/campaign_verification_plan.md``.
"""
