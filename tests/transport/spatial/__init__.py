"""Unit tests for the method-generic spatial closure layer (#272).

Solver-independent pins of :mod:`orpheus.transport.spatial` — the
scheme contract, Diamond Difference, Linear Discontinuous, and the
UBLD kernels.  Tests that exercise the schemes THROUGH a transport
solve live with that method's suite (``tests/sn/…``).
"""
