"""Symbolic *origins* of the Peierls integral form.

Each module here carries the symbolic / mathematical pre-history of
a piece of the Peierls form — the algebraic identity it relies on,
the boundary-condition formalism it uses — without itself being a
continuous reference. Re-exported from
:mod:`orpheus.derivations.continuous.peierls.origins.specular` for
the specular-BC family.
"""

from . import specular  # noqa: F401  (re-exported for downstream)
