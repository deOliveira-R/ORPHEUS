"""Method of Manufactured Solutions references.

The reference flux is *constructed* (analytically) and the
corresponding source is back-computed from the operator the solver
discretises. Tests verify the solver reproduces the constructed flux
under mesh refinement at the expected convergence order.
"""
