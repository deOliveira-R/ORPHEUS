"""Analytical Green's-function references — placeholder.

Tracked under Plan-2. The intent is to add a sub-package that
provides closed-form Green's-function references for fixed-source
transport problems (point/line sources in an infinite medium with
given material), useful as an L1 reference target for the SN and
MOC sweep verification ladder.
"""
