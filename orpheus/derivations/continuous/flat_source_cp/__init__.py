"""Flat-source collision-probability references.

Closed-form / semi-analytical CP eigenvalues for slab, cylinder, and
sphere geometries. The continuous reference each module commits to
is the flat-source CP integral form before any spatial mesh.

See the :mod:`~orpheus.derivations.continuous.peierls` sub-package
for the Peierls integral form with arbitrary spatial source shape;
this sub-package is restricted to the flat-per-region case that the
production CP solver in :mod:`orpheus.cp` discretises.
"""
